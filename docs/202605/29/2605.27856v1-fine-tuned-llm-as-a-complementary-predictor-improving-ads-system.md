---
title: Fine-Tuned LLM as a Complementary Predictor Improving Ads System
title_zh: 微调大语言模型作为辅助预测器提升广告系统
authors: "Hui Yang, Daiwei He, Kevin Jiang, Taejin Park, Kungang Li, Jiajun Luo, Yuying Chen, Xinyi Zhang, Sihan Wang, Haoyu He, Yu Liu, Lakshmi Manoharan, David Xue, Shubham Barhate, Runze Su, Duna Zhan, Ling Leng, Siping Ji, Jinfeng Zhuang, Alice Wu, Leo Lu, Han Sun, Zhifang Liu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27856v1"
tags: ["query:rec"]
score: 9.0
evidence: LLM作为广告推荐的辅助预测器
tldr: 广告推荐系统引入微调LLM作为辅助预测器，根据用户画像和历史行为预测感兴趣的广告主，增强候选生成并为排序提供先验。在大规模生产系统中，该方法带来显著离线提升和在线业务收益，证明LLM世界知识可高效用于广告，端到端提升检索与排序效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27856v1/fig-001.webp\", \"caption\": \"Figure 2: Serving architecture for large-scale inference. The deployment uses a distributed vLLM + Ray stackwith batching, prefix caching, fault tolerance, and incremental updates for efficient daily inference.\", \"page\": 5, \"index\": 1, \"width\": 482, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.27856v1/fig-002.webp\", \"caption\": \"Table 4: Feature ablation on the zero-shot 4B setup. Negative values indicate performance drops relative to the baseline.\", \"page\": 6, \"index\": 2, \"width\": 452, \"height\": 302}]"
motivation: 现有LLM在推荐系统的成功应用有限，尤其在广告领域缺乏生产级高效利用LLM的方法。
method: 使用微调的开源LLM作为广告主预测辅助模型，输入用户特征，输出可能感兴趣的广告主，融入候选生成和排序流程。
result: 在大规模广告系统中实现显著离线指标改进和可衡量的在线业务影响。
conclusion: 有针对性的LLM辅助预测可有效提升广告推荐全链路效果，为规模化LLM增强推荐提供了可行路径。
---

## 摘要
推荐系统在信息流、广告和短视频平台上推动着用户参与和变现，但将大语言模型的最新进展转化为推荐系统的收益仍属罕见，尤其是在广告和大规模生产级工业环境中。此前大语言模型在实际中的成功应用通常分为三类：（a）生成式检索，直接预测下一个项目用于候选生成；（b）后期重排序，使用大语言模型进行排序；以及（c）利用大语言模型进行辅助信号增强。我们为广告引入了一种互补范式：一个经过微调的开源大语言模型，不作为排序器使用，而是作为广告领域特定的辅助预测器，根据用户画像和历史行为预测可能的广告主。这种由大语言模型驱动的广告主预测增强了传统的候选生成，并为下游排序提供了信息丰富的先验知识。该方法在一个大规模生产广告系统中开发而成，带来了显著的离线改进和可衡量的线上业务影响，证明了大语言模型的世界知识和预测能力可以被高效利用。除了验证大语言模型在广告应用中的价值外，我们的结果表明，有针对性的辅助预测可以在检索和后期排序两个阶段都实现端到端的收益，为大规模大语言模型增强推荐提供了一条切实可行的路径。

## Abstract
Recommendation systems power engagement and monetization across feeds, ads, and short-video platforms, but translating the latest advances in Large Language Models into Recommendation Systems (RecSys) gains remains rare, particularly in advertising and production-scale real-world industry setups. Prior real-world LLM successes typically fall into three buckets: (a) generative retrieval that directly predicts the next items for candidate generation, (b) late-stage re-ranking that uses LLMs, and (c) auxiliary signal enrichment with LLMs. We introduce a complementary paradigm for ads: a fine-tuned open-source LLM used not as a ranker, but as an ads-specific ancillary predictor, forecasting likely advertisers from user profiles and histories. This LLM-driven advertiser prediction augments conventional candidate generation and provides informative priors to downstream ranking. Developed in a large-scale production advertising system, our approach produces substantial offline improvements and measurable online business impact, demonstrating that LLM world knowledge and predictive capacity can be efficiently harnessed. Beyond validating LLMs for ads applications, our results show that targeted ancillary predictions can unlock end-to-end gains across both retrieval and late-stage ranking, offering a practical path to LLM-enhanced recommendation at scale.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：
    *   推荐系统是大型互联网平台的基础，但将大语言模型直接应用于生产级推荐系统，尤其是广告系统，仍面临巨大挑战。
    *   现有大语言模型在推荐系统的成功应用主要局限于三类：生成式检索、后期重排序、以及辅助信号增强。
    *   广告系统特有的**广告主实体、定向约束、转化历史**等因素，与纯文本语义存在天然不匹配，导致当前大语言模型的成功经验难以直接复制。
    *   本文旨在探索一种**新的、互补的范式**，将大语言模型作为广告系统的增强组件，而非直接替代核心的检索或排序模块。

*   **整体含义**：
    *   论文证明了将微调的大语言模型作为**广告领域特定的辅助预测器**是可行且高效的。它能够从复杂的用户历史中提炼出可操作的广告主先验知识和意图总结，以一种成本可控的方式，同时提升检索和排序两个关键阶段的性能，为大规模广告推荐系统的大语言模型增强提供了一条务实路径。

### 2. 论文提出的方法论

*   **核心思想**：
    *   不采用大语言模型直接进行项目检索或排序，而是将其训练为一个根据用户画像和历史行为预测**未来可能发生转化的广告主**的辅助模型。
    *   预测出的广告主列表作为**额外信号**，注入到传统的候选生成和排序模型中，以弥补现有协同过滤信号的不足。

*   **关键技术细节**：
    *   **系统流程**：包含用户选择、特征编译、基于提示词的广告主预测和下游应用四个阶段。
    *   **用户与数据构建**：仅对高价值活跃用户进行推理；标签定义为预测窗口内（例如，未来7天）首次发生转化的广告主。
    *   **用户特征编译**：将异构的用户行为（站内搜索、站外转化、URL、类别、品牌等）和画像信息（年龄、性别）编译为结构化文本。
    *   **后训练策略**：
        *   **监督微调**：训练模型根据用户上下文预测单一的下一个广告主，任务设定精准。
        *   **强化学习优化**：在监督微调后，使用强化学习进行优化。模型被要求生成一个包含20个广告主和5个用户兴趣的排序列表。奖励函数 `R_total` 的设计旨在鼓励正确的排序和格式遵从：
            $$R_{\text{total}} = R_{\text{match}} - P_{\text{adv\_len}} - P_{\text{interest\_len}}$$
            其中匹配奖励 `R_match` 基于正确广告主在列表中的位置 `i`：
            $$R_{\text{match}}(i) = R_{\text{base}}(i) + R_{\text{bonus}}(i)$$
            $$R_{\text{base}}(i) = 0.1 \times (20 - i)$$
            排名靠前或格式不符都有相应惩罚。
    *   **语义ID增强**：通过继续预训练，将多模态内容语义ID融入大语言模型，使模型能理解超越文本的协同信号，进一步提升预测召回率。
    *   **下游集成**：
        *   **检索阶段**：将预测的广告主作为定向过滤条件，创建一个新的、补充性的候选生成通道。
        *   **排序阶段**：将预测的广告主和用户兴趣作为输入特征，供转化率模型使用。
    *   **推理服务**：采用分布式vLLM + Ray堆栈进行批量推理，利用前缀缓存和连续批处理优化效率。

### 3. 实验设计

*   **数据集/场景**：
    *   **V0 数据集**：实验性数据集，选取了特征序列长度足够（如匹配转化序列至少10条）的丰富用户。
    *   **V1 数据集**：与线上服务流量对齐的数据集，更接近生产环境，但特征表达性较弱。
    *   **线上 A/B 测试**：在Pinterest的美国市场信息流、相关Pin和搜索场景中，针对“购物广告”切片进行实验。

*   **基准对比方法**：
    *   **广告主预测任务**：对比了零样本提示、监督微调（不同解码策略）、以及强化学习（有/无显式推理提示）。
    *   **下游排序任务**：对比了在转化率模型中添加大语言模型特征前后的AUC变化。
    *   **在线检索任务**：对比了启用基于大语言模型的候选生成器前后的广告支出回报率。

### 4. 资源与算力

*   论文**未明确提及**训练和推理所使用的具体GPU型号、数量以及总训练时长。
*   文中仅提到了使用vLLM + Ray构建分布式推理服务，并通过“虚拟纪元与检查点”、“增量更新”等工程手段来提高效率和可靠性，暗示了大规模推理的成本是设计中重点考量的问题。

### 5. 实验数量与充分性

*   **实验数量**：论文设计了较为完整的实验链路，包括离线预测精度对比（表2、表3）、特征消融实验（表4）、下游模型离线效果评估（表5）以及在线A/B测试（表6）。总共约有4-5组核心实验。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了从模型自身能力（广告主预测）、到组件重要性（特征消融）、再到系统级影响（离线和在线）的完整验证链，逻辑链条清晰，说服力较强。
    *   **公平性**：在语义ID对比实验中，明确控制了“文本内容完全一致”这一变量，对比严谨。消融实验系统地评估了各类特征的价值。
    *   **潜在偏差**：实验主要在Pinterest平台及其特定用户群上进行，结论的普适性需要更多跨平台验证。V0数据集的构建标准（过滤长序列用户）可能导致在该数据集上的表现优于线上真实分布。

### 6. 论文的主要结论与发现

*   **后训练至关重要**：经过监督微调和强化学习优化的大语言模型，其广告主预测能力远超零样本提示，证明了领域适配的必要性。
*   **特征价值明确**：“用户过去有过转化的广告主”是预测未来广告主的最强信号，站外行为（URL、搜索）比站内搜索等信号更具预测力。
*   **强化学习有效**：强化学习阶段能显著提升Top-K召回率，但监督微调是基础。对于此类意图聚合任务，显式的“推理”提示并非必需，甚至可能无效。
*   **语义ID带来增益**：融入语义ID能利用文本之外的协同信息，在所有召回率指标上带来超过10%的显著提升。
*   **端到端系统收益**：该方法在离线转化率模型（PR-AUC显著提升）和在线广告支出回报率（提升+4.94%至+6.69%）上都取得了可衡量的正面业务影响，证明了大语言模型作为辅助预测器的价值。
*   **系统设计是成功关键**：候选生成器的模型目标、配额和用户分群等系统层面的设计决策，对最终在线收益至关重要。

### 7. 优点

*   **方法论创新**：提出了一种务实的、将大语言模型作为“辅助预测器”而非主排序器的新范式，有效规避了大语言模型在工业级推荐系统面临的延迟、成本和技术对齐等挑战。
*   **端到端验证**：从模型训练、离线评估到线上A/B测试，提供了完整的证据链，证明了方法在真实大规模生产环境下的有效性。
*   **务实的工程设计**：论文详细讨论了用户筛选、增量更新、分布式推理等工程优化手段，对工业界落地具有很高的参考价值。
*   **细致的实验分析**：通过特征消融、训练策略对比等实验，提供了深入的分析和洞察，如揭示了站外信号和语义ID的价值。

### 8. 不足与局限

*   **用户范围有限**：模型仅对具有足够历史行为特征的活跃高价值用户进行推理，这可能导致“马太效应”，对那些冷启动或低活跃度用户的提升有限。
*   **商业目标单一**：线上实验的收益仅在“广告支出回报率”这一电商广告指标上得到验证，对平台其他目标（如用户体验、非电商广告）的影响未充分探讨。
*   **实验规模与普适性**：结论基于Pinterest的特定业务场景和用户群，其方法的泛化能力有待在其他平台和业务中验证。
*   **长期影响未知**：实验结果是短期的A/B测试数据，该方法的长期效果（如用户厌倦、广告主生态变化）尚未可知。
*   **技术与成本细节缺失**：论文未披露具体的算力资源消耗和成本，这为评估其方案的经济可行性带来困难。同时，语义ID的线上效果也尚未验证。
*   **存在失败案例**：论文提及，如果候选生成器的配额控制不当，可能导致候选集中化和广告主多样性受损，说明该方案需要谨慎的调优。

（完）
