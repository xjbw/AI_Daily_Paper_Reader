Title: Linear and Neural Dueling Bandits with Delayed Feedback

URL Source: https://arxiv.org/pdf/2605.26554v1

Published Time: Wed, 27 May 2026 00:41:13 GMT

Number of Pages: 22

Markdown Content:
# Linear and Neural Dueling Bandits with Delayed Feedback 

Xiangyi Wang 1 Pingchen Lu 1 Jie Mao 1 Mingze Kong 1 Zhi Hong 1 Zhiyong Wang 2 Zhongxiang Dai 1

## Abstract 

Contextual dueling bandits form a cornerstone of preference-based decision-making, with crit-ical applications in recommender systems and large language model alignment. However, stan-dard algorithms rely on the idealized assumption of immediate feedback, a condition frequently violated in real-world scenarios such as prompt optimization. This setting introduces a unique theoretical challenge: unlike linear bandits, duel-ing bandit estimators lack closed-form solutions, rendering naive adaptations of standard weighting techniques biased. To address this, we formalize the problem of Contextual Dueling Bandits with Stochastic Delayed Feedback and propose two novel algorithms: Linear (LDB-DF) and Neural (NDB-DF) Dueling Bandits with Delayed Feed-back. Central to our approach is a novel estimator that integrates an Inverse Probability Weighting (IPW) mechanism directly into the loss function, ensuring unbiased correction for delayed or miss-ing feedback. We provide comprehensive theo-retical analysis, establishing an ˜O(d√T ) regret bound for the linear setting and sub-linear guaran-tees for the neural setting. Extensive experiments on both simulated and real-world datasets demon-strate the effectiveness of our proposed methods. 

## 1. Introduction 

The contextual multi-armed bandit (MAB) framework serves as a cornerstone of sequential decision-making under uncertainty, finding widespread application in recommenda-tion systems, online advertising, and personalized services (Li et al., 2010; Lattimore & Szepesv ´ari, 2020). While tra-ditional MAB algorithms predicate on absolute numerical rewards, many real-world scenarios—such as calibrating Large Language Models (LLMs) or ranking search results—   

> 1The Chinese University of Hong Kong, Shenzhen 2The Chi-nese University of Hong Kong. Correspondence to: Zhiyong Wang
> <zhiyongwang@cuhk.edu.hk >, Zhongxiang Dai <daizhongxi-ang@cuhk.edu.cn >.
> Preprint. May 27, 2026.

fundamentally rely on relative preference feedback. In these settings, assigning an absolute scalar score to a response is often cognitively burdensome and noisy for human annota-tors. In contrast, preference-based feedback, which requires an annotator to simply compare two candidate responses (e.g., x1 ≻ x2), is widely recognized as being more reliable and consistent (Yue et al., 2012; Yue & Joachims, 2009). This paradigm is formally captured by the Contextual Duel-ing Bandit (CDB) framework (Yue et al., 2012; Saha, 2021; Bengs et al., 2022), where the agent selects a pair of arms and updates its policy based on binary preference outcomes. Despite the theoretical success of CDBs, a critical aspect of real-world deployment remains largely unaddressed: the temporal dynamics of feedback. Standard CDB algorithms typically operate under the assumption that preference feed-back is observed immediately following an action. However, in many practical applications, feedback is inherently de-layed. For instance, in display advertising and e-commerce, user reactions such as conversions or clicks can lag behind impressions by hours or even days (Chapelle, 2014). Sim-ilarly, in the optimization of LLMs via human-in-the-loop reinforcement learning (RLHF), human evaluation is asyn-chronous and slow (Lin et al., 2024). Furthermore, feedback systems often impose a “waiting window”; if a response does not arrive within this window, it is treated as missing data (Vernade et al., 2017). Ignoring these delays introduces significant bias into reward estimation. While stochastic delayed feedback has been studied in standard MABs and Bayesian Optimization (BO), the unique mathematical structure of dueling bandits ren-ders existing solutions insufficient. For instance, in delayed BO, Verma et al. (2022) successfully employed a “weight-ing” strategy where pending observations are temporarily imputed with minimum values. Such approaches are often feasible in linear or kernelized settings where estimators admit closed-form solutions (e.g., ridge regression), allow-ing the bias from imputation to be theoretically bounded. However, this presents a unique theoretical challenge for preference learning: parameter estimation in contextual dueling bandits—typically formulated via Maximum Like-lihood Estimation (MLE) under the Bradley-Terry-Luce model—lacks a closed-form solution and exhibits complex, implicit dependencies on binary outcomes. Consequently, naively adapting standard weighting techniques induces a 1

> arXiv:2605.26554v1 [cs.LG] 26 May 2026 Linear and Neural Dueling Bandits with Delayed Feedback

systematic bias in the optimization landscape that cannot be easily rectified. To bridge this gap, we formalize the problem of Contex-tual Dueling Bandits with Stochastic Delayed Feedback and propose a principled solution that departs from heuristic imputation. We introduce two novel algorithms: Linear Du-eling Bandits with Delayed Feedback (LDB-DF) for linear rewards, and Neural Dueling Bandits with Delayed Feed-back (NDB-DF) to capture complex, non-linear reward land-scapes. Central to our approach is a novel estimator that integrates an Inverse Probability Weighting (IPW) mecha-nism directly into the regularized loss function. By scaling the contribution of observed feedback by the inverse of its arrival probability, this mechanism explicitly ensures that the expected gradient of the surrogate loss remains an unbiased estimator of the true gradient, effectively neutral-izing the bias introduced by stochastic observation. We provide a comprehensive theoretical analysis, establishing an ˜O(d√T ) regret bound for the linear setting and proving sub-linear regret guarantees for the neural setting via Neural Tangent Kernel (NTK) analysis. Extensive experiments on both simulated environments and real-world datasets demon-strate the superior performance of our methods compared to baselines that ignore feedback delays. 

## 2. Problem Setting 

Throughout this paper, we denote vectors using bold low-ercase letters (e.g., x) and matrices using bold uppercase letters (e.g., M). Let |A| represent the cardinality of a set A,and [m] denote the integer set {1, 2, . . . , m }. Given a posi-tive semi-definite (PSD) matrix M, we define the weighted matrix norm of a vector x as ∥x∥M = √x⊤Mx .Consider a sequential decision-making scenario where a learning agent interacts with an environment over T rounds. At each time step t ∈ [T ], the agent receives a set of arms Xt

and selects a pair (xt, 1, x t, 2). In a departure from standard bandit settings that provide scalar rewards, we operate under the dueling bandit paradigm, where the agent only receives a binary preference feedback yt ∈ { 0, 1} indicating which arm in the pair is superior. Distinct from standard settings, the preference feedback yt is not immediately available; instead, it is observed after a random delay Dt.We impose a hard observation threshold M . Under this setting, the feedback arrives within M rounds with proba-bility ρ = P(Dt ≤ M ), while it arrives outside this window (i.e., Dt > M ) with probability 1 − ρ. Following standard assumptions in delayed feedback literature (Vernade et al., 2017; Garg & Akash, 2020), we assume ρ is known. Con-sequently, from the agent’s perspective, at any given round 

t, we can only utilize the historical data that has already arrived, i.e., s + Ds ≤ t.

Modeling Preference Feedback. We assume the existence of a latent reward function f (x). For any pair of arms x

and x′, the pairwise preference feedback y = 1(x ≻ x′) is generated by the Bradley-Terry-Luce (BTL) model (Wang et al., 2025). Specifically, y is sampled from a Bernoulli distribution with probability: 

P (x ≻ x′) = μ(f (x) − f (x′)) ,

where μ(z) = 11+ e−z is the logistic link function. 

Performance Metric. Consistent with standard contextual dueling bandit literature (Bengs et al., 2022; Saha & Krishna-murthy, 2022), we evaluate the algorithm using cumulative regret: 

RT =

> T

X

> t=1

rt =

> T

X

> t=1

(2 f (x∗ 

> t

) − f (xt, 1) − f (xt, 2)) ,

where x∗ 

> t

= arg max x∈X t f (x) denotes the optimal arm in round t.We adopt the following standard assumptions regarding the preference model (Yue et al., 2012; Saha, 2021; Bengs et al., 2022; Wang et al., 2025): 

Assumption 2.1 (Standard Assumptions) .

A1. |μ(f (x)) − μ(g(x)) | ≤ Lμ|f (x) − g(x)|.

A2. ∀x ∈ X : min | ˙μ(f (x)) | ≥ κμ > 0.

A3. ∥ϕ(xt, 1) − ϕ(xt, 2)∥2 ≤ L, ∀t = 1 , . . . , T .The notation ˙μ(·) denotes the first-order derivative of the link function μ(·). The assumptions listed above are widely adopted in studies involving binary feedback and linear structures. For the specific case where μ is the logistic function, the Lipschitz constant is Lμ = 1 /4. Regarding Assumption (A3), while classical linear bandit analysis typ-ically assumes a bound on the norm of individual feature vectors (i.e., ∥ϕ(x)∥ ≤ L), our condition on the pairwise difference is structurally equivalent. By the triangle inequal-ity, the standard assumption implies ours with a scaling factor of 2L.

Reward Function Realizations. Following established conventions in contextual bandits (Zhou et al., 2020; Zhang et al., 2021), we consider two realizations for the latent reward function f :

2.1. Linear Dueling Bandits With Delayed Feedback 

In the linear setting, we posit that the reward function is linear in a fixed feature space, f (x) = θ⊤ϕ(x), where the feature map ϕ : Rd′

→ Rd satisfies ∥ϕ(x)∥2 ≤ 1. Under this assumption, the preference probability simplifies to: 

P (xt, 1 ≻ xt, 2) = μ  θ⊤(ϕ(xt, 1) − ϕ(xt, 2))  .

The objective is to estimate the unknown parameter θ ∈

Rd. However, the estimator must account for the observing 2Linear and Neural Dueling Bandits with Delayed Feedback 

mechanism: at any round t, we can only utilize feedback where s + Ds ≤ t.

2.2. Neural Dueling Bandits With Delayed Feedback 

In the neural setting,we allow the reward functions f to be non-linear functions. We approximate f (x) using a fully connected neural network (NN) h(x; θ) with depth L ≥ 2,width m, and ReLU activations. The network output is defined as: 

h(x; θ) = WLReLU (WL−1ReLU (· · · ReLU (W1x))) ,

where Wl represents the weight matrices. The parameter vector θ comprises all flattened weights, and g(x; θ) = 

∇θ h(x; θ) denotes the gradient. Analogous to the linear case, the primary challenge lies in training the network using only the subset of feedback avail-able within the observation window. To facilitate theoretical analysis via the Neural Tangent Kernel (NTK) regime, we adopt standard assumptions from the neural bandit litera-ture: 

Assumption 2.2 (Neural Assumptions) .

B1. |f (x)| ≤ 1, ∀x ∈ X .

B2. H ⪰ λ0I, for some λ0 > 0.

B3. ∀x ∈ X : ∥x∥2 = 1 and xj = xj+d/ 2.The last assumption in Assumption 2.2, together with the initialization of θ0, follows the standard practice in neural bandits (Zhou et al., 2020; Zhang et al., 2021), which en-sures that h(x; θ0) = 0 for all x ∈ X t, t ∈ [T ]. Moreover, the assumption xj = xj+d/ 2 is a mild condition commonly used in the neural bandit literature (Zhou et al., 2020; Zhang et al., 2021) and has been recently adopted in extensions to dueling bandits (Verma et al., 2025). This assumption is primarily for the convenience of regret analysis: for any context x with ∥x∥2 = 1 , one can always construct a padded context x′ = ( x⊤, x ⊤)⊤/√2 that satisfies this requirement (Zhou et al., 2020). 

## 3. Algorithms 

3.1. Linear Dueling Bandits with Delayed Feedback (LDB-DF) 

To address the challenge of parameter estimation under stochastic delays, we introduce the Linear Dueling Bandits with Delayed Feedback (LDB-DF) algorithm. The core difficulty in this setting is that naively utilizing available data introduces systematic bias. LDB-DF overcomes this by integrating an unbiased estimator with an optimistic ex-ploration strategy. Specifically, the algorithm relies on an Inverse Probability Weighting (IPW) mechanism to correct the gradient bias in the Maximum Likelihood Estimation. 

Estimating Reward Parameters with Observed Data (Line 5-Line 6). Unlike standard approaches that update the model immediately, LDB-DF maintains an estimator derived exclusively from the subset of historical data that has reliably ”arrived” within the valid window. At the onset of round t, the algorithm first updates the set of observed feedback Ot (Line 5). To formalize the observation mechanism, we define the ef-fective observable feedback y′ 

> s

for a past round s. From the learner’s perspective, any feedback with a delay exceeding the threshold M is strictly treated as zero: 

y′ 

> s

= ys1{Ds ≤ M }. (1) Under this model, the observation is governed by the rela-tion y′ 

> s

= μ(f (xs, 1) − f (xs, 2)) ρ + ϵs, where ϵs captures the stochasticity of both the preference generation and the random observation. Consequently, a naive Maximum Like-lihood Estimation (MLE) update restricted to these raw observations would suffer from significant selection bias: since the effective sample size is systematically reduced by the observation probability ρ = P(Ds ≤ M ), the estima-tor would incorrectly treat missing data as non-informative zeros. To rectify this, we introduce an Inverse Probability Weight-ing (IPW) strategy. We define the effective weight for the feedback from round s observed at round t as: 

ωs,t ≜ 1{Ds ≤ min( M, t − s)}

ρ . (2) By re-weighting observed feedback instances by ωs,t , we compensate for the mass of unobserved instances. Con-sequently, in Line 6, θt is determined by minimizing the following IPW-regularized loss function: 

Lt(θ) = −

> t

X

> s=1

h

ωs,t ys log μ(θ⊤∆ϕs)+ (1 − ωs,t ys) log μ(−θ⊤∆ϕs)

i

+ λ

2 ∥θ∥22,

(3) where ∆ϕs ≜ ϕ(xs, 1) − ϕ(xs, 2). The weight ωs,t effec-tively neutralizes the bias introduced by the observation mechanism where Ds > M . Simultaneously, LDB-DF maintains an aggregated information matrix Vt−1 (updated in Line 10) to quantify exploration uncertainty in the feature space: 

Vt−1 = λκμ

I+

> t−1

X

> s=1

(ϕ(xs, 1)−ϕ(xs, 2))( ϕ(xs, 1)−ϕ(xs, 2)) ⊤.

(4) It is crucial to note that while the parameter estimator θt

relies on the arrived feedback (weighted by ωs,t ), the design matrix Vt−1 is updated using the features of all selected pairs up to round t − 1, as context vectors are observed immediately upon action selection. 3Linear and Neural Dueling Bandits with Delayed Feedback 

Algorithm 1 Linear Dueling Bandits with Delayed Feedback (LDB-DF)  

> 1:

Input: Confidence δ ∈ (0 , 1) , Regularization λ, probability ρ ∈ (0 , 1) , delay threshold M . 

> 2:

Initialize: V0 ≜ λκμ I 

> 3:

Initialize: History of arrived feedback D ← ∅ . 

> 4:

for t = 1 , . . . , T do  

> 5:

Update observed set: Ot = {(s, x s, 1, x s, 2, y s) | s < t and feedback arrives at t}. 

> 6:

Compute θt = arg min θ Lt(θ) via Eq. (3) using currently available data.  

> 7:

Select first arm: xt, 1 = arg max x∈X t θ⊤ 

> t

ϕ(x). 

> 8:

Select second arm: xt, 2 = arg max x∈X t

h

θ⊤ 

> t

(ϕ(x) − ϕ(xt, 1)) + βt 

> κμ

∥ϕ(x) − ϕ(xt, 1)∥V −1

> t−1

i

. 

> 9:

Duel: Play (xt, 1, x t, 2). Feedback yt is observed after delay Dt. 

> 10:

Update Matrix: Vt ← Vt−1 + ( ϕ(xt, 1) − ϕ(xt, 2))( ϕ(xt, 1) − ϕ(xt, 2)) ⊤. 

> 11:

end for Arm Recommendation Strategy. Leveraging the estimated preference vector θt and the information matrix Vt−1, LDB-DF selects a pair of arms (xt, 1, x t, 2) from the current set Xt

via the following strategy: • First Arm Selection (Line 7). The first arm is selected greedily to maximize the estimated utility based on the current model θt:

xt, 1 = arg max 

> x∈X t

θ⊤ 

> t

ϕ(x). (5) • Second Arm Selection (Line 8). The second arm is chosen to balance exploitation and exploration by max-imizing an Upper Confidence Bound (UCB) relative to the first arm: 

xt, 2 = arg max 

> x∈X t

h

θ⊤

> t

 ϕ(x) − ϕ(xt, 1)+

βt

κμ

ϕ(x) − ϕ(xt, 1) V −1

> t−1

i

.

(6) Intuitively, Eq. (6) encourages selecting an arm x that either exhibits a strong predicted preference over xt, 1

(exploitation) or is sufficiently distinct from xt, 1 within the feature space defined by Vt−1 (exploration). The confidence radius βt accounts for uncertainty arising from both stochastic noise and feedback delays. 

Updating Interaction History (Line 9). Upon recommend-ing the pair (xt, 1, x t, 2), the environment generates a binary preference yt which is not immediately observable. Instead, it arrives after an unknown stochastic delay Dt (Line 9). Conceptually, this feedback enters a “pending” state. It is only incorporated into the observed set Ot′ for training in a future round t′ once the arrival condition is met (i.e., 

t ≥ s + Ds) and provided the delay satisfies the observation constraint Dt ≤ M . Meanwhile, the information matrix Vt

is updated immediately using the context vectors of the se-lected arms (Line 10), as feature availability is independent of feedback latency. 

3.2. Neural Dueling Bandits with Delayed Feedback (NDB-DF) 

Recognizing the limitations of linear models in capturing complex reward landscapes, we propose Neural Dueling Bandits with Delayed Feedback (NDB-DF). This algorithm extends our IPW-based approach to non-linear function ap-proximation using neural networks. NDB-DF maintains a neural approximator updated online, crucially learning only from the subset of effectively arrived feedback. 

Estimating Reward Model with Data (Line 5-Line 7). 

Similar to the linear setting, NDB-DF trains the neural net-work using only the subset of feedback that has reliably arrived by the current round t and satisfies the observation constraint Ds ≤ M . To correct for the systematic bias in-troduced by this selective observation, we employ the same Inverse Probability Weighting (IPW) weights ωs,t as defined in Eq. (2). Defining the predicted preference difference as 

∆hs(θ) ≜ h(xs, 1; θ) − h(xs, 2; θ), the network parameters 

θt are obtained by minimizing the following regularized, probability-weighted cross-entropy loss: 

Lt(θ) = −

> t−1

X

> s=1

"

ωs,t ys log μ(∆ hs(θ)) + (1 − ωs,t ys) log μ(−∆hs(θ)) 

#

+ λ

2 ∥θ − θ0∥22.

(7) where θ0 represents the initialized parameters. The term 

ωs,t ensures the estimator remains unbiased despite stochas-tic observation. Additionally, we compute the aggregated information matrix Vt−1 (updated in Line 11) using Neural Tangent Kernel (NTK) features: 

Vt−1 = λκμ

I + 1

m

> t−1

X

> s=1

g′

> s

(θ0)g′

> s

(θ0)⊤, (8) where g′

> s

(θ0) ≜ g(xs, 1; θ0) − g(xs, 2; θ0) denotes the gradi-4Linear and Neural Dueling Bandits with Delayed Feedback 

ent difference at initialization. 

Arm Recommendation Strategy (Line 8-Line 9). Leverag-ing the trained network h(·; θt) and the information matrix 

Vt−1, NDB-DF recommends two arms as follows: • First Arm Selection (Line 8). The first arm is selected greedily based on the current neural point estimate to maximize the predicted reward: 

xt, 1 = arg max 

> x∈X t

h(x; θt). (9) • Second Arm Selection (Line 9). The second arm is selected via an optimistic strategy to encourage explo-ration. We choose the arm that maximizes the Upper Confidence Bound (UCB) of the preference difference relative to xt, 1:

xt, 2 = arg max 

> x∈X t

h

h(x; θt) + νT σt−1(x, x t, 1)

i

. (10) Here, the scaling factor νT is explicitly defined as: 

νT ≜ βT + B

s

λκμ

+ 1 

!

κμ

λ , (11) where βT represents the confidence radius. The explo-ration bonus σt−1 quantifies the epistemic uncertainty of the preference difference in the NTK feature space: 

σt−1(x, x ′) ≜

s

λκμ

1

√m

 g(x; θ0) − g(x′; θ0) 

> V−1
> t−1

.

(12) 

Updating Interaction History (Line 10-Line 11). Similar to the linear setting, the preference feedback yt is observed after a stochastic delay Dt (Line 10) and is added to the training set Ot′ only upon arrival (provided Dt ≤ M ). The information matrix Vt, however, is updated immediately. Crucially, unlike the linear case which uses raw features, NDB-DF updates Vt using the gradient difference at ini-tialization g′

> t

(θ0) (Line 11) to capture the geometry of the neural tangent kernel independent of feedback latency. 

## 4. Theoretical Analysis 

This section is dedicated to the theoretical analysis of our algorithms, where we establish rigorous regret bounds and discuss their implications. 

4.1. Regret Analysis for Linear Setting (LDB-DF) 

The following theorem establishes an upper bound on the cumulative regret for LDB-DF. 

Theorem 4.1. Suppose Assumption 2.1 holds. Let λ > κμL2. With probability at least 1 − δ for some δ ∈ (0 , 1) ,the cumulative regret of LDB-DF satisfies: 

RT = ˜O

 1

ρκ μ

√d + M



d√T



= ˜O

 Mρκ μ

d√T



. (13) The regret bound in Theorem 4.1 explicitly characterizes the impact of feedback latency. The term Mρ acts as a multi-plicative factor, underscoring two key intuitions. A smaller observation probability ρ implies that the effective sample size is reduced. Consequently, the variance of the estimator increases, scaling the regret by 1/ρ . Increasing the threshold 

M reduces data observation. A larger M implies tolerating longer feedback delays, which necessitates a wider confi-dence interval to bound the uncertainty, resulting in a linear penalty on M . This exacerbates the systematic bias in the es-timator, linearly increasing the regret. In the standard setting where feedback is immediate and lossless (i.e., ρ = 1 and 

M = 1 ), our regret bound simplifies to ˜O(d√T /κ μ). This fully aligns with the state-of-the-art regret bounds for stan-dard linear dueling bandits established in prior work (Saha, 2021; Bengs et al., 2022; Li et al., 2024), demonstrating that LDB-DF is a generalization of existing methods. The proof hinges on bounding the estimation error ∥θ −

θt∥Vt under delayed feedback. A primary challenge lies in managing the composite error introduced by the IPW estima-tor. To address this, we employ a novel “add-and-subtract” decomposition technique. Specifically, we decompose the gradient error into a martingale noise term and a systematic bias term, then analyze them separately. 

4.2. Regret Analysis for Neural Setting (NDB-DF) 

We now extend our analysis to the neural setting. 

Theorem 4.2. Suppose Assumption 2.2 holds. Let m be sufficiently large. With probability at least 1 − δ for some 

δ ∈ (0 , 1) , the cumulative regret of NDB-DF satisfies: 

RT = ˜O

p ˜dρκ μ

+ B

s

λκμ

+ Mκμmρ 

! p ˜dT 

!

= ˜O

 Mρκ μ

˜d√T



, (14) 

where ˜d is the effective dimension of the Neural Tangent Kernel (NTK) matrix. 

Mirroring the linear case, the neural regret bound scales linearly with the factor Mρ , capturing the dual sources of difficulty inherent to delayed feedback. Specifically, the inverse dependence on ρ reflects the variance inflation in the IPW estimator caused by a reduced effective sample size. Meanwhile, the linear dependence on M arises because 5Linear and Neural Dueling Bandits with Delayed Feedback 

Algorithm 2 Neural Dueling Bandits with Delayed Feedback (NDB-DF)  

> 1:

Input: Confidence δ ∈ (0 , 1) , probability ρ ∈ (0 , 1) ,regularization λ > 0, width M > 0. 

> 2:

Initialize: V0 ≜ λκμ I 

> 3:

Initialize: History of arrived feedback D ← ∅ . 

> 4:

for t = 1 , . . . , T do  

> 5:

Update observed set: Ot = {(s, x s, 1, x s, 2, y s) | s < t and feedback arrives at t}. 

> 6:

Let Dt be the cumulative set of all observations arrived by round t. 

> 7:

Train NN to find θt by minimizing Lt(θ) in Eq. (7).  

> 8:

Select first arm: xt, 1 = arg max x∈X t h(x; θt). 

> 9:

Select second arm: xt, 2 = arg max x∈X t [h(x; θt) + νT σt−1(x, x t, 1)] . 

> 10:

Duel: Play (xt, 1, x t, 2). Feedback yt is observed after delay Dt. 

> 11:

Update Matrix: Vt ← Vt−1 + 1 

> m

g′

> t−1

(θ0)g′

> t−1

(θ0)⊤. 

> 12:

end for 

tolerating longer feedback delays necessitates a wider confi-dence interval to bound the uncertainty, thereby imposing a linear penalty. This confirms that the fundamental hard-ness of the problem—governed by the trade-off between observation likelihood and delay magnitude—persists re-gardless of the reward function’s complexity. When ρ = 1 

and M = 1 , our result simplifies to ˜O( ˜d√T /κ μ), aligning with the bounds for Neural Dueling Bandits with immedi-ate feedback derived by Verma et al. (2025). The analysis proceeds in the Neural Tangent Kernel (NTK) regime. We employ a crucial “add-and-subtract” decomposition strat-egy on the gradient error. This decomposition allows us to explicitly bound the delay-induced bias by Mκμmρ .

## 5. Experiments 

In this section, we empirically evaluate the performance of our proposed algorithms, LDB-DF (Linear) and NDB-DF (Neural). We compare them against baselines that do not explicitly handle the bias caused by delays, demonstrating the necessity of our IPW-based delay-handling mechanism. 

5.1. Experimental Setup 

We conduct experiments on both controlled synthetic envi-ronments and a large-scale real-world prompt optimization benchmark. 

Synthetic Environments. To rigorously test the theoreti-cal properties of our algorithms, we generate synthetic data with varying reward structures. Linear Setting: We generate a ground-truth preference vec-tor θ∈Rd uniformly at random on the unit sphere. The reward function is strictly linear: f (x) = θ⊤x.Non-Linear (Neural) Settings: To evaluate the capacity of NDB-DF to capture complex dependencies, we de-sign two non-linear environments: (1) a Quadratic set-ting where f (x) = ( θ⊤x)2, and (2) a Cubic setting where 

f (x) = ( θ⊤x)3. In all synthetic experiments, we set the arm set size K = 20 and feature dimension d = 20 . The feedback delay Dt is sampled from a Geometric distribution to simulate stochastic network latency. 

Real-world Data. To demonstrate practical applicability, we apply our contextual dueling bandit framework to the task of Automated Prompt Optimization(Fernando et al., 2023; Lin et al., 2023; Chen et al., 2024). The goal is to identify the optimal prompt that elicits the highest quality response from a Large Language Model (LLM). We con-duct 29 instruction induction tasks from InstructZero (Chen et al., 2024), covering a wide range of settings, including sentiment analysis, reasoning, and translation. Following the experimental protocol of (Lin et al., 2024), we formulate the prompt optimization task as a contextual bandit problem. For each dataset, we generate a candidate pool of K = 500 

prompts. The reward of a selected prompt (arm) is calcu-lated as its accuracy on a held-out validation set of size 

N = 50 .

5.2. Baselines 

We compare our proposed methods against two categories of baselines to validate the effectiveness of our bias-correction mechanism. LDB-Ignore / NDB-Ignore: These are naive baselines that utilize the same underlying model architectures (Linear or Neural) as our proposed methods but simply disregard de-layed feedback. Pending observations are treated as missing and are excluded from the training set until they arrive, without any importance weighting. LDB-Heuristic / NDB-Heuristic: These baselines adopt the “hallucination” strategy adapted from delayed Bayesian Op-timization (Verma et al., 2022). Instead of ignoring pending feedback, these methods impute the missing outcome using the current model’s predicted preference probability (i.e., the posterior mean estimate). This predicted soft label is 6Linear and Neural Dueling Bandits with Delayed Feedback 

then substituted into the loss function to update the estima-tor, thereby utilizing the pending data albeit with a biased proxy. Comparing against Ignore baselines isolates the benefit of utilizing delayed data, while comparing against Heuristic 

baselines demonstrates the necessity of our unbiased IPW correction versus biased imputation. 

5.3. Results and Analysis 

We report the cumulative regret as the primary metric. For the Prompt Optimization task, we present the average cu-mulative regret across all 29 Instruction-Induction tasks to ensure the results are statistically robust and not driven by outliers. 

Linear Setting. Figure 1 presents the performance of LDB-DF compared to the baselines. In the Synthetic envi-ronment (Fig. 1a), LDB-DF significantly outperforms both LDB-Ignore and LDB-Heuristic. Both baselines suffer from significantly higher regret compared to our method. This demonstrates that neither disregarding delayed feedback (LDB-Ignore) nor naively imputing it with biased predic-tions (LDB-Heuristic) is sufficient for effective learning. Our IPW-based approach effectively bridges this gap by providing an unbiased correction. In the Real-world Prompt Optimization task (Fig. 1b), LDB-DF consistently achieves the lowest cumulative regret averaged across the 29 datasets. The shaded error region indicates that this performance gain is statistically significant. This confirms that a linear approx-imation with proper unbiased delay handling is far more effective than the suboptimal strategies employed by the baselines. 

Neural Setting. Figure 2 summarizes the results for NDB-DF. In the Non-linear Synthetic environments (Fig. 2a & 2b), NDB-DF demonstrates superior convergence speed and lower final regret compared to both NDB-Ignore and NDB-Heuristic in Quadratic and Cubic settings. Both baselines often converge to suboptimal regions. The naive strategies fail to capture the complex reward landscape: NDB-Ignore suffers from data sparsity, while NDB-Heuristic is misled by self-reinforcing prediction errors (hallucinations). In contrast, NDB-DF remains robust to feedback latency and consistently identifies the optimal arm. Similarly, in the Prompt Optimization tasks (Fig. 2c), NDB-DF effectively navigates the semantic search space. By using a neural approximator with our delay-aware objective, it captures non-linear nuances in prompt embeddings and significantly outperforms the biased and naive baselines.      

> (a) Synthetic ( d= 20 )
> (b) Prompt Opt. (Avg over 29 datasets)
> Figure 1. Linear Results. Comparison of LDB-DF (Ours) vs. LDB-Ignore. (a) Synthetic data. (b) Aggregated performance on Prompt Optimization.

## 6. Related Work 

Our work is situated at the intersection of contextual dueling bandits, neural bandits, and online learning with delayed feedback. 

6.1. Contextual Dueling Bandits 

The Dueling Bandit framework, where the learner receives relative preference feedback (e.g., A ≻ B) rather than ab-solute rewards, was introduced by Yue & Joachims (2009); Yue et al. (2012) to model scenarios like information re-trieval. Early algorithms focused on the K-armed setting (Zoghi et al., 2014; Komiyama et al., 2015). To handle large or continuous action spaces, the problem was extended to 

Contextual Dueling Bandits, typically assuming a linear re-lationship between the context vectors and the latent utility function (Saha, 2021; Bengs et al., 2022; Saha & Krish-namurthy, 2022). Recently, Li et al. (2024) and Di et al. (2023) have further improved the regret bounds for stochas-tic contextual dueling bandits, refining the theoretical under-standing of preference-based learning. However, all these works operate under the assumption of immediate feedback. They fail to account for the stochastic delays inherent in human-in-the-loop systems or complex evaluation pipelines, which can lead to suboptimal decision-making in practice. 7Linear and Neural Dueling Bandits with Delayed Feedback      

> (a) Quadratic
> (b) Cubic
> (c) Prompt Opt.
> Figure 2. Neural Results. Comparison of NDB-DF (Ours) vs. NDB-Ignore. (a) Quadratic synthetic. (b) Cubic synthetic. (c) Aggregated Neural Prompt Optimization.

6.2. Neural Bandits 

To address the limitations of linear models in complex real-world applications, Neural Bandits were proposed, utiliz-ing deep neural networks to approximate non-linear reward functions (Zhou et al., 2020; Zhang et al., 2021). This direc-tion has seen rapid development, with extensions to various settings such as shallow exploration (Xu et al., 2020) and batch learning (Dai et al., 2022). Most relevant to our work is the recent proposal of Neural Dueling Bandits by Verma et al. (2024), which combines the expressiveness of neural networks with preference-based feedback. While Verma et al. (2024) effectively addresses the non-linearity of re-ward functions, it does not address the temporal aspect of data collection, specifically the challenges posed by latency. Our NDB-DF algorithm extends this line of research by explicitly modeling and correcting for stochastic delays and missing data within the neural training process, offering a more robust solution for real-world deployment. 

6.3. Learning with Delayed Feedback 

Delayed feedback is a pervasive challenge in online learn-ing, extensively studied in the context of display advertis-ing (Chapelle, 2014) and conversion optimization (Vernade et al., 2017). Joulani et al. (2013) provided a foundational analysis for general online learning with delays. In the spe-cific context of bandits, Zhou & Gu (2019) and Vernade et al. (2017) developed algorithms for Generalized Linear Bandits that handle stochastic delays and missing data (where feed-back is lost if it arrives after a threshold). Garg & Akash (2020) and Grover & Lazic (2018) further explored best-arm identification and parallel experimentation under delay. However, existing delayed bandit algorithms predominantly focus on absolute numerical rewards (standard MAB or Lin-ear Bandits). To the best of our knowledge, no prior work has established a unified framework that simultaneously handles preference feedback (dueling), non-linear function approximation (neural), and stochastic delays/missing. Our LDB-DF and NDB-DF algorithms bridge this gap, provid-ing a comprehensive solution that integrates these three critical components. 

## 7. Conclusion 

In this work, we addressed the practical challenge of Con-textual Dueling Bandits under Stochastic Delayed Feedback, a setting pervasive in human-in-the-loop optimization. We formalized the problem and proposed two novel algorithms, LDB-DF and NDB-DF, which leverage Inverse Probabil-ity Weighting (IPW) to correct for censorship bias. Our theoretical analysis establishes the first regret bounds for this setting, explicitly characterizing the trade-off between observation latency and sample complexity in both linear and neural regimes. Empirically, our methods demonstrate significant robustness against stochastic delays, consistently outperforming base-lines across synthetic environments and large-scale Prompt Optimization benchmarks. These results validate our frame-work as a principled solution for asynchronous preference learning. Future directions include extending this approach to general delay distributions and exploring applications to preference-based reinforcement learning with feedback delays. 

## Impact Statement 

This paper presents work whose goal is to advance the field of Machine Learning, specifically in sequential decision-making under realistic constraints like latency. Our pro-8Linear and Neural Dueling Bandits with Delayed Feedback 

posed framework for Prompt Optimization could signifi-cantly enhance the efficiency of Large Language Models in various downstream tasks. There are many potential societal consequences of our work, none of which we feel must be specifically highlighted here. 

## References 

Abbasi-Yadkori, Y., P ´al, D., and Szepesv ´ari, C. Improved algorithms for linear stochastic bandits. In Advances in Neural Information Processing Systems , volume 24, 2011. Bengs, V., Busa-Fekete, R., and H ¨ullermeier, E. Stochastic contextual dueling bandits under linear stochastic transi-tivity models. In Proceedings of the 39th International Conference on Machine Learning , 2022. Chapelle, O. Modeling delayed feedback in display ad-vertising. In Proceedings of the 20th ACM SIGKDD International Conference on Knowledge Discovery and Data Mining , 2014. Chen, L., Chen, J., Goldstein, T., Huang, H., and Zhou, T. InstructZero: Efficient instruction optimization for black-box large language models. In Advances in Neural Information Processing Systems , 2024. Dai, Z., Chen, Y., Low, B. K. H., Jaillet, P., and Ho, T. L. Sample-then-optimize batch neural Thompson sampling. In Advances in Neural Information Processing Systems ,2022. Di, Q. et al. Nearly optimal algorithms for contextual du-eling bandits from adversarial feedback. In Advances in Neural Information Processing Systems , 2023. Fernando, C., Banarse, D., Michalewski, H., Osindero, S., and Rockt ¨aschel, T. Promptbreeder: Self-referential self-improvement via prompt evolution. arXiv preprint arXiv:2309.16797 , 2023. Garg, S. and Akash, K. Stochastic bandits with delayed composite anonymous feedback. In Proceedings of the International Conference on Machine Learning , 2020. Grover, A. and Lazic, N. Best arm identification in multi-armed bandits with delayed feedback. In Proceedings of the International Conference on Artificial Intelligence and Statistics , 2018. Joulani, P., Gy ¨orfi, A., and Szepesv ´ari, C. Online learn-ing under delayed feedback. In Proceedings of the 30th International Conference on Machine Learning , 2013. Komiyama, J., Honda, J., Kashima, H., and Nakagawa, H. Regret lower bound and optimal algorithm in dueling bandit problem. In Proceedings of the 28th Conference on Learning Theory , 2015. Lattimore, T. and Szepesv ´ari, C. Bandit Algorithms . Cam-bridge University Press, 2020. Li, L., Chu, W., Langford, J., and Schapire, R. E. Acontextual-bandit approach to personalized news article recommendation. In Proceedings of the 19th Interna-tional Conference on World Wide Web , 2010. Li, X., Zhao, H., and Gu, Q. Feel-good thompson sam-pling for contextual dueling bandits. In arXiv preprint arXiv:2404.06013 , 2024. Lin, X., Wu, Z., Dai, Z., Hu, W., Jaillet, P., and Low, B. K. H. Use your INSTINCT: Instruction optimization using neu-ral bandits coupled with transformers. In Proceedings of the International Conference on Machine Learning ,2023. Lin, X., Dai, Z., Verma, A., Ng, S.-K., Jaillet, P., and Low, B. K. H. Prompt optimization with human feedback. In Advances in Neural Information Processing Systems ,2024. Saha, A. Optimal algorithms for stochastic contextual pref-erence bandits. In Advances in Neural Information Pro-cessing Systems , volume 34, 2021. Saha, A. and Krishnamurthy, A. Efficient and optimal algo-rithms for contextual dueling bandits under realizability. In Proceedings of Algorithmic Learning Theory , 2022. Verma, A., Dai, Z., and Low, B. K. H. Bayesian optimization under stochastic delayed feedback. In Proceedings of the 39th International Conference on Machine Learning ,2022. Verma, A., Dai, Z., Lin, X., Jaillet, P., and Low, B. K. H. Neural dueling bandits. arXiv preprint arXiv:2407.17112 ,2024. Verma, A., Dai, Z., Lin, X., Jaillet, P., and Low, B. K. H. Neural dueling bandits: Preference-based optimization with human feedback. In International Conference on Learning Representations , 2025. Vernade, C., Capp ´e, O., and Perchet, V. Stochastic bandit models for delayed conversions. In Proceedings of the 33rd Conference on Uncertainty in Artificial Intelligence ,2017. Wang, Z., Sun, J., Kong, M., Xie, J., Hu, Q., Lui, J. C., and Dai, Z. Online clustering of dueling bandits. arXiv preprint arXiv:2502.02079 , 2025. Xu, P., Wen, Z., Zhao, H., and Gu, Q. Neural contextual bandits with shallow exploration. In International Con-ference on Learning Representations , 2020. 9Linear and Neural Dueling Bandits with Delayed Feedback 

Yue, Y. and Joachims, T. Interactively optimizing infor-mation retrieval systems as a dueling bandits problem. In Proceedings of the 26th International Conference on Machine Learning , 2009. Yue, Y., Broder, J., Kleinberg, R., and Joachims, T. The K-armed dueling bandits problem. Journal of Computer and System Sciences , 78(5):1538–1556, 2012. Zhang, W., Zhou, D., Li, L., and Gu, Q. Neural Thomp-son sampling. In International Conference on Learning Representations , 2021. Zhou, D. and Gu, Q. Learning contextual bandits in a non-stationary environment. In Proceedings of the ACM SIGMETRICS , 2019. Zhou, D., Li, L., and Gu, Q. Neural contextual bandits with UCB-based exploration. In Proceedings of the 37th International Conference on Machine Learning , 2020. Zoghi, M., Whiteson, S., Munos, R., and De Rijke, M. Relative upper confidence bound for the K-armed dueling bandit problem. In Proceedings of the 31st International Conference on Machine Learning , 2014. 10 Linear and Neural Dueling Bandits with Delayed Feedback 

## A. Overview and Notation 

This appendix provides complete proofs of the theoretical results stated in the main text. We organize the material as follows: • Section B: Full proof of the regret bound for the linear setting (Theorem 4.1). • Section C: Full proof of the regret bound for the neural setting (Theorem 4.2). 

Notation Recap. For the reader’s convenience, we summarize the key notation used throughout the proofs: 

Symbol Definition 

θ True (unknown) reward parameter 

θt Estimated parameter at round t (via IPW-weighted MLE) 

ϕ(x) Feature map of arm x

eϕs Pairwise feature difference: ϕ(xs, 1) − ϕ(xs, 2)

μ(·) Logistic link function: μ(z) = 1 /(1 + e−z )˙μ(·) Derivative of the link function 

κμ Lower bound on | ˙μ| (Assumption 2.1, A2) 

Lμ Lipschitz constant of μ (Assumption 2.1, A1) 

L Upper bound on ∥ eϕs∥2 (Assumption 2.1, A3) 

Ds Stochastic delay for round sM Hard observation threshold 

ρ Observation probability: P(Ds ≤ M )

ωs,t IPW weight: 1{Ds ≤ min( M, t − s)}/ρ Vt Information matrix at round tβt Confidence radius at round t

Key Technical Tool. A central ingredient in both the linear and neural proofs is the self-normalized martingale inequality due to Abbasi-Yadkori et al. (2011). We state it here for reference: 

Theorem A.1 (Self-Normalized Bound (Abbasi-Yadkori et al., 2011, Theorem 1)) . Let {F t}∞ 

> t=0

be a filtration. Let {ηt}∞

> t=1

be a real-valued stochastic process adapted to {F t} such that ηt is conditionally R-sub-Gaussian given Ft−1. Let {Xt}∞

> t=1

be an Rd-valued stochastic process with Xt being Ft−1-measurable. Define ¯Vt = V + Pts=1 XsX⊤ 

> s

for a positive definite matrix V . Then, for any δ > 0, with probability at least 1 − δ:

> t

X

> s=1

ηsXs 

> 2¯V−1
> t

≤ 2R2 log 

 det( ¯Vt)1/2 det( V )−1/2

δ



. (15) We also make use of the following standard determinant-trace inequality (Abbasi-Yadkori et al., 2011, Lemma 10): 

Lemma A.2 (Determinant-Trace Inequality (Abbasi-Yadkori et al., 2011, Lemma 10)) . If ∥Xs∥2 ≤ L for all s, then 

det( ¯Vt) ≤

 tr( V )+ tL 2

> d

d

.

Additionally, we use the following information gain bound (Abbasi-Yadkori et al., 2011, Lemma 11): 

Lemma A.3 (Information Gain Bound (Abbasi-Yadkori et al., 2011, Lemma 11)) . Suppose ∥Xs∥2 ≤ L and ∥Xs∥2¯V −1

> s−1

≤ 1

for all s ∈ [T ]. Then: TX

> s=1

∥Xs∥2¯V −1

> s−1

≤ 2 log det( ¯VT )det( V ) . (16) 11 Linear and Neural Dueling Bandits with Delayed Feedback 

## B. Proofs for the Linear Setting (LDB-DF) 

In this section, we provide the complete proof of Theorem 4.1. The proof proceeds through three key lemmas: (1) a confidence ellipsoid bound showing that the true parameter θ lies within an ellipsoid centered at the estimate θt (Lemma B.1); (2) a pointwise estimation error bound (Lemma B.2); and (3) an information gain bound that controls the sum of exploration bonuses (Lemma B.3). 

Proof Strategy. The main challenge in our setting, compared to standard linear dueling bandits, is that the MLE is computed from an incomplete dataset: only feedback satisfying Ds ≤ min( M, t − s) is available at round t. Our approach uses an “add-and-subtract” decomposition to isolate: (i) a martingale noise term (bounded via Theorem A.1), (ii) a delay-induced bias term (bounded deterministically by M L/ (ρκ μ)), and (iii) a regularization-induced term (bounded by pλκ μ/κ μ). 

B.1. Confidence Ellipsoid for the Linear Estimator 

The following lemma establishes that, under delayed feedback, the true parameter θ remains within a confidence ellipsoid centered at the MLE estimate θt, with a radius that explicitly accounts for feedback delays. 

Lemma B.1 (Confidence Ellipsoid—Linear Setting) . Under Assumption 2.1, define 

βt ≜

q

2 log(1 /δ ) + d log  1 + tL 2κμ/(dλ ) + M L + pλκ μ. (17) 

Then, with probability at least 1 − δ, for all t = 1 , . . . , T :

∥θ − θt∥Vt ≤ βt

ρκ μ

. (18) 

Proof. Let eϕs ≜ ϕ(xs, 1) − ϕ(xs, 2) denote the pairwise feature difference and ˜fs ≜ f (xs, 1) − f (xs, 2) = θ⊤ eϕs the true preference difference. For any θ′ ∈ Rd, define the auxiliary function: 

Gt(θ′) ≜

> t

X

> s=1

 μ(θ′⊤ eϕs) − μ(θ⊤ eϕs) eϕs + λθ ′. (19) We first establish that Gt is strongly monotone. For any θ′

> 1

, θ ′ 

> 2

∈ Rd, let ¯θ = λ′θ′ 

> 1

+ (1 − λ′)θ′ 

> 2

for some λ′ ∈ (0 , 1) . By the mean value theorem applied componentwise: 

Gt(θ′

> 1

) − Gt(θ′

> 2

) = 

" tX

> s=1

˙μ(¯θ⊤ eϕs) eϕs eϕ⊤ 

> s

+ λI

#

(θ′ 

> 1

− θ′

> 2

). (20) Since Assumption 2.1 (A2) ensures ˙μ(¯θ⊤ eϕs) ≥ κμ > 0 for all s, we obtain the lower bound: 

Gt(θ′

> 1

) − Gt(θ′

> 2

) ⪰ κμ

" tX

> s=1

eϕs eϕ⊤ 

> s

+ λκμ

I

#

(θ′ 

> 1

− θ′

> 2

) = κμ Vt (θ′ 

> 1

− θ′

> 2

). (21) By construction, Gt(θ) = λθ (since the sum telescopes to zero when θ′ = θ). Using the strong monotonicity from (21): 

∥Gt(θt) − λθ ∥2 

> V−1
> t

= ∥Gt(θ) − Gt(θt)∥2 

> V−1
> t

≥  κμVt(θ − θt)⊤V −1

> t

 κμVt(θ − θt)

= κ2

> μ

∥θ − θt∥2 

> Vt

.

Therefore, by the triangle inequality: 

∥θ − θt∥Vt ≤ 1

κμ

∥Gt(θt)∥V −1

> t

+ 1

κμ

∥λθ ∥V −1

> t

. (22) 12 Linear and Neural Dueling Bandits with Delayed Feedback 

Recall that θt is the minimizer of the IPW-regularized loss in (3) . Setting the gradient to zero yields the first-order optimality (MLE) condition: tX

> s=1



μ(θ⊤ 

> t

eϕs) − ys

ρ 1{Ds ≤ min( M, t − s)}

eϕs + λθ t = 0 . (23) Denoting ft,s ≜ θ⊤ 

> t

eϕs, we expand and rearrange: 

Gt(θt)=

> t

X

> s=1

 μ(ft,s ) − μ( ˜fs) eϕs + λθ t

=

> t

X

> s=1



μ(ft,s ) − 1

ρ ys1{Ds ≤ M } + 1

ρ ϵs

eϕs + λθ t, (24) where we used the identity μ( ˜fs) = 1

> ρ

 ys1{Ds ≤ M } − ϵs

, with ϵs ≜ ys1{Ds ≤ M } − μ( ˜fs)ρ being the observation noise. Adding and subtracting 1 

> ρ

ys1{Ds ≤ min( M, t − s)} and applying (23): 

Gt(θt) = 1

ρ

> t

X

> s=1

ϵs eϕs + 1

ρ

> t

X

> s=t−M

ys

 1{Ds ≤ min( M, t − s)} − 1{Ds ≤ M } eϕs. (25) where we denote the first summation as Term (I) (the martingale noise) and the second summation as Term (II) (the delay-induced bias). The cancellation of the MLE term works as follows: for any s < t − M , we have t − s > M , so 

min( M, t − s) = M . Thus, 1{Ds ≤ min( M, t − s)} = 1{Ds ≤ M }, and the difference in Term (II) vanishes. Only rounds s ∈ [t − M, t ] contribute to the bias. For Term (I), The noise sequence {ϵs} satisfies E[ϵs | F s−1] = 0 and |ϵs| ≤ 1, making it conditionally 1-sub-Gaussian. Moreover, eϕs is Fs−1-measurable. Applying Theorem A.1 with R = 1 and V = λκμ I:

> t

X

> s=1

ϵs eϕs 

> 2
> V−1
> t

≤ 2 log det( Vt)1/2

δ · det( λκμ I)1/2

!

, (26) with probability at least 1 − δ. By Lemma A.2, since ∥ eϕs∥2 ≤ L:

det( Vt) ≤

 λ/κ μ + tL 2/d 

1

d

·

 1

λ/κ μ

−d

, (27) which gives: 

log det( Vt)1/2

det( λκμ I)1/2 ≤ d

2 log 



1 + tL 2κμ

dλ 



. (28) Combining with (26): 

1

ρκ μ

> t

X

> s=1

ϵs eϕs 

> V−1
> t

≤ 1

ρκ μ

q

2 log(1 /δ ) + d log  1 + tL 2κμ/(dλ ). (29) For Term (II), Since |ys| ≤ 1 and the difference of indicators satisfies |1{Ds ≤ min( M, t − s)} − 1{Ds ≤ M }| ≤ 1, at most M terms contribute non-trivially. Using V −1 

> t

⪯ κμ 

> λ

I and ∥ eϕs∥2 ≤ L:

1

ρκ μ

> t

X

> s=t−M

1

ρ ys

 1{Ds ≤ min( M, t − s)} − 1{Ds ≤ M } eϕs 

> V−1
> t

≤ M L ρκ μ

. (30) 13 Linear and Neural Dueling Bandits with Delayed Feedback 

For the regularization term, Since Vt ⪰ λκμ I implies V −1 

> t

⪯ κμ 

> λ

I, and using the standard assumption ∥θ∥2 ≤ 1:

1

κμ

∥λθ ∥V −1

> t

= λκμ

q

θ⊤V −1 

> t

θ ≤ λκμ

r κμ

λ ∥θ∥2 ≤

pλκ μ

κμ

. (31) Combining, Substituting (29), (30), and (31) into (22): 

∥θ − θt∥Vt ≤ 1

ρκ μ

q 

2 log(1 /δ ) + d log  1 + tL 2κμ/(dλ ) + M L + pλκ μ



= βt

ρκ μ

, (32) which completes the proof. 

B.2. Pointwise Estimation Error 

The confidence ellipsoid immediately implies a pointwise bound on the estimation error for any pair of arms. 

Lemma B.2 (Pointwise Estimation Error—Linear Setting) . Under the conditions of Lemma B.1, for any t = 1 , . . . , T and all x, x ′ ∈ X t, with probability at least 1 − δ:

(f (x) − f (x′)) − θ⊤ 

> t

(ϕ(x) − ϕ(x′)) ≤ βt

ρκ μ

∥ϕ(x) − ϕ(x′)∥V −1

> t−1

. (33) 

Proof. Since f (x) = θ⊤ϕ(x) in the linear setting: 

(f (x) − f (x′)) − θ⊤ 

> t

(ϕ(x) − ϕ(x′)) = (θ − θt)⊤(ϕ(x) − ϕ(x′)) 

≤ ∥ θ − θt∥Vt−1 · ∥ ϕ(x) − ϕ(x′)∥V −1

> t−1

, (34) where the inequality follows from Cauchy–Schwarz in the Vt−1-norm. Applying Lemma B.1 yields the desired bound. 

B.3. Information Gain Bound 

The following lemma bounds the cumulative sum of exploration bonuses, which is essential for converting per-round regret bounds into a cumulative guarantee. 

Lemma B.3 (Information Gain—Linear Setting) . Under Assumption 2.1, choosing λ ≥ κμL2 ensures: 

> T

X

> t=1

∥ eϕt∥2 

> V−1
> t−1

≤ 2d log 



1 + T L 2κμ

dλ 



. (35) 

Proof. Since Vt−1 ⪰ λκμ I, we have V −1 

> t−1

⪯ κμ 

> λ

I. Therefore: 

∥ eϕt∥2 

> V−1
> t−1

≤ κμ

λ ∥ eϕt∥22 ≤ κμL2

λ ≤ 1, (36) where the last step uses the assumption λ ≥ κμL2. Since each summand is at most 1, the inequality x ≤ 2 log(1 + x) is applicable for x ∈ [0 , 1] . Thus: 

> T

X

> t=1

∥ eϕt∥2 

> V−1
> t−1

≤

> T

X

> t=1

2 log  1 + ∥ eϕt∥2 

> V−1
> t−1



= 2  log det VT − log det V0



= 2 log det VT

det V0

≤ 2d log 



1 + T L 2κμ

dλ 



, (37) where the second equality uses the matrix determinant lemma (rank-one updates), and the last inequality follows from Lemma A.2 with the bound det( VT ) ≤ (λ/κ μ + T L 2/d )d.14 Linear and Neural Dueling Bandits with Delayed Feedback 

B.4. Proof of the Main Regret Bound (Theorem 4.1) 

We now assemble the preceding lemmas to prove the main regret bound. 

Theorem B.4 (Restatement of Theorem 4.1) . Let βt be defined as in (17) and suppose λ > κ μL2. With probability at least 

1 − δ:

RT ≤ 32

βT

ρκ μ

q

2dT log  1 + T L 2κμ/(dλ ) = ˜O

 Mρκ μ

d√T



. (38) 

Proof. Recall that 2rt = f (x∗ 

> t

) − f (xt, 1) + f (x∗ 

> t

) − f (xt, 2). We bound each term using Lemma B.2: 

2rt = f (x∗ 

> t

) − f (xt, 1) + f (x∗ 

> t

) − f (xt, 2)

≤



θ⊤ 

> t

(ϕ(x∗ 

> t

) − ϕ(xt, 1)) + βt

ρκ μ

∥ϕ(x∗ 

> t

) − ϕ(xt, 1)∥V −1

> t−1



+



θ⊤ 

> t

(ϕ(x∗ 

> t

) − ϕ(xt, 2)) + βt

ρκ μ

∥ϕ(x∗ 

> t

) − ϕ(xt, 2)∥V −1

> t−1



, (39) where we applied Lemma B.2 to both terms (step (a)). For the second term, we decompose ϕ(x∗ 

> t

) − ϕ(xt, 2) = ( ϕ(x∗ 

> t

) − ϕ(xt, 1)) + ( ϕ(xt, 1) − ϕ(xt, 2)) and apply the triangle inequality to the norm (step (b)): 

2rt ≤ 2θ⊤ 

> t

(ϕ(x∗ 

> t

) − ϕ(xt, 1)) + 2 βt

ρκ μ

∥ϕ(x∗ 

> t

) − ϕ(xt, 1)∥V −1

> t−1

+ θ⊤ 

> t

(ϕ(xt, 1) − ϕ(xt, 2)) + βt

ρκ μ

∥ϕ(xt, 1) − ϕ(xt, 2)∥V −1

> t−1

. (40) By the selection rule for the second arm (Line 8 of Algorithm 1), xt, 2 maximizes the UCB over Xt. In particular, since 

x∗ 

> t

∈ X t:

θ⊤ 

> t

(ϕ(xt, 2) − ϕ(xt, 1)) + βt

ρκ μ

∥ϕ(xt, 2) − ϕ(xt, 1)∥V −1

> t−1

≥ θ⊤ 

> t

(ϕ(x∗ 

> t

) − ϕ(xt, 1)) + βt

ρκ μ

∥ϕ(x∗ 

> t

) − ϕ(xt, 1)∥V −1

> t−1

. (41) This implies that the first two lines in (40) are bounded by the UCB value of xt, 2 (step (c)): 

2rt ≤ 2θ⊤ 

> t

(ϕ(xt, 2) − ϕ(xt, 1)) + 2 βt

ρκ μ

∥ϕ(xt, 2) − ϕ(xt, 1)∥V −1

> t−1

+ θ⊤ 

> t

(ϕ(xt, 1) − ϕ(xt, 2)) + βt

ρκ μ

∥ϕ(xt, 1) − ϕ(xt, 2)∥V −1

> t−1

= θ⊤ 

> t

(ϕ(xt, 2) − ϕ(xt, 1)) + 3 βt

ρκ μ

∥ϕ(xt, 1) − ϕ(xt, 2)∥V −1

> t−1

. (42) By the selection rule for the first arm (Line 7), xt, 1 = arg max x θ⊤ 

> t

ϕ(x), so θ⊤ 

> t

ϕ(xt, 1) ≥ θ⊤ 

> t

ϕ(xt, 2), which means 

θ⊤ 

> t

(ϕ(xt, 2) − ϕ(xt, 1)) ≤ 0 (step (d)). Therefore: 

2rt ≤ 3 βt

ρκ μ

∥ϕ(xt, 1) − ϕ(xt, 2)∥V −1

> t−1

. (43) Using βt ≤ βT (since βt is non-decreasing) and applying the Cauchy–Schwarz inequality: 

RT =

> T

X

> t=1

rt ≤ 32

βT

ρκ μTX

> t=1

∥ eϕt∥V −1

> t−1

≤ 32

βT

ρκ μ

vuutT

> T

X

> t=1

∥ eϕt∥2 

> V−1
> t−1

15 Linear and Neural Dueling Bandits with Delayed Feedback 

≤ 32

βT

ρκ μ

q

2dT log  1 + T L 2κμ/(dλ ), (44) where the last step applies Lemma B.3. Substituting βT = p2 log(1 /δ ) + d log(1 + T L 2κμ/(dλ )) + M L + pλκ μ and absorbing logarithmic factors into the ˜O

notation: 

RT = ˜O

 1

ρκ μ

(√d + M ) d√T



= ˜O

 Mρκ μ

d√T



, (45) where the final simplification holds because M ≥ 1 (a delay threshold of at least one round is necessary). This completes the proof. 16 Linear and Neural Dueling Bandits with Delayed Feedback 

## C. Proofs for the Neural Setting (NDB-DF) 

In this section, we provide the complete proof of Theorem 4.2. The analysis proceeds within the Neural Tangent Kernel (NTK) regime, where sufficiently wide networks behave approximately as linear functions in the gradient feature space. We leverage this linearization to extend the confidence ellipsoid approach from the linear setting, while carefully controlling the approximation errors that arise from the non-linearity of the neural network. 

C.1. Preliminaries: Neural Tangent Kernel Framework 

We begin by recalling the key definitions and properties of the NTK framework that underpin our analysis. 

NTK Matrix Construction. Let {xn}T K n=1 denote the set of all context-arm feature vectors encountered across T

rounds with K arms per round: {xt,a }1≤t≤T, 1≤a≤K , indexed as n = K(t − 1) + a. The Neural Tangent Kernel matrix 

H ∈ RT K ×T K is constructed recursively as follows. Define: 

Σ(1)  

> p,q

= ⟨xp, x q ⟩, (46) 

A(l) 

> p,q

= Σ(l) 

> p,p

Σ(l)

> p,q

Σ(l) 

> q,p

Σ(l)

> q,q

!

, (47) 

Σ(l+1)  

> p,q

= 2 E(u,v )∼N (0 ,A(l) 

> p,q )

max {u, 0} max {v, 0}, (48) 

˜H(1)  

> p,q

= Σ (1)  

> p,q

, (49) 

˜H(l+1)  

> p,q

= 2 ˜H(l) 

> p,q

· E(u,v )∼N (0 ,A(l) 

> p,q )

1{u ≥ 0}1{v ≥ 0} + Σ (l+1)  

> p,q

. (50) The NTK matrix is then H =   ˜H(L)

> p,q

 + Σ (L), where L is the network depth. Under Assumption 2.2 (B2), this matrix satisfies H ⪰ λ0I for some λ0 > 0, ensuring the NTK is non-degenerate. 

Width Requirements. Our analysis requires the network width m to be sufficiently large, specifically: 

m ≥ poly  T, L, K, 1/κ μ, L μ, 1/λ 0, 1/λ, log(1 /δ ). (51) More precisely, m must satisfy the following conditions for some absolute constant C > 0:

m ≥ C T 4K4L6 log( T 2K2L/δ ) / λ 40, (52) 

m(log m)−3 ≥ C κ −3 

> μ

τ 8L21 λ−5, (53) 

m(log m)−3 ≥ C κ −3 

> μ

τ 14 L21 λ−11 L6

> μ

, (54) 

m(log m)−3 ≥ C T 14 L18 λ−8, (55) where τ = 2 pT / (mλ ) is an upper bound on the parameter drift (see Lemma C.2 below). These polynomial conditions ensure that: (i) the NTK approximation is valid; (ii) gradient perturbation bounds hold; and (iii) linearization errors are negligible relative to the confidence radius. 

Effective Dimension. Define the pairwise NTK Gram matrix H′ ≜ PTt=1 

P

> (i,j )∈

(K 

> 2

) 1 

> m

zti,j (zti,j )⊤, where zti,j ≜

g(xt,i ; θ0) − g(xt,j ; θ0). The effective dimension is: 

˜d ≜ log det 

 κμ

λ H′ + I



. (56) This quantity plays the role of the ambient dimension d in the linear setting and captures the intrinsic complexity of the problem in the NTK feature space. 

C.2. Key Lemmas for the Neural Setting 

We now state and prove the supporting lemmas needed for the main theorem. 17 Linear and Neural Dueling Bandits with Delayed Feedback 

Lemma C.1 (Linear Approximation of f (Zhang et al., 2021, Lemma B.3)) . Suppose the width m satisfies (52) . Then, with probability at least 1 − δ, there exists a parameter θf ∈ Rp such that for all x ∈ X t, t ∈ [T ]:

f (x) = ⟨g(x; θ0), θ f − θ0⟩, with √m∥θf − θ0∥2 ≤ √2 h⊤H−1h ≜ B, (57) 

where h = ( f (x1), . . . , f (xT K )) ⊤ is the vector of true rewards and p denotes the total number of network parameters. 

This lemma ensures that, in the NTK regime, the true reward function f admits an exact linear representation in the gradient feature space g(x; θ0) = ∇θ h(x; θ0) at initialization. 

Lemma C.2 (Parameter Norm Bound) . For all t ∈ [T ], the estimated parameters satisfy: 

∥θt − θ0∥2 ≤

r 2tmλ ≜ τ. (58) 

Proof. Since θt minimizes Lt(θ) defined in (7), we have Lt(θt) ≤ L t(θ0). From the regularization term: 

λ

2 ∥θt − θ0∥22 ≤ L t(θt) ≤ L t(θ0). (59) By Assumption 2.2 (B3) and the initialization scheme, h(x; θ0) = 0 for all x, so μ(h(xs, 1; θ0) − h(xs, 2; θ0)) = μ(0) = 1 /2.Evaluating the loss at θ0:

Lt(θ0) = −

> t−1

X

> s=1

[ωs,t ys log μ(0) + (1 − ωs,t ys) log μ(0)] + 0 = −

> t−1

X

> s=1

log(1 /2) = ( t − 1) log 2 ≤ t log 2 . (60) Therefore, ∥θt − θ0∥22 ≤ 2t log 2  

> mλ

≤ 2tmλ . Taking square roots yields the result. 

Lemma C.3 (Gradient Stability) . Under the width conditions (53) –(55) , for absolute constants C1, C 3 > 0 and with probability at least 1 − δ, the following hold for all x ∈ X t, t ∈ [T ]:

∥g(x; θt)∥2 ≤ C3

√m L, (61) 

∥g(x; θ0) − g(x; θt)∥2 ≤ C1 m1/3plog m

 tλ

1/3

L7/2. (62) 

Proof. Lemma C.2 guarantees ∥θt − θ0∥2 ≤ τ = 2 pt/ (mλ ), which satisfies the perturbation radius requirement of the gradient stability results in Zhang et al. (2021, Lemmas B.5 and B.6). Their results then directly yield (61) and (62). 

Lemma C.4 (Linearization Error) . Define ε′ 

> m,t

≜ C2 m−1/6√log m L 3(t/λ )4/3 for an absolute constant C2 > 0. Then, for all t ∈ [T ] and x, x ′ ∈ X t:

⟨g(x; θ0) − g(x′; θ0), θ t − θ0⟩ − (h(x; θt) − h(x′; θt)) ≤ 2ε′

> m,t

. (63) 

Proof. By the triangle inequality, the left-hand side decomposes as: 

⟨g(x; θ0) − g(x′; θ0), θ t − θ0⟩ − (h(x; θt) − h(x′; θt)) 

≤ ⟨g(x; θ0), θ t − θ0⟩ − h(x; θt) + h(x′; θt) − ⟨ g(x′; θ0), θ t − θ0⟩ . (64) Each term is the single-point linearization error, which is bounded by ε′ 

> m,t

using the gradient stability bounds from Lemma C.3 combined with the Taylor remainder estimate (see Zhang et al. 2021, Lemma B.4). Summing the two terms yields 2ε′

> m,t

.18 Linear and Neural Dueling Bandits with Delayed Feedback 

C.3. Confidence Ellipsoid for the Neural Estimator 

The following lemma is the neural counterpart of Lemma B.1. It establishes that the true reward function’s “linear proxy” 

θf lies within a confidence ellipsoid around the estimated parameters θt.

Lemma C.5 (Confidence Ellipsoid—Neural Setting) . Define βT ≜ 1

> κμ

q ˜d + 2 log(1 /δ ) and recall the information matrix 

Vt−1 = λκμ I + 1

> m

Pt−1 

> s=1

ϕ′

> s

(ϕ′

> s

)⊤, where ϕ′ 

> s

≜ g(xs, 1; θ0) − g(xs, 2; θ0). Under the width conditions (51) , with probability at least 1 − δ:

√m ∥θf − θt∥Vt−1 ≤ βT

ρ + B

s

λκμ

+ 1 + Mκμmρ , ∀ t ∈ [T ]. (65) 

Proof. For notational convenience, let ϕ′ 

> s

≜ g(xs, 1; θ0)−g(xs, 2; θ0) (gradient at initialization), ˜ϕ′ 

> s

≜ g(xs, 1; θt)−g(xs, 2; θt)

(gradient at current estimate), and ˜hs,t ≜ h(xs, 1; θt) − h(xs, 2; θt) (predicted preference difference). Define the auxiliary function for any θ′ ∈ Rp:

Gt(θ′) ≜ 1

m

> t−1

X

> s=1

μ(⟨θ′ − θ0, ϕ ′

> s

⟩) − μ(⟨θf − θ0, ϕ ′

> s

⟩)ϕ′ 

> s

+ λ(θ′ − θ0). (66) By the same mean value theorem argument as in the linear case (using Assumption 2.1 (A2)): 

Gt(θ′

> 1

) − Gt(θ′

> 2

) ⪰ κμ Vt−1 (θ′ 

> 1

− θ′

> 2

), (67) where Vt−1 = 1

> m

Pt−1 

> s=1

ϕ′

> s

(ϕ′

> s

)⊤ + λκμ I.Since Gt(θf ) = λ(θf − θ0) by construction, the same argument as (22) yields: 

∥θf − θt∥Vt−1 ≤ 1

κμ

∥Gt(θt)∥V −1

> t−1

+ 1

κμ

∥λ(θf − θ0)∥V −1

> t−1

. (68) The regularization term is bounded using Lemma C.1: 

1

κμ

∥λ(θf − θ0)∥V −1

> t−1

≤

s

λκμ

∥θf − θ0∥2 ≤

s

λκμ

B

√m . (69) Denoting ft,s ≜ ⟨θt − θ0, ϕ ′

> s

⟩ (linearized prediction) and using the identity μ(⟨θf − θ0, ϕ ′

> s

⟩) = μ(f (xs, 1) − f (xs, 2)) from Lemma C.1, we expand Gt(θt):

Gt(θt) = 1

m

> t−1

X

> s=1

μ(ft,s ) − μ(f (xs, 1) − f (xs, 2)) ϕ′ 

> s

+ λ(θt − θ0). (70) Using the observation model μ(f (xs, 1) − f (xs, 2)) = 1 

> ρ

(ys1{Ds ≤ M } − ϵs) and adding/subtracting the IPW-weighted observation 1 

> ρ

ys1{Ds ≤ min( M, t − s − 1) }, we obtain: 

Gt(θt) = 1

mρ 

> t−1

X

> s=1

ϵs ϕ′ 

> s

+ 1

mρ 

> t−1

X

> s=1

ys

 1{Ds ≤ min( M, t −s−1) } − 1{Ds ≤ M }ϕ′

> s

+ 1

m

> t−1

X

> s=1

 μ(ft,s ) − 1

ρ ys1{Ds ≤ min( M, t −s−1) }ϕ′ 

> s

+ λ(θt − θ0). (71) We refer to the three groups above as Term (I) (martingale noise), Term (II) (delay bias), and Term (III) (MLE residual with linearization error), respectively. 19 Linear and Neural Dueling Bandits with Delayed Feedback 

The first-order optimality condition for the NDB-DF loss (7) states: 

1

m

> t−1

X

> s=1

 μ(˜hs,t ) − 1

ρ ys1{Ds ≤ min( M, t − s − 1) } ˜ϕ′ 

> s

+ λ(θt − θ0) = 0 . (72) Note that this condition involves ˜ϕ′ 

> s

(gradient at θt) and ˜hs,t (network output at θt), whereas Term (III) involves ϕ′ 

> s

(gradient at θ0) and ft,s (linearized output). We bridge this gap by introducing two auxiliary error terms. Add and subtract ˜ϕ′ 

> s

and μ(˜hs,t ) inside Term (III): (III) = 1

m

> t−1

X

> s=1

 μ(ft,s ) − 1

ρ ys1{Ds ≤ min( M, t − s − 1) }(ϕ′ 

> s

− ˜ϕ′

> s

)+ 1

m

> t−1

X

> s=1

 μ(ft,s ) − μ(˜hs,t ) ˜ϕ′

> s

+ 1

m

> t−1

X

> s=1

 μ(˜hs,t ) − 1

ρ ys1{Ds ≤ min( M, t − s − 1) } ˜ϕ′ 

> s

+ λ(θt − θ0)

≜ A1 + A2 + 0 , (73) where the last line equals zero by the MLE first-order condition (72). Therefore, (III) = A1 + A2.We bound each term separately: For ∥A1∥2, Using |μ(ft,s ) − 1 

> ρ

ys1{Ds ≤ min( M, t − s − 1) }| ≤ 1 and Lemma C.3: 

∥A1∥2 ≤ 1

m

> t−1

X

> s=1

∥ϕ′ 

> s

− ˜ϕ′

> s

∥2

≤ 1

m

> t−1

X

> s=1

2C1 m1/3plog m

 tλ

1/3

L7/2

= 2 C1 m−2/3plog m t 4/3 λ−1/3L7/2. (74) For ∥A2∥2, By the Lipschitz property of μ (with constant Lμ) and Lemma C.4: 

|μ(ft,s ) − μ(˜hs,t )| ≤ Lμ|ft,s − ˜hs,t | ≤ 2Lμ C2 m−1/6plog m L 3

 tλ

4/3

. (75) Combined with ∥ ˜ϕ′

> s

∥2 ≤ 2C3

√m L from (61): 

∥A2∥2 ≤ 1

m

> t−1

X

> s=1

|μ(ft,s ) − μ(˜hs,t )| · ∥ ˜ϕ′

> s

∥2

≤ 4LμC2C3 m−2/3plog m t 7/3 L7/2 λ−4/3. (76) Since Vt−1 ⪰ λκμ I implies ∥v∥V −1

> t−1

≤ pκμ/λ ∥v∥2, we have: 

1

κμ

∥(III) ∥V −1

> t−1

≤ 1

κμ

r κμ

λ

 ∥A1∥2 + ∥A2∥2

. (77) Under the width conditions (53)–(55), both ∥A1∥2 and ∥A2∥2 are o(1 /√m), so their combined contribution satisfies: 

√mκμ

∥(III) ∥V −1

> t−1

≤ 1. (78) 20 Linear and Neural Dueling Bandits with Delayed Feedback 

The noise ϵs = ys1{Ds ≤ M }− μ(f (xs, 1)−f (xs, 2)) ρ is conditionally 1-sub-Gaussian (since |ϵs| ≤ 1 and E[ϵs | F s−1] = 0). The vectors 1√m ϕ′ 

> s

are Fs−1-measurable. Applying Theorem A.1 to the sequence {ϵs, 1√m ϕ′

> s

}:

> t−1

X

> s=1

ϵs

ϕ′

> s

√m 

> 2
> V−1
> t−1

≤ 2 log(1 /δ ) + log det( Vt−1)det( V0) . (79) Using the effective dimension bound: log det( Vt−1)det( V0) ≤ log det   κμ 

> λ

H′ + I = ˜d, we obtain: 

1

ρκ μ

> t−1

X

> s=1

ϵs

ϕ′

> s

√m V −1

> t−1

≤ 1

ρκ μ

q

˜d + 2 log(1 /δ ) = βT

ρ . (80) By the same argument as in the linear setting, for s < t − 1 − M the difference of indicators vanishes. At most M terms contribute, each bounded by ∥ϕ′

> s

/√m∥V −1

> t−1

≤ pκμ/λ . Therefore: 

√mκμmρ 

X

> s

ys

 1{Ds ≤ min( M, t − s − 1) } − 1{Ds ≤ M }ϕ′ 

> s
> V−1
> t−1

≤ Mκμmρ . (81) Multiplying (68) by √m and combining the above bounds: 

√m ∥θf − θt∥Vt−1 ≤ βT

ρ + Mκμmρ + 1 + B

s

λκμ

, (82) which completes the proof. 

C.4. Pointwise Estimation Error and Regret Bound 

We can now prove the main theorem for the neural setting. 

Theorem C.6 (Neural Pointwise Error and Regret—Restatement of Theorem 4.2) . Under the width conditions (51) and Assumptions 2.1–2.2, with probability at least 1 − δ, for all x, x ′ ∈ X t and t ∈ [T ]:

|[f (x) − f (x′)] − [h(x; θt) − h(x′; θt)] | ≤ νT σt−1(x, x ′) + 2 ε′

> m,t

, (83) 

where νT = ( βT /ρ + Bpλ/κ μ + 1 + M/ (κμmρ )) · κμ/λ and σt−1 is defined in (12) . Furthermore, the cumulative regret satisfies: 

RT = ˜O

p ˜dρκ μ

+ B

s

λκμ

+ Mκμmρ 

! p ˜d T 

!

= ˜O

 Mρκ μ

˜d√T



. (84) 

Proof. Using the linearization from Lemma C.1, f (x) − f (x′) = ⟨ϕ′ 

> x,x ′

, θ f − θ0⟩ where ϕ′ 

> x,x ′

≜ g(x; θ0) − g(x′; θ0). By the triangle inequality and Lemma C.4: 

|f (x) − f (x′) − (h(x; θt) − h(x′; θt)) |≤ |⟨ ϕ′ 

> x,x ′

, θ f − θt⟩| + |⟨ ϕ′ 

> x,x ′

, θ t − θ0⟩ − (h(x; θt) − h(x′; θt)) |. (85) The first term is controlled by the confidence ellipsoid (Lemma C.5), and the second term is bounded by the linearization error 2ε′ 

> m,t

(Lemma C.4). For the first term, applying Cauchy–Schwarz and Lemma C.5: 

|⟨ ϕ′ 

> x,x ′

, θ f − θt⟩| =

 ϕ′

> x,x ′

√m , √m(θf − θt)



≤ ϕ′

> x,x ′

√m V −1

> t−1

· √m ∥θf − θt∥Vt−1

21 Linear and Neural Dueling Bandits with Delayed Feedback 

≤ σt−1(x, x ′) · λ/κ μ

pλ/κ μ

· βT

ρ + B

s

λκμ

+ 1 + Mκμmρ 

!

= νT σt−1(x, x ′). (86) Combining (85) and (86) yields (83). The analysis mirrors the linear case. For the instantaneous regret 2rt = f (x∗ 

> t

) − f (xt, 1) + f (x∗ 

> t

) − f (xt, 2), we use the pointwise bound (83) (step (a)), the triangle inequality (step (b)), the Lemma C.4 to relate the linearized inner product to the network output (step (c)), and the arm selection rules (steps (d) and (e)): 

2rt ≤ (h(xt, 2; θt) − h(xt, 1; θt)) + 3 νT

ϕ′

> t, 1:2

√m V −1

> t−1

+ 6 ε′

> m,t

≤ 3νT

ϕ′

> t, 1:2

√m V −1

> t−1

+ 6 ε′

> m,t

, (87) where ϕ′ 

> t, 1:2

≜ g(xt, 1; θ0) − g(xt, 2; θ0) and the last step uses h(xt, 1; θt) ≥ h(xt, 2; θt) from the first-arm selection rule. The detailed steps (a)–(e) follow the same logical chain as in the linear proof (Section B.4), with the addition of Lemma C.4 to handle the gap between ft,s and h(·; θt).Under the width conditions, 6T ε ′ 

> m,T

≤ 1 (the linearization errors are negligible when the network is sufficiently wide). Therefore: 

RT ≤ 32 νTTX

> t=1

ϕ′

> t, 1:2

√m V −1

> t−1

+ 3 T ε ′

> m,T

≤ 32 νT

vuutT

> T

X

> t=1

ϕ′

> t, 1:2

√m 

> 2
> V−1
> t−1

+ 1 . (88) By Lemma A.3 (applied with the NTK features 1√m ϕ′

> t, 1:2

): 

> T

X

> t=1

ϕ′

> t, 1:2

√m 

> 2
> V−1
> t−1

≤ 2 ˜d, (89) where we used log det( VT )det( V0) ≤ ˜d and verified the unit bound condition ∥ 1√m ϕ′

> t, 1:2

∥2 

> V−1
> t−1

≤ 1 (which follows from λ/κ μ > 1

and 1 

> m

∥ϕ′

> t, 1:2

∥22 ≤ c0 for a constant c0). Substituting and using βT = 1

> κμ

q ˜d + 2 log(1 /δ ):

RT ≤ 32

βT

ρ + B

s

λκμ

+ 1 + Mκμmρ 

!

κμ

λ

p

2 ˜d T + 1 = ˜O

p ˜dρκ μ

+ B

s

λκμ

+ Mκμmρ 

! p ˜d T 

!

. (90) When the width m is sufficiently large (absorbing the 1/m factor), the dominant term is 

√ ˜dρκ μ

p ˜d T = ˜d√Tρκ μ , multiplied by the delay factor. This yields the simplified bound RT = ˜O( Mρκ μ

˜d√T ), completing the proof. 22