---
title: Linear and Neural Dueling Bandits with Delayed Feedback
title_zh: 带有延迟反馈的线性和神经对决赌博机
authors: "Xiangyi Wang, Pingchen Lu, Jie Mao, Mingze Kong, Zhi Hong, Zhiyong Wang, Zhongxiang Dai"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26554v1"
tags: ["query:rec"]
score: 9.0
evidence: 带延迟反馈的推荐系统决斗赌博机算法
tldr: 上下文对决赌博机在推荐系统与大模型对齐中至关重要，但实际延迟反馈使传统算法有偏。本文形式化带随机延迟的上下文对决赌博机问题，提出线性LDB-DF和神经NDB-DF算法，创新性地在损失函数中集成逆概率加权以无偏估计。理论证明线性场景O(d√T)遗憾界，神经场景次线性遗憾，并通过实验验证有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26554v1/fig-001.webp\", \"caption\": \"Figure 1. Linear Results. Comparison of LDB-DF (Ours) vs. LDB-Ignore. (a) Synthetic data. (b) Aggregated performance on Prompt Optimization.\", \"page\": 7, \"index\": 1, \"width\": 442, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2605.26554v1/fig-002.webp\", \"caption\": \"Figure 2. Neural Results. Comparison of NDB-DF (Ours) vs. NDB-Ignore. (a) Quadratic synthetic. (b) Cubic synthetic. (c) Aggregated Neural Prompt Optimization.\", \"page\": 8, \"index\": 2, \"width\": 408, \"height\": 831}]"
motivation: 现实场景中反馈常存在延迟，且对决赌博机缺乏闭式解，导致直接加权估计有偏，亟需专用算法。
method: 提出将逆概率加权机制直接融入损失函数的无偏估计器，并基于此构建线性和神经网络两种算法。
result: 理论分析获得线性算法O(d√T)遗憾界与神经算法次线性遗憾，实验表明所提算法性能优异。
conclusion: LDB-DF和NDB-DF有效解决延迟反馈下对决赌博机的无偏估计问题，理论和实验均证实其有效性。
---

## 摘要
上下文对决赌博机构成了基于偏好的决策制定的基石，在推荐系统和大语言模型对齐中具有关键应用。然而，标准算法依赖于即时反馈这一理想化假设，而在真实世界场景（如提示优化）中，该条件常常被违背。这一设定引入了一个独特的理论挑战：与线性赌博机不同，对决赌博机估计器缺乏闭式解，导致对标准加权技术的简单改编存在偏差。为解决此问题，我们对具有随机延迟反馈的上下文对决赌博机问题进行形式化建模，并提出了两种新颖算法：带有延迟反馈的线性（LDB-DF）和神经（NDB-DF）对决赌博机。我们方法的核心是一种新颖的估计器，它将逆概率加权（IPW）机制直接集成到损失函数中，确保对延迟或缺失反馈进行无偏校正。我们提供了全面的理论分析，在线性设定下建立了O(d*sqrt(T))的遗憾界，在神经设定下保证了次线性遗憾。在模拟和真实数据集上的大量实验证明了我们提出方法的有效性。

## Abstract
Contextual dueling bandits form a cornerstone of preference-based decision-making, with critical applications in   recommender systems and large language model alignment. However, standard algorithms rely on the idealized assumption   of immediate feedback, a condition frequently violated in real-world scenarios such as prompt optimization. This   setting introduces a unique theoretical challenge: unlike linear bandits, dueling bandit estimators lack closed-form   solutions, rendering naive adaptations of standard weighting techniques biased. To address this, we formalize the   problem of Contextual Dueling Bandits with Stochastic Delayed Feedback and propose two novel algorithms: Linear   (LDB-DF) and Neural (NDB-DF) Dueling Bandits with Delayed Feedback. Central to our approach is a novel estimator that   integrates an Inverse Probability Weighting (IPW) mechanism directly into the loss function, ensuring unbiased   correction for delayed or missing feedback. We provide comprehensive theoretical analysis, establishing an   O(d*sqrt(T)) regret bound for the linear setting and sub-linear guarantees for the neural setting. Extensive   experiments on both simulated and real-world datasets demonstrate the effectiveness of our propose.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：标准上下文对决赌博机（Contextual Dueling Bandits）假设反馈即时可得，然而在推荐系统、大语言模型对齐等实际应用中，反馈（如人类标注的偏好）常存在随机延迟甚至丢失。直接忽略延迟或进行简单插补会引入系统性偏差，因为对决赌博机的参数估计依赖最大似然估计（MLE），缺乏闭式解，常见加权方法难以纠正梯度的有偏性。
- **整体含义**：本文首次对“带随机延迟反馈的上下文对决赌博机”进行形式化建模，并提出了一类基于逆概率加权（IPW）的无偏估计算法，填补了延迟反馈与基于偏好的序列决策之间的理论空白。

### 2. 论文提出的方法论
- **核心思想**：将 **IPW 机制直接融入损失函数**，通过对已观测的反馈样本按到达概率的倒数进行重加权，使损失函数的期望梯度成为真实梯度的无偏估计，从而消除延迟反馈引入的偏差。
- **线性算法 LDB-DF**：
  - **延迟反馈模型**：假设反馈 $y_s$ 在 $D_s$ 步后到达，设定硬观测阈值 $M$，只有 $D_s \le M$ 的反馈最终被用于训练，且到达概率 $\rho = P(D_s \le M)$ 已知。
  - **IPW 损失函数**：对每一条历史数据，定义权重 $\omega_{s,t} = \mathbb{1}\{D_s \le \min(M, t-s)\} / \rho$，构建带有 $L_2$ 正则化的加权交叉熵损失，参数 $\hat{\theta}_t$ 通过最小化该损失得到（公式 3）。
  - **探索策略**：第一支臂贪婪选择最大化 $\hat{\theta}_t^\top \phi(x)$ 的臂；第二支臂基于上置信界（UCB），平衡预测偏好得分与特征空间的不确定性 $\|\phi(x) - \phi(x_{t,1})\|_{V_{t-1}^{-1}}$。
  - **信息矩阵**：$V_{t-1}$ 由所有已选择的臂对特征差构建，与反馈到达无关，用于量化探索不确定性。
- **神经算法 NDB-DF**：
  - 同样采用 IPW 思想，用神经网络 $h(x;\theta)$ 近似奖励函数。
  - 损失函数为带 IPW 权重的交叉熵损失（公式 7），并加入 $L_2$ 正则化 $\|\theta - \theta_0\|_2^2$。
  - 利用 **神经正切核（NTK）** 进行理论分析，定义信息矩阵 $V_{t-1}$ 时使用初始化时刻的梯度差 $g'(s; \theta_0)$。
  - 第二支臂选择最大化 $h(x;\theta_t) + \nu_T \sigma_{t-1}(x, x_{t,1})$，其中 $\sigma_{t-1}$ 是基于 NTK 特征的不确定性度量。
- **理论保证**：
  - 线性场景：遗憾上界为 $\tilde{O}\big(\frac{M}{\rho \kappa_\mu} d\sqrt{T}\big)$，当无延迟时退化为标准界 $\tilde{O}(d\sqrt{T})$。
  - 神经场景：遗憾上界为 $\tilde{O}\big(\frac{M}{\rho \kappa_\mu} \tilde{d}\sqrt{T}\big)$，其中 $\tilde{d}$ 为 NTK 矩阵的有效维度。

### 3. 实验设计
- **数据集/场景**：
  - **合成环境**：线性、二次 $(f(x) = (\theta^\top x)^2)$、三次 $(f(x) = (\theta^\top x)^3)$ 奖励函数，手臂数 $K=20$，特征维度 $d=20$；延迟 $D_t$ 服从几何分布。
  - **真实世界数据**：自动提示优化（Prompt Optimization）任务，采用 29 个指令归纳任务，每个任务生成 500 个候选提示，奖励为验证集准确率。
- **基准对比方法**：
  - **LDB‑Ignore / NDB‑Ignore**：忽略延迟反馈，仅在到达后才加入训练集，不加权。
  - **LDB‑Heuristic / NDB‑Heuristic**：对未到达的反馈，用当前模型预测的偏好概率进行插补（即“幻觉”策略）。
- **评估指标**：累积遗憾。

### 4. 资源与算力
- 论文正文及附录均 **未明确说明** 使用的 GPU 型号、数量、训练时长等算力相关细节。

### 5. 实验数量与充分性
- **实验组数**：
  - 线性设定下：合成数据 1 组 + 提示优化 1 组（29 任务平均），共 2 组对比实验。
  - 神经设定下：二次合成、三次合成各 1 组 + 提示优化 1 组，共 3 组对比实验。
  - 每种场景均与两种朴素基线对比，并对提示优化结果给出了置信区间（阴影），表明统计显著性。
- **充分性与公平性**：
  - 覆盖了线性和非线性奖励结构，兼顾合成与真实应用，实验设置较为合理。
  - 对比的基线能够区分“完全忽略”和“带偏插补”两种常见朴素策略，凸显 IPW 无偏校正的必要性。
  - 但消融实验不足（例如未探讨不同 $M$、$\rho$ 取值的影响），神经实验仅在有限参数下测试，宽度 $m$ 等超参数的选择未详细展示。

### 6. 论文的主要结论与发现
- 提出的 LDB‑DF 和 NDB‑DF 算法在合成与真实提示优化任务上均显著优于忽略延迟或启发式插补的基线。
- 理论分析首次给出了延迟反馈下对决赌博机的遗憾界，明确了观测概率 $\rho$ 和延迟阈值 $M$ 对性能的影响：遗憾与 $M/\rho$ 成线性关系。
- 将 IPW 嵌入损失函数是一种有效的无偏校正方法，且可自然地扩展到神经网络。

### 7. 优点
- **方法创新**：直接对损失函数使用 IPW，避免了传统加权方法的偏差，同时保持了梯度无偏，思路清晰且易于实现。
- **理论扎实**：为线性和神经两种设定均提供了严谨的遗憾界，并将结果退化为无延迟时的已知最优界。
- **实验验证全面**：包含合成和真实世界数据，覆盖线性与复杂非线性，对比基线选择合理。

### 8. 不足与局限
- **强假设**：要求观测概率 $\rho$ 已知，并采用硬截断阈值 $M$；实际中 $\rho$ 可能需要在线估计，且延迟超过 $M$ 的反馈被丢弃可能导致信息损失。
- **理论约束**：神经算法要求网络宽度 $m$ 非常大，难以在有限资源下完全满足。
- **实验局限性**：
  - 未系统考察不同延迟参数 $M$、$\rho$ 的影响，也未对神经网络的宽度、深度等超参数进行消融。
  - 真实世界任务仅限提示优化，缺少更多领域（如推荐系统、在线广告）的验证。
- **未讨论计算成本**：神经训练和 IPW 加权可能增加计算开销，但论文未提供耗时对比。

（完）
