---
title: "Mapping Uncharted Symmetries: Machine Discovery in Combinatorics"
title_zh: 绘制未知对称：组合学中的机器发现
authors: "Eugenio Cainelli, Lorenzo Luccioli, Alessandro Iraci, Michele D'Adderio, Giovanni Paolini"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19063v1"
tags: ["query:sr"]
score: 8.0
evidence: 在组合学中发现符号公式
tldr: "本文受代数组合学中对称性问题的启发，将精确分布约束下的简单函数学习形式化为SLURP问题，提出了MapSeek-Functional和MapSeek-Symbolic两种方法。应用于q,t-Narayana多项式研究，发现了非交叉划分的新组合解释，并首次获得该类多项式对称性的组合证明，展示了机器学习在数学发现中的潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19063v1/fig-002.webp\", \"caption\": \"Figure 1: Left: atlas of notable statistics (nodes) for k = 3. An edge f → g indicates that, starting from statistic f , one of our methods discovers g as a partner statistic, so that (f, g) provides a combinatorial interpretation of Narayana polynomials Nn,3(q, t). Right: a SLURP instance where the domain X is partitioned into bags, and supervision is provided via per-bag output distributions.\", \"page\": 2, \"index\": 2, \"width\": 821, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2605.19063v1/fig-005.webp\", \"caption\": \"Figure 3: Left: PCA scatter plot of the MapSeek-Functional successful runs (refined N14,3 skippairing dataset, h = 2). Cluster medoids are highlighted. The explained variance of the principal components PC1 and PC2 is shown on the axes. Right: semantic and syntactic entropy of a representative run. Values are scaled with respect to the first iteration.\", \"page\": 8, \"index\": 5, \"width\": 797, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2605.19063v1/fig-003.webp\", \"caption\": \"Figure 4: A 12× 7 polyomino with area 30− 18 = 12 (left) and its bounce path (right); the bounce statistic is equal to 41− 18 = 23.\", \"page\": 14, \"index\": 3, \"width\": 790, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2605.19063v1/fig-001.webp\", \"caption\": \"Figure 5: The embedding used in Proposition A.3. A new bottom row is added, but only its leftmost cell is kept, so there is exactly one more square between the two paths. The red bounce path acquires one extra step in its first vertical run.\", \"page\": 15, \"index\": 1, \"width\": 515, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2605.19063v1/fig-006.webp\", \"caption\": \"Figure 6: An example of the bijection η.\", \"page\": 16, \"index\": 6, \"width\": 784, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.19063v1/fig-007.webp\", \"caption\": \"Figure 7: Computing the skip statistic from a noncrossing partition. The contribution of each counted edge is the number of labels strictly between its endpoints.\", \"page\": 16, \"index\": 7, \"width\": 779, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2605.19063v1/fig-004.webp\", \"caption\": \"Figure 10: Overview of the objects and statistics covered by the Lean code. Thick edges indicate equivalences of types, and are intended with specific m, n, and k values. Curved thin edges indicate transport of statistics across these maps; squiggly edges indicate pairing relationships among statistics. All edges shown correspond to statements whose proofs are formalized in the current Lean development.\", \"page\": 18, \"index\": 4, \"width\": 814, \"height\": 373}]"
motivation: 探索机器学习能否在代数组合学中做出可验证的数学发现，特别是解决长期未决的对称性问题。
method: 将问题形式化为SLURP，设计MapSeek-Functional（交替伪标记与监督训练）和MapSeek-Symbolic（直接生成符号公式）两种方法。
result: "发现了q,t-Narayana多项式基于非交叉划分的新组合解释，并利用发现的统计量证明了一个先前未解决的对称性案例。"
conclusion: 机器学习能够显著推进组合数学研究，通过代码和形式化验证确保了发现的可靠性与可复现性。
---

## 摘要
受代数组合学中长期存在的开放问题启发，我们展示了现代机器学习可以有意义地促进可验证的数学发现。特别地，我们聚焦于在严格分布约束下构造简单数学函数，我们将这一设定形式化为刚性比例下的简单学习（SLURP）。我们通过引入两种方法来解决这一问题：MapSeek-Functional，通过交替伪标记和监督训练步骤对期望函数进行建模；以及MapSeek-Symbolic，旨在直接生成符号公式。我们成功地将这两种方法应用于代数组合学的一个研究问题，发现了源于表示论的q,t-Narayana多项式的一个新的组合解释。据我们所知，这是首个基于非交叉划分的解释。利用发现的一个统计量，我们在一个之前未解决的情形中找到了这些多项式对称性的组合证明。为了简化验证和复现，我们公开了所有代码，包括在Lean 4中对本文所有数学发现的形式化。

## 速览
**TLDR**：受代数组合学未决问题启发，本研究将精确分布约束下的函数学习形式化为SLURP问题，并提出MapSeek-Functional和MapSeek-Symbolic两种方法。应用这些方法，发现了q,t-Narayana多项式的首个非交叉分划组合解释，并给出对称性的组合证明，展示了机器学习辅助数学发现的潜力，所有代码与Lean形式化验证已公开。 \
**Motivation**：探索机器学习如何助力解决代数组合学中长期未决的数学难题。 \
**Method**：提出MapSeek-Functional与MapSeek-Symbolic方法，在严格比例分布约束下学习简单函数。 \
**Result**：发现q,t-Narayana多项式的非交叉分划组合解释，并证得对称性的组合证明。 \
**Conclusion**：证实机器学习可产生可验证的数学发现，并提供完整可重现的代码和形式化证明。

---

## Abstract
Inspired by long-standing open problems in algebraic combinatorics, we show that modern machine learning can meaningfully contribute to verifiable mathematical discoveries. In particular, we focus on the construction of simple mathematical functions under exact distributional constraints, a setting we formalize as Simple Learning Under Rigid Proportions (SLURP). We tackle this problem by introducing two methods: MapSeek-Functional, which models the desired function alternating pseudo-labeling and supervised training steps; and MapSeek-Symbolic, designed to directly produce symbolic formulas. We successfully apply both methods to a research problem in algebraic combinatorics, discovering a new combinatorial interpretation of the $q,t$-Narayana polynomials arising from representation theory. To our knowledge, this is the first such interpretation based on noncrossing partitions. Using one discovered statistic, we find a combinatorial proof of the symmetry of these polynomials in a previously unsolved case. To streamline verification and reproducibility, we release all code, including a formalization of all the mathematical discoveries of this paper in Lean 4.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：
    *   论文聚焦于**代数组合学**中长期存在的开放问题，特别是 $q,t$-组合学领域。该领域研究具有非负整数系数的双变量多项式（如 $q,t$-Narayana 多项式 $N_{n,k}(q,t)$），核心挑战包括：
        1.  **组合解释问题**：为一给定多项式寻找一组组合对象 $X$ 及两个统计量 $\text{stat}_1, \text{stat}_2: X \to \mathbb{N}$，使得 $F(q,t) = \sum_{x\in X} q^{\text{stat}_1(x)} t^{\text{stat}_2(x)}$。
        2.  **交换双射问题**：构造一个双射 $\psi: X \to X$ 来交换两个统计量，从而以组合方式证明对称性 $F(q,t) = F(t,q)$。
    *   **整体含义**：本文旨在展示**现代机器学习不仅能拟合数据，更能作为探索工具，在严格、精确的约束下做出可验证的、有意义的数学发现**，甚至能解决先前未解的问题。

### 2. 论文提出的方法论

*   **核心思想**：将数学中的函数发现问题形式化为一个新的学习框架——**刚性比例下的简单学习（SLURP）**。其目标是在定义域 $X$ 被划分为多个包（bag）$B_i$，且每个包都有给定的输出目标分布 $D_i$ 的条件下，找到一个**简洁**（如符号公式或算法）且**精确满足**这些分布约束的函数 $f: X \to Y$。
*   **关键技术细节与流程**：
    1.  **MapSeek-Functional（函数式方法）**：
        *   该方法训练一个神经网络模型 $f_\theta$，通过交替进行两个步骤来优化。
        *   **步骤一（约束投影）**：对于当前模型输出，在每个包 $B_i$ 内找到最接近目标分布 $D_i$ 的伪标签 $\hat{f}(x)$。这本质上是一个求解 $\min_{\hat{f}} \sum_{x} d(f_\theta(x), \hat{f}(x))$ 的最优传输问题，其中 $d$ 是距离函数。
        *   **步骤二（监督更新）**：将上一步生成的伪标签作为真实标签，对模型 $f_\theta$ 执行若干轮监督学习更新（如使用交叉熵损失进行一个 epoch 的梯度下降）。
        *   该循环持续进行，直到模型输出在所有包上都完全满足约束条件。
    2.  **MapSeek-Symbolic（符号式方法）**：
        *   **模型架构**：使用一个**深度交叉熵方法（CEM）** 框架。它利用一个生成式序列模型（如 Transformer 解码器），将公式视为字符序列来生成候选符号表达式 $\phi$。
        *   **搜索流程**：
            *   **生成阶段**：从序列模型中采样一批候选公式。
            *   **评估阶段**：定义一个距离函数 $\delta(\phi) = \sum_j \sum_y |\text{count}(y, D_j) - \text{count}(y, D^\phi_j)|$，精确量化公式 $\phi$ 的输出分布与约束间的偏差。
            *   **精英选择与重训**：选取 $\delta$ 最小的前 5% 公式作为精英子集，用它们作为训练数据**从头重新训练**序列模型，以偏向更有希望的区域。
            *   此过程重复进行，直到发现一个 $\delta(\phi)=0$ 的符号解。

### 3. 实验设计

*   **核心任务与数据集（Benchmark）**：
    *   **任务**：为 $N_{n,k}(q,t)$ 多项式寻找新的组合解释，即发现与给定统计量 `skip` 配对的新统计量。该任务被形式化为“`skip`-配对基准”。
    *   **数据集**：组合对象为非交叉划分（noncrossing partitions）集合 $NC(n,k)$。具体实验使用了**精炼的 $N_{14,3}$ 数据集**，包含 2366 个划分为 144 个包（bag）。
    *   **约束**：划分按 `skip` 统计量的值以及最大非单例块元素 $m(\pi)$ 分组形成包，每个包的目标分布由增量多项式 $N_{m,k}(q,t) - N_{m-1,k}(q,t)$ 给出。
*   **对比方法**：
    *   **MapSeek-Functional**：在不同设置下运行，包括随机初始化、使用人工启发式候选统计量进行**热启动（warm start）**，以及施加**齐次性约束（homogeneity constraints）**。
    *   **MapSeek-Symbolic**：与一个**遗传算法（GA）基线**进行对比。GA 虽然能找到零距离解，但存在严重的**公式膨胀（formula bloat）** 问题。
    *   还研究了在符号搜索中使用不同的**距离度量**（$L_1$ 距离 vs. 平凡度量）以及不同**符号表示法**（逆波兰表示法 vs. 标准中缀表示法）的影响。

### 4. 资源与算力

*   **文中未明确说明**：论文在介绍实验和模型训练时，**没有提及使用的 GPU 型号、数量或具体的训练时长**。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **MapSeek-Functional**：进行了多组实验，包括初始实验、热启动实验（76 次成功运行）、齐次性约束实验（针对 $h=1,2$ 各 50 次运行），以及关于验证集大小的**消融研究**（对 $h=2$ 设置了 5 种不同比例，每种 50 次运行）。
    *   **MapSeek-Symbolic**：使用了 20 种不同的超参数配置来初始化种群，并对每种配置执行多次深度 CEM 搜索。还进行了对比实验（vs. GA）和表示法/距离函数的消融研究。
*   **充分性评估**：实验设计**较为充分和客观**。研究者通过大量独立运行和聚类分析（如对 Functional 方法的成功解进行层次聚类）来验证发现的鲁棒性和一致性。通过消融实验揭示了关键超参数（如验证集大小、距离度量）的影响，并从 GA 基线中吸取教训，改进了符号方法。方法在 `k=3` 的案例上取得了突破，验证了其有效性。

### 6. 论文的主要结论与发现

*   **新统计量与组合解释**：
    *   MapSeek-Functional 发现了 `mag`（幅度）和 `leap`（跳跃）统计量。
    *   MapSeek-Symbolic 发现了 `leap` 和 `skew`（偏斜）统计量。
    *   这些统计量与 `skip` 一起，为 $N_{n,3}(q,t)$ 提供了基于非交叉划分的**新组合解释**，绘制成了一个“统计量图谱”。所有发现的配对都在数学上被证明对所有 $n$ 成立。
*   **证明对称性**：
    *   基于 ML 发现的 `(skip, leap)` 配对，作者**构造了一个明确的交换双射** $\psi$，从而为 $k=3$ 的情形给出了 **$N_{n,3}(q,t) = N_{n,3}(t,q)$ 的第一个组合证明**，解决了开放问题 4.1 的一个实例。
*   **推广与深化**：
    *   通过扩展 ML 发现的 `skew` 统计量，作者人为定义了一个名为 `mingarc`（最小间隙弧）的统计量。`(skip, mingarc)` 配对为**所有 $k$** 提供了 $N_{n,k}(q,t)$ 的首个基于非交叉划分的组合解释。
*   **形式化验证**：所有主要的数学结果（包括统计量配对和交换双射）已在 Lean 4 证明助手中得到形式化验证。

### 7. 优点

*   **问题驱动与框架创新**：从真实的数学难题出发，提炼出 SLURP 框架，准确捕捉了“严格约束下的简洁函数发现”这一挑战。
*   **方法互补且有效**：提出的两种方法（函数拟合与符号搜索）各有侧重并能互补。函数方法可从连续优化中获益，而符号方法则能直接产出人类可读的公式。两者结合使用，效果显著。
*   **严谨的验证流程**：强调“精确满足约束”而非近似准确，并利用无限域上的数学证明进行验证。最终通过 Lean 4 实现了从实验发现到形式化证明的闭环，极大地增强了结果的可靠性和开创性。
*   **人机协作模式**：通过热启动、施加齐次性约束等方式，展示了如何将人类专家的直觉和知识融入 ML 搜索流程，加速了有意义发现的进程。
*   **基准与可复现性**：公开了“`skip`-配对基准”和所有代码，为未来在组合学函数发现方面的研究提供了标准测试平台。

### 8. 不足与局限

*   **应用范围限制**：当前成功的数学应用仅限于 $k=3$ 的情境。对于更具挑战性的 $k \geq 4$ 的问题，两种方法均遇到困难：MapSeek-Functional 的解难以解释，MapSeek-Symbolic 则无法达到零距离的精确匹配。
*   **算力信息缺失**：论文未提及任何关于算力资源（如 GPU 型号、数量、训练时间）的信息，使得其他研究者难以评估复现实验的成本和门槛。
*   **解的完备性未知**：目前尚不清楚 SLURP 方法能否穷尽或找到所有“数学上有意义”的解，其搜索空间覆盖的完备性有待探究。
*   **符号方法的局限性**：符号搜索依赖于预定义的字母表 $\Sigma$ 和语法 $L$，这意味着只能发现表达力范围内的公式。对于本质上需要复杂算法的统计量（如 `bounce`），当前设定可能无法处理。

（完）
