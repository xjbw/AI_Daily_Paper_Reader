---
title: "PEARL: Unbiased Percentile Estimation via Contrastive Learning for Industrial-Scale Livestream Recommendation"
title_zh: PEARL：通过对比学习实现无偏百分位数估计用于工业级直播推荐
authors: "Blake Gella, Wei Wu, Yuhao Yin, Zexi Huang, Zikai Wang, Emily Liu, Junlin Zhang, Wentao Guo, Qinglei Wang"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21752v1"
tags: ["query:rec"]
score: 9.0
evidence: 基于对比学习的无偏百分位数估计用于直播推荐
tldr: "推荐系统中的行为强度不平衡会导致模型过分放大高活跃用户信号，损害推荐质量与鲁棒性。本文提出PEARL，一种非参数对比百分位近似框架，通过成对比较直接建模相对偏好信号，无需额外分布估计，理论上保证无偏估计。还引入预测引导的bootstrap平滑、广义价值加权和协同训练以增强灵活性与表征学习。离线实验显示有效缓解偏差，在线A/B测试在亿万用户直播平台中带来观看时长+2.10%、消费金额+0.80%、互动率+1.49%、举报率-6.91%的显著提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21752v1/fig-002.webp\", \"caption\": \"Figure 1: The overall architecture of PEARL, which consists of two core components: A user-keyed reservoir pool (𝑁 = 50) that dynamically maintains an unbiased historical distribution. (2) Contrastive Learning: A multi-sample comparator that cross-evaluates model predictions against historical samples to compute the multi-sample contrastive loss (L𝑀𝐵𝐶𝐸 ) without explicit distribution tracking.\", \"page\": 4, \"index\": 2, \"width\": 973, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2605.21752v1/fig-001.webp\", \"caption\": \"Figure 2: The comparison of the UAUC among different user active level cohorts. The non-live cohort corresponds to users who have not watched livestreams in the past month, while the high-active cohort corresponds to users who consumes livestreams content on a daily basis.\", \"page\": 8, \"index\": 1, \"width\": 457, \"height\": 280}]"
motivation: 行为强度不平衡导致交互信号失真，模型过度偏向高活跃用户，损害推荐质量与公平性。
method: 提出PEARL框架，利用对比学习直接近似百分位偏好，结合bootstrap平滑、价值加权和协同训练。
result: "离线缓解偏差并提升多目标排名；在线A/B测试实现观看时长+2.10%、消费金额+0.80%、互动率+1.49%、举报率-6.91%。"
conclusion: PEARL有效消除行为偏差，在工业级直播推荐中取得显著业务增益，证明其有效性与可扩展性。
---

## 摘要
基于用户交互数据训练的推荐系统易受行为强度失衡的影响——这是一种由用户间异质参与模式引起的系统性偏差。这种失衡扭曲了反馈信号，使观察到的交互不再忠实地反映真实偏好，导致模型不成比例地放大高活跃度用户的信号，而对其他用户表示不足，最终在大规模下损害了推荐质量和鲁棒性。为了解决这个问题，我们提出了一种非参数对比百分位数近似框架PEARL，它建模相对偏好信号而非绝对参与强度。在相对优势去偏的基础上，PEARL利用真实的对比交互样本直接近似百分位数关系，而不依赖辅助分布估计模型。我们提供了理论证明，表明这种成对比较可产生百分位数偏好信号的无偏估计。为了更广泛的适用性，我们引入了一种基于预测的自举机制用于百分位数平滑，以处理稀疏和离散的反馈，以及一种广义价值加权公式和一种协同训练策略，以增强建模灵活性和表征学习。大量离线实验表明，PEARL有效地缓解了行为偏差，并在多个排序目标上持续提高了推荐性能。在总用户数达数十亿的生产直播平台中部署后，在线A/B测试证实了显著的现实收益：观看时长增加2.10%，消费金额增加0.80%，互动率提高1.49%，举报率降低6.91%。

## Abstract
Recommender systems trained on user interaction data are susceptible to behavioral intensity imbalance--a systematic distortion arising from heterogeneous engagement patterns across users. This imbalance skews feedback signals such that observed interactions no longer faithfully reflect true preferences, causing models to disproportionately amplify signals from highly active users while underrepresenting others, which ultimately degrades recommendation quality and robustness at scale. To address this issue, we propose a nonparametric contrastive percentile approximation framework, PEARL, that models relative preference signals instead of absolute engagement magnitudes. Building upon relative advantage debiasing, PEARL leverages real contrastive interaction samples to approximate percentile relationships directly, without relying on auxiliary distribution estimation models. We provide theoretical justification demonstrating that such pairwise comparisons yield unbiased estimates of percentile-based preference signals. For broader applicability, we introduce a prediction-based bootstrapping mechanism for percentile smoothing to handle sparse and discrete feedback, alongside a generalized value-weighted formulation and a co-training strategy to enhance both modeling flexibility and representation learning. Extensive offline experiments demonstrate that PEARL effectively mitigates behavioral bias and consistently improves recommendation performance across multiple ranking targets. Deployed in a production livestream platform with a combined user base of billions, online A/B testing confirms substantial real-world gains: +2.10% Watch Duration, +0.80% Consumption Amount, +1.49% Interaction Rate, and -6.91% Report Rate.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：推荐系统在利用用户交互数据训练时，存在**行为强度不平衡（Behavioral Intensity Imbalance）**。高活跃、高消费用户产生大量密集的交互信号，而低活跃用户信号稀疏。这种异质性导致模型严重偏向高活跃用户的反馈模式，观察到的交互无法真实反映全体用户的偏好，损害推荐质量与鲁棒性。
*   **整体含义**：本文旨在将预测目标从**绝对交互强度（如观看时长、消费金额）转换为用户个性化的相对百分位数**，从而消除行为偏差。通过一个名为 PEARL 的非参数对比学习框架，直接利用历史交互样本的成对比较来近似偏好排序，无需显式分布估计，最终在工业级直播推荐中实现显著的在线收益。

### 2. 论文提出的方法论
*   **核心思想**：将连续百分位回归问题转化为二值对比学习任务。预测一个模型分数 $\hat{p}$，使其接近当前交互值 $y$ 在用户历史分布中的相对位置（CDF值）。
*   **关键技术细节**：
    *   **单样本对比学习**：为当前交互值 $y$ 从同一用户的历史样本中随机采样一个参考值 $Y'$，定义对比标签 $I(y > Y')$。通过最小化二值交叉熵损失 $\mathcal{L} = -[I(y>Y') \log \hat{p} + (1-I(y>Y'))\log(1-\hat{p})]$ 来训练模型。**定理1**证明该标签的期望正是真实的百分位数 $\text{CDF}_u(y)$，故该估计无偏。
    *   **多样本对比学习（MBCE）**：为降低单样本带来的高方差（方差为 $p(1-p)$），从历史池中采样 $N=50$ 个独立样本，构造软标签 $\bar{p} = \frac{1}{N}\sum I(y>Y'_i)$。**定理2**证明方差可降低至单样本的 $1/N$，大大稳定训练。
    *   **价值加权对比学习（VWBCE）**：在损失中按每个历史参考样本的实际值 $Y'_i$ 进行加权，使模型最优解 $\hat{p}^*$ 不再是简单计数百分位，而是“累积价值占比”（ Partial Expectation Ratio），兼容绝对价值与相对排序。
    *   **自举式对比学习**：针对稀疏离散目标（如二值举报），将对比对象从原始标签替换为**模型预测的连续值** $\hat{y}$，构建“软化的”对比标签 $\bar{p}_{\text{boot}} = \frac{1}{N}\sum I(\hat{y} > \hat{Y}'_i)$，从而赋予模型细粒度排序监督。
    *   **百分位协同训练**：将PEARL作为去偏排序正则项，与绝对的回归任务联合训练：$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{reg}}(\hat{y}, y) + \lambda \cdot \mathcal{L}_{\text{PEARL}}(\hat{p}, y, \mathcal{P}_u)$，使模型同时学好绝对标度和相对偏好。
    *   **系统实现**：采用**储层采样**在参数服务器中维护每用户 $N=50$ 的历史对比池，保证无偏分布；使用**梯度门控**（当池内样本数 $M_u < 10$ 时跳过更新）防止稀疏用户梯度污染模型。

### 3. 实验设计
*   **数据集与场景**：工业级直播推荐平台排序阶段的生产流数据，规模为 168B 实例，4552 特征，98 个目标。
*   **评估指标**：离线的**用户平均 AUC（UAUC）**，针对回归目标采用回归 AUC；在线指标为**人均观看场次、观看时长、消费金额、互动率和举报率**。
*   **对比方法**：
    *   原始的回归/分类基线。
    *   **CQE**：条件分位估计。
    *   **RAD**：相对优势去偏。
*   **实验配置**：所有方法共享一个现代排序骨干网络，优化器采用 RMSPropV2（lr=0.0015，momentum=0.999999）。

### 4. 资源与算力
*   文中仅说明了在线部署系统拥有数十亿合并用户基数和毫秒级延迟要求，并提及了参数服务器与储层采样的架构实现。**未明确给出训练所用的 GPU 型号、数量、训练时长等具体算力信息**。

### 5. 实验数量与充分性
*   进行了多组离线实验，覆盖**交互、会话观看时长、消费、举报、长期观看时长**共5个核心目标，每个目标对比了原始基线和对应 PEARL 变体。还分析了**不同用户活跃度分群（非直播、低、中、高活跃）的 UAUC 对比**，直观展示了去偏效果。
*   在线部分对多个目标分别进行了**为期一周的 A/B 测试**，并报告了对应的关键业务指标提升。
*   实验设计较为全面，覆盖了连续、离散、稀疏等多种目标类型，验证了方法的不同变体及在用户分群中的效果。对比了同领域的前沿去偏方法（CQE, RAD），客观性较强。

### 6. 论文的主要结论与发现
*   PEARL 能够有效缓解行为强度偏差，在所有离线 UAUC 指标上均优于原始基线和现有去偏方法。
*   特别地，对于非直播用户和低活跃用户（平台多数用户群），PEARL 的排序能力有**非常显著的提升**（如从接近随机猜测提升至 0.501 以上），这是去偏后模型更公平、更个性化的关键证据。
*   在线 A/B 测试证实了巨大业务价值：观看时长 +2.10%、消费金额 +0.80%、互动率 +1.49%、举报率 -6.91%。

### 7. 优点
*   **方法论简洁优雅**：将连续百分位估计转化为简单的对比二分类，无需显式建模或追踪复杂的用户分布，非参数、易实现。
*   **理论扎实**：提供了无偏估计、方差异减少、价值加权下最优解性质的数学证明。
*   **高度工程化**：针对工业场景设计了储层采样、梯度门控、协同训练等配套方案，充分考虑了流式数据、稀疏反馈和推理延迟等现实约束。
*   **效果显著**：不仅在离线指标上全面领先，还获得了多项目正向的在线业务指标，直接证明了对用户体验和平台价值的提升。

### 8. 不足与局限
*   **算力细节缺失**：未提供训练所需算力的具体数据，难以评估其在大规模场景下的计算开销和可复现性。
*   **实验平台单一**：所有实验均基于单一工业直播平台，未在公开学术数据集或其他推荐场景（如短视频、电商）验证其泛化能力。
*   **公平性探讨有限**：虽主要解决了用户活性偏差，但对推荐系统中的其他公平性维度（如物品曝光公平、小众内容发现）未做深入拓展。
*   **百分位回归的固有局限**：该方法依赖于用户有足够的历史交互构建分布，对于完全新用户（冷启动）需依赖其他机制；且对比池大小 $N=50$ 是一个经验超参数，其在不同分布下的敏感性未做分析。

（完）
