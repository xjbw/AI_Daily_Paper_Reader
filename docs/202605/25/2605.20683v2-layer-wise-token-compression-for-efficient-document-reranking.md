---
title: Layer-wise Token Compression for Efficient Document Reranking
title_zh: 面向高效文档重排序的逐层令牌压缩
authors: "Shengyao Zhuang, Zhichao Xu, Ivano Lauriola"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20683v2"
tags: ["query:rec"]
score: 7.0
evidence: 高效文档重排序用于信息检索
tldr: "针对Transformer交叉编码器重排器计算成本高的问题，发现现有初始层token压缩方法对该任务效果不佳，提出层级式token压缩（LTC），在中间层进行自适应token池化。在MS MARCO上，LTC保持排序质量同时将推理吞吐提升25%~116%，并能轻松扩展到列表式LLM重排器，甚至发现压缩可作为一种正则化手段提升长文档排序性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20683v2/fig-001.webp\", \"caption\": \"Figure 1: Effect of LTC to pointwise passage ranking. Left: nDCG@10; Right: QPS. Compression rate 1.0 or Target Layer 28 means no compression applied. Cells with * denote statistically significant difference from no compression (𝑝 < 0.05).\", \"page\": 3, \"index\": 1, \"width\": 506, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2605.20683v2/fig-002.webp\", \"caption\": \"Figure 2: Effect of LTC to pointwise document ranking. Left: nDCG@10; Right: QPS. Compression rate 1.0 or Target Layer 28 means no compression applied. Cells with * denote statistically significant difference from no compression (𝑝 < 0.05).\", \"page\": 3, \"index\": 2, \"width\": 506, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2605.20683v2/fig-003.webp\", \"caption\": \"Figure 4: Effect of LTC to listwise passage ranking. Left: nDCG@10; Right: QPS. Cells with * denote statistically significant difference from no compression (𝑝 < 0.05).\", \"page\": 5, \"index\": 3, \"width\": 506, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2605.20683v2/fig-004.webp\", \"caption\": \"Figure 5: Effect of LTC to listwise document ranking. Left: nDCG@10; Right: QPS. Cells with * denote statistically significant difference from no compression (𝑝 < 0.05).\", \"page\": 5, \"index\": 4, \"width\": 506, \"height\": 382}]"
motivation: 现有token压缩方法在交叉编码器重排器中无效，亟需更有效的压缩方案以提升推理效率。
method: 提出层级式token压缩（LTC），在Transformer中间层进行自适应token池化以逐步减少序列长度。
result: "在MS MARCO上，LTC使段落重排QPS提升25%，文档重排提升116%，且压缩训练模型在长文档任务上超越未压缩模型。"
conclusion: LTC通过中间层压缩实现了高效重排，不仅加速推理，还意外地作为正则化器增强了模型对长文档的泛化能力。
---

## 摘要
基于Transformer的文档交叉编码器重排序模型是现代信息检索系统的核心组件。尽管这些模型取得了成功，但由于在推理时需要处理较长的查询-文档序列，它们面临着高昂的计算成本。一种已知的提高效率的方法是令牌压缩，它通过在初始嵌入层中将令牌分组聚合，减少有效令牌数量，从而加快计算速度。虽然令牌压缩已被证明对双编码器检索引擎有效，但我们通过实验观察到，这种方法对交叉编码器重排序器可能无效。在本文中，我们提出了逐层令牌压缩（LTC），它在Transformer中间层应用自适应令牌池化。通过在MS MARCO段落和文档排序任务上进行广泛的消融研究，我们证明在中间层进行压缩能够在保持排序质量的同时，将段落排序的推理QPS提高最多25%，文档排序的推理QPS提高最多116%。我们还将LTC扩展到列表式LLM重排序器，并表明相同的方法可以轻松应用于长上下文的列表式重排序，其中QPS的提升更为显著。更令人惊讶的是，当将在短段落上训练的重排序器应用于长文档排序任务时，使用压缩训练的模型优于未压缩的模型，这表明压缩可能起到一种有益的规范化作用，鼓励长度不变的表示。

## Abstract
Transformer-based document cross-encoder rerankers are a central component of modern information retrieval systems. Despite their success, these models suffer from high computational costs due to processing long query-document sequences at inference time. A known approach to improve efficiency is token compression, which consists of aggregating groups of tokens together in the initial embedding layer, reducing the effective number of tokens, and making the computation faster. While token compression has proven to be successful for bi-encoder retrievers, we empirically observed that this approach may be ineffective for cross-encoder rerankers. In this paper, we propose Layer-wise Token Compression (LTC), which applies adaptive token pooling at intermediate transformer layers. Through extensive ablation studies on MS MARCO passage and document ranking tasks, we demonstrate that compression at middle layers preserves ranking quality while increasing inference QPS by up to 25% for passage ranking and up to 116% for document ranking. We also extend LTC to listwise LLM rerankers and show that the same approach can be easily applied to long-context listwise reranking, where the QPS improvements are even greater. More surprisingly, when applying rerankers trained on short passages to long-document ranking tasks, models trained with compression outperform their uncompressed counterparts, suggesting that compression may act as a beneficial regularizer that encourages length-invariant representations.