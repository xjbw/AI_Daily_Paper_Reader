---
title: Towards Generalizable and Efficient Large-Scale Generative Recommenders
title_zh: 迈向通用且高效的大规模生成式推荐系统
authors: "Qiuling Xu, Ko-Jen Hsiao, Moumita Bhattacharya"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23312v1"
tags: ["query:rec"]
score: 9.0
evidence: 将生成式推荐系统扩展至10亿参数
tldr: 本文探讨大规模生成式推荐模型在生产中的迁移挑战，从2M到1B骨干参数扩展，发现任务相关缩放行为，并针对训练效率、服务延迟和物品冷启动问题提出多token预测、采样softmax、投影解码头、语义物品塔等方法。生产评估显示1B模型全面优于基线，强调模型规模需与任务空间、解耦成本、延迟对齐和物品泛化协同优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23312v1/fig-003.webp\", \"caption\": \"Figure 1: Scaling-law fits for three anonymized recommendation task categories: Task A captures lower-predictability long-horizon taste, Task B captures moderate-predictability short-horizon engagement, and Task C captures higher-predictability time- or availability-driven behavior. The x-axis counts backbone parameters, excluding embedding and decoding layers; Section 3 details the fitting setup.\", \"page\": 2, \"index\": 3, \"width\": 904, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-001.webp\", \"caption\": \"Figure 2: Estimated training FLOPs per training token for a 6-layer transformer with hidden dimension 1024 and sequence length 512. Values include a fixed backbone term plus vocabularydependent decoding cost; lines compare full decoding with sampled-softmax and projected-head variants as the output vocabulary grows.\", \"page\": 6, \"index\": 1, \"width\": 671, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-004.webp\", \"caption\": \"Figure 3: Latency mismatch between next-token training and delayed cached serving. Title A is the immediate next event used as the training label, but after cached serving delay it may already be consumed; title B can then become the relevant serving-time target.\", \"page\": 7, \"index\": 4, \"width\": 579, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-006.webp\", \"caption\": \"Figure 4: Relative MRR degradation as cached outputs become stale. Delays are simulated by replaying evaluation at increasing serving horizons; task definitions follow Figure 1 and Table 1.\", \"page\": 8, \"index\": 6, \"width\": 680, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-007.webp\", \"caption\": \"Figure 5: MTP comparison across serving scenarios. Bars report relative MRR changes for different future-target window sizes under online serving with p95 latency below one second and cached serving with a 48-hour horizon; task labels use the taxonomy in Table 1.\", \"page\": 9, \"index\": 7, \"width\": 739, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-005.webp\", \"caption\": \"Figure 6: Shared semantic title metadata for encoder events and decoder title representations. Blue denotes learned representations and computation, red denotes input-side event context, and green denotes reusable title metadata from knowledge-graph message passing, LLM2Vec language embeddings, and human annotation features.\", \"page\": 10, \"index\": 5, \"width\": 512, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-002.webp\", \"caption\": \"Figure 7: Weekly production-shadow MRR over 1M users. The 1B-backbone model is compared with a 2M-backbone baseline, excluding embedding and decoding layers from the size count, across the three task categories and cold-start titles outside the model vocabulary.\", \"page\": 11, \"index\": 2, \"width\": 625, \"height\": 399}]"
motivation: 生产环境中预训练收益无法自动转化为下游任务改善，需诊断模型规模的实际适用性。
method: 采用偏移规模法则拟合诊断任务收益，结合多token预测对齐延迟、采样softmax与投影解码头提升训练效率，以及语义物品塔与协同嵌入掩码处理冷启动。
result: 在1M用户的生产影子评估中，1B骨干模型在所有任务上的MRR均超越2M基线。
conclusion: 模型规模应作为生产迁移问题的一个组件，与任务空间、延迟对齐和物品泛化等因素共同考量。
---

## 摘要
生成式推荐模型可以将用户行为建模为事件序列，并为多个推荐任务提供共享的骨干网络。然而，在实际生产中，预训练的收益并不会自动转化为下游应用的改进：任务余量、重复训练成本、服务延迟和物品新鲜度都会影响迁移。我们描述了在工业级标题推荐场景中，将一个生成式推荐模型的骨干参数从200万扩展到10亿（不包括嵌入层和解码层）的经验。在多个下游任务中，我们观察到与任务相关的扩展行为：一些任务在观察到的规模范围内接近经验上限，而其他任务则继续从额外的容量中受益。这促使我们使用偏移缩放定律拟合作为诊断工具，来判断在何处增加模型规模可能更有用或更没用。然后，我们研究了在实际应用模型时出现的生产约束。在数万亿行为令牌上的频繁重训练使得训练和解码效率变得重要；缓存服务可能会使即时下一个令牌目标过时；而新推出的标题可能需要在协同ID嵌入可靠之前，从语义元数据中进行评分。我们通过以下方法解决这些问题：用于服务延迟对齐的多令牌预测；用于高效重复训练的采样softmax和投影解码头；以及用于冷启动适应的带有协同嵌入掩码的语义物品塔。在一项针对100万用户、为期一周的生产影子评估中，10亿骨干模型在所有报告的任务上均比200万骨干基线实现了更高的MRR。总体而言，这些结果支持将模型规模视为生产迁移问题的一个组成部分，与任务余量、解码成本、服务延迟对齐和物品泛化并列。

## Abstract
Generative recommendation models can model user behavior as sequences of events and provide a shared backbone for multiple recommendation tasks. In production, however, pre-training gains do not automatically translate into downstream application improvements: task headroom, repeated-training cost, serving latency, and item freshness all affect transfer. We describe our experience scaling a generative recommender from 2M to 1B backbone parameters, excluding embedding and decoding layers, in a production-scale title recommendation setting. Across multiple downstream tasks, we observe task-dependent scaling behavior: some tasks approach an empirical ceiling within the observed scale range, while others continue to benefit from additional capacity. This motivates using offset scaling-law fits as a diagnostic for where additional model scale may be more or less useful.   We then study production constraints that arise when applying the model in practice. Frequent retraining over trillions of behavior tokens makes training and decoding efficiency important; cached serving can make the immediate next-token target stale; and newly launched titles may need to be scored from semantic metadata before collaborative ID embeddings are reliable. We address these issues with multi-token prediction for serving-latency alignment, sampled softmax and a projected decoding head for efficient repeated training, and semantic item towers with collaborative-embedding masking for cold-start adaptation. In a one-week production-shadow evaluation over 1M users, the 1B-backbone model achieves higher MRR than the 2M-backbone baseline across all reported tasks. Overall, the results support treating model scale as one component of a production transfer problem, alongside task headroom, decoding cost, serving-latency alignment, and item generalization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：生成式推荐模型通过将用户行为建模为事件序列，能为多种推荐任务提供统一骨干，但在工业级生产环境中，单纯增大模型规模并不能保证实际推荐质量的提升。
- **核心问题**：预训练收益难以自动迁移到下游应用，主要受限于三个现实约束：不同推荐任务的性能余量差异巨大；大规模模型频繁重训练与低延迟服务的成本高昂；新物品或稀疏交互物品需要超越协同过滤的泛化能力。
- **整体含义**：本文是将生成式推荐系统规模化落地到生产环境（Netflix 标题推荐）的案例研究，主张将模型规模视为一个需要与任务余量、解码成本、服务延迟对齐和物品泛化共同优化的“生产迁移问题”。

### 2. 论文提出的方法论
论文围绕“如何让大规模生成式推荐有效迁移至生产”提出了四项关键技术。

#### 2.1 用于诊断任务余量的偏移缩放定律
- **核心思想**：不同推荐任务对模型容量增加的收益天花板不同。通过拟合带偏移的幂律函数，可估计各任务的饱和水平 $P_0$，从而判断继续增大模型参数是否仍有明显收益。
- **关键公式**：  
  $$P(N) = P_0 - \left(\frac{N}{N_0}\right)^{-a}, \quad a > 0$$
  其中 $P(N)$ 为参数规模 $N$（仅骨干网络参数，不含嵌入和解码层）时的验证性能（MRR），$P_0$ 为饱和值，$N_0$ 为尺度参数，$a$ 控制提升速率。

#### 2.2 对齐服务延迟的多令牌预测（MTP）
- **问题**：传统下一令牌预测（NTP）假设模型输出即时消费，但在缓存服务的场景下，模型生成的 top 推荐可能已过时，即时标签与真实服务时刻存在延迟失配。
- **核心思想**：将训练目标从预测单个“下一物品”改为预测一个带权重的高价值未来物品集合，并用指数时间衰减强调近未来。
- **损失函数**：  
  $$L = -\sum_{y_i \in Y} w_i \log p_\theta(y_i \mid x), \quad w_i = r_i \exp\left(-\frac{\ln(2)(t_i - t_{\text{context}})}{\beta}\right)$$
  其中 $Y$ 为未来高价值目标集合，$r_i$ 可为观看时长等奖励信号，$\beta$ 设为 1 小时。

#### 2.3 高效重训练：采样 softmax 与投影解码头
- **采样 softmax**：训练时仅对正样本和随机采样的 1% 负样本计算 logits，大幅减少解码计算量。
- **投影解码头**：在输出层前加入共享线性投影，将骨干隐状态维度 $d$ 降至 $d/8$（如 4096 → 512），进一步砍断后续矩阵乘法的规模依赖。两者结合可在输出规模 $10^6$ 时降低约 35.5 倍的训练 FLOPs（每 token）。

#### 2.4 冷启动与弱 ID 物品的语义物品塔
- **问题**：新上线或交互稀少的物品没有可靠的协同 ID 嵌入。
- **方法**：构建语义物品塔，利用知识图谱消息传递、LLM2Vec 语言嵌入和人工标注特征生成物品的语义表示 $z_i$。解码器使用 ID 嵌入的可选掩码机制：
  - 对成熟物品，同时使用协同 ID 嵌入 $e_i^{ID}$ 和 $z_i$。
  - 对冷启动物品，使用学习的 out-of-vocabulary 嵌入 $e_{\text{OOV}}$ 并完全依赖 $z_i$。训练时按在线冷启动率随机掩码输入或输出侧的物品 ID 嵌入，迫使模型学会结合两种证据。

### 3. 实验设计
- **数据与场景**：基于 Netflix 生产级标题推荐，预训练数据集包含 2 万亿行为令牌；评估使用三种匿名任务类别（A：长期口味，低可预测性；B：短期互动，中等可预测性；C：时间/可用性驱动，高可预测性）。
- **评估基准与方法对比**：
  - 缩放定律分析：固定训练数据、评估窗口、物品词表，仅改变骨干参数规模（100K–1B），对比偏移幂律拟合与对数线性拟合的拟合误差。
  - MTP 消融：在不同服务延迟（在线 <1s、48h 缓存）下，对比 MTP 与 NTP，改变未来窗口大小（2–5 个 tokens）。
  - 效率分析：通过解析计算比较全 softmax、采样 softmax、投影头及组合方案的每 token 训练 FLOPs。
  - 最终生产影子评估：对比 2M 骨干基线模型与 1B 骨干模型，在 1M 用户一周影子流量中收集 MRR 表现，细分为三个任务和冷启动物品。
- 主要指标：均采用平均倒数排名（MRR），因其连续且上界为 1，便于拟合天花板。

### 4. 资源与算力
- 论文未明确说明训练所用的 GPU 型号、数量或精确训练时长。仅提及预训练数据集为“2 万亿行为令牌”，并强调“频繁重训练”的工程需求，使得训练与推理效率成为核心优化目标。骨干参数达到 1B（10 亿），但具体的算力投入未披露。

### 5. 实验数量与充分性
- 实验组数较为丰富，覆盖诊断、消融、效率分析和最终系统评估：
  1. 缩放定律拟合：三种任务在多个参数尺度（至少 5 个）下的曲线拟合，并比较两种函数形式的 RMSE。
  2. 解码效率分析：不同输出词表大小下的 FLOPs 估算，对比 4 种配置。
  3. 服务延迟影响：3 种任务在 5 个延迟条件下的 MRR 退化重放实验。
  4. MTP 全面消融：2 种服务场景 × 5 种窗口大小 × 3 种任务。
  5. 最终影子评估：1B 模型与 2M 基线的对比，涵盖 4 个子类（3 任务 + 冷启动）。
- **充分性与公平性**：实验设计遵循控制变量原则（如缩放定律固定数据与评估）；评估基于大规模用户影子流量，减少了过拟合研发集的风险；匿名任务虽然隐藏细节，但保持了行业相关性和客观性。总体实验足够支撑结论。

### 6. 论文的主要结论与发现
- **任务相关缩放**：在观察到范围内，Task A 已接近经验天花板（$P_0=0.311$），而 Task C 仍高度可预测。偏移幂律比对数线性法更适合描述有界指标。
- **延迟对齐至关重要**：缓存服务使短期互動任务 MRR 下跌达 41%，而长期口味任务对延迟较鲁棒。MTP 能在高延迟缓存服务下显著提升所有任务（五 token 窗口提升 22~28%），但在线服务场景下 MTP 可能损害短期任务。
- **效率与性能可兼得**：采样 softmax + 投影头方案使频繁重训练操作可行，且未牺牲骨干建模容量。
- **冷启动大幅改善**：语义塔+嵌入掩码在冷启动物品上取得最大相对提升（+28.1%），表明内容信息对无 ID 物品不可或缺。
- **整体系统提升**：1B 骨干模型在所有任务上 MRR 均超越 2M 基线，验证了在考虑生产约束下的规模化价值。

### 7. 优点
- **紧密结合工业痛点**：将模型规模、训练成本、服务延迟、物品冷启动四个生产关键因素统筹考虑，非孤立研究。
- **任务级诊断工具**：使用偏移缩放定律预测任务天花板，为“该继续扩大模型还是改进目标/表征”提供决策依据。
- **简单有效的效率方案**：1% 负采样 + d/8 投影头，实现简单，与其它先进输出层优化兼容。
- **务实的服务对齐**：MTP 只修改训练目标，推理时仍为单次打分，无需多轮生成，易于落地。
- **冷启动方案优雅**：不破坏成熟物品的 ID 嵌入，通过掩码训练让模型学会混合使用语义与协同信号。

### 8. 不足与局限
- **任务匿名化**：将任务分为 A、B、C 三类，虽保护商业机密但降低了可复现性和具体领域参考价值。
- **场景单一**：仅在 Netflix 视频标题推荐场景评估，向其它领域（如电商、新闻）的泛化性未知。
- **算力细节缺失**：未披露具体 GPU 资源与训练时间，难以评估方法在更小团队或资源受限环境下的可行性。
- **模型规模范围有限**：只测到 1B 骨干参数，并未探索更大尺度（如 10B+）下的利弊，且未明确展示 1B 是否已到达某些任务的绝对饱和。
- **效率分析为解析估算**：FLOPs 比较基于解析公式而非端到端实测吞吐，可能忽略通信、内存等实际瓶颈。
- **MTP 的局限性**：权重、衰减率等超参数对任务敏感，且在线场景下对部分任务有负面影响，需要按任务和延迟分面配置。

（完）
