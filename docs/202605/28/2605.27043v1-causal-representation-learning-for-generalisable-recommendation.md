---
title: Causal Representation Learning for Generalisable Recommendation
title_zh: 面向可泛化推荐的因果表示学习
authors: "Yorgos Felekis, Michael O'Riordan, Oriol Corcoll, Ciarán M. Gilligan-Lee"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27043v1"
tags: ["query:rec"]
score: 9.0
evidence: 通过因果表示学习直接解决推荐系统中的分布偏移问题
tldr: 推荐系统因训练数据受部署策略、用户行为等混杂而产生分布偏移，导致离线指标无法可靠预测线上表现。本文提出基于因果表示学习的解耦准则，仅利用观测数据优化变分下界，使模型关注因果成分以提升分布外泛化。方法无需额外推理成本，在Spotify A/B测试中线上用户参与度显著提升，并在公开与合成数据集上验证了离线持平、偏移下增益明显的效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27043v1/fig-002.webp\", \"caption\": \"Figure 1: Hierarchical Causal Entanglement. Bottom (Micro-Latent): Only the red subset {tic} causally drives Y ; the blue subset {tjnc} contains spurious factors correlated with Z. Middle (MesoLatent): Micro-variables abstract into causal (TC) and non-causal (TnC) clusters. Top (Observed): We observe only the entangled treatment T = m(TC , TnC), which creates the spurious backdoor path T ← TnC ← Z → Y that standard models will exploit.\", \"page\": 3, \"index\": 2, \"width\": 460, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-001.webp\", \"caption\": \"Figure 3: Synthetic SCM, sweep over outcome-noise scale σY ∈ [0, 1] across 2,000 seeds per noise level. Lines are per-seed medians; shaded bands are the inter-quartile range across seeds. Left: intervention sensitivity |Ŷ (T )− Ŷ (T ′)|, where T ′ resamples the non-causal coordinates of T ; lower is better, with 0 indicating that the model has not absorbed any signal from TnC . Right: paired ∆MAE = MAECRL −MAEbaseline per seed, centred on the zero line. The CRL penalty does not degrade predictive performance, and is in fact slightly better at higher noise levels.\", \"page\": 7, \"index\": 1, \"width\": 771, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-003.webp\", \"caption\": \"Figure 4: Progressive removal of non-causal leakage. The objective J(g) is plotted as a function of the penalty weight λ for representations retaining different amounts of the non-causal component TnC . Since TnC contributes confounder information without adding outcome information beyond TC , representations with more non-causal leakage incur a larger penalty. The purely causal representation g(T ) = TC achieves the highest score among the tested representations.\", \"page\": 18, \"index\": 3, \"width\": 664, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.27043v1/fig-004.webp\", \"caption\": \"Figure 5: Lossless regime and critical penalty weight. The objective J(g) is plotted as a function of λ for stochastic compressed representations G = TC + ϵG, where ϵG ∼ N (0, σ2 G). The dashed curve corresponds to the uncompressed causal representation σ2 G = 0. For small λ, preserving the full causal component is optimal; for larger λ, compression can become preferable because the reduction in confounder information outweighs the utility loss.\", \"page\": 19, \"index\": 4, \"width\": 664, \"height\": 446}]"
motivation: 推荐系统的训练与部署分布存在严重偏移，离线指标无法预测线上性能。
method: 提出信息论解耦准则并推导出可优化的变分下界，使模型从观测数据中学习因果表征以提升泛化。
result: 在Spotify线上A/B测试中线上用户参与度大幅提升，且多个数据集上离线一致、分布偏移下增益显著。
conclusion: 加入因果解耦目标能有效增强模型分布外泛化能力，方法简单且无推理开销。
---

## 摘要
基于观测数据训练的预测模型在部署时往往难以泛化到所遇到的数据分布，尤其是当训练数据是系统优化的产物时。推荐系统是一个典型例子：它们基于交互日志进行训练，而这些日志受到已部署策略、过往用户行为和平台过滤的混淆。因此，训练分布与服务时评分的候选分布存在显著差异，这一差距使得离线指标难以可靠预测在线表现。我们采用一种受因果表示学习（CRL）启发的方法来解决分布偏移问题。我们提出一个信息论解耦准则，并证明其最优解仅依赖于输入的因果成分。接着推导出一个可优化的变分下界，使得该准则可仅从有限观测数据中优化。本方法的研究范围比多数CRL文献更窄，我们旨在分布偏移下获得更好的泛化能力，而非完全识别所有潜在因果因子。这一更聚焦的目标使方法更加实用：仅需现有的混淆日志，适用于任何标准监督模型，且不增加推理时间成本。我们的核心评估是在Spotify上针对个性化播放列表生成的生产级排序器，对数百万用户进行的A/B测试。一个容量匹配的CRL变体离线表现与基线相当，但线上带来了听众参与度的显著提升。在公开的KuaiRand推荐数据集和已知因果结构的合成基准上的补充实验也呈现出相同模式：离线与基线持平，分布偏移下获得提升。在所有三个场景中，加入我们的因果解耦目标均带来了显著更好的分布外泛化能力。

## Abstract
Predictive models trained on observational data often fail to generalise to the distributions they encounter when deployed, especially when the training data is a product of the system being optimised. Recommender systems are a canonical example: they are trained on interaction logs confounded by the deployed policy, past user behaviour, and platform filtering. As a result, the training distribution differs substantially from the candidate distribution scored at serving time, a gap that makes offline metrics unreliable predictors of online performance. We address the distribution shift problem with a method motivated by causal representation learning (CRL). We propose an information-theoretic disentanglement criterion and prove that its optimum depends only on the causal components of the input. We then derive a tractable variational lower bound that makes the criterion optimisable from finite observational data alone. The scope of our method is narrower than that of much of the CRL literature, in that we target better generalisation under distribution shift, not full identification of all latent causal factors. This narrower target is what makes the method practical, requiring only the existing confounded logs, applying to any standard supervised model, and adding no inference-time cost. Our headline evaluation is an A/B test with millions of users on Spotify, applied to a production ranker for personalised playlist generation. A capacity-matched CRL variant performed on par offline but delivered substantial online gains in listener engagement. Complementary evidence on the public KuaiRand recommendation dataset and a synthetic benchmark with known causal structure shows the same pattern: offline parity with baseline, gains under distribution shift. Across all three settings, adding our causal disentanglement objective yields meaningfully better out-of-distribution generalisation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：推荐系统等预测模型在训练和部署时面临严重的**分布偏移**（Covariate/Policy Shift）。由于训练日志由已部署的推荐策略产生、并受用户行为和平台过滤混淆，导致离线指标无法可靠预测在线性能（如点击率、播放时长）。  
- **背景动机**：现有的因果表示学习（CRL）方法常依赖干预数据、多环境或强可识别性假设，难以直接应用于观测日志。工业界虽有去偏方法，但多绑定特定偏差机制（如流行度偏差），通用性不足。  
- **整体含义**：作者提出一种**更窄但更实用的目标**：不追求恢复全部潜在因果因子，而是通过**信息论解耦**直接消除表示与混杂物间的依赖，从而提升在新分布下的泛化能力。该方法仅需现有的混淆日志，可附加于任何监督模型，且不增加推理成本。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **结构化因果模型（SCM）设定**：  
  设定变量为 $V = \{T, Y, Z\}$，其中 $Z$ 为已观测混杂物（如用户特征），$T$ 为待排序的候选（如物品），$Y$ 为结果。假设 $T$ 由因果潜在分量 $T_C$ 和非因果分量 $T_{nC}$ 混合得到 $T = m(T_C, T_{nC})$，且满足：  
  - **A1**：$Y \perp T_{nC} \mid T_C, Z$（结果仅通过 $T_C$ 依赖处理变量）  
  - **A2**：$I(T_{nC}; Z | T_C) > 0$（非因果分量携带除 $T_C$ 外的额外混杂信息）

- **解耦准则**：  
  学习一个表示 $g(T)$，最大化以下目标：  
  $$J(g) = I(g(T); Y \mid Z) - \lambda \, I(g(T); Z)$$
  其中第一项为**效用项**（保留预测 $Y$ 所需的因果信息），第二项为**惩罚项**（抑制与 $Z$ 共享的非因果信息）。超参数 $\lambda$ 平衡信息保留与去混杂。

- **理论性质**：  
  - **命题1**：理想因果表示 $g^\star(T) = T_C$ 严格优于简单保留所有成分的表示 $\tilde{g}(T) = (T_C, T_{nC})$，因后者有相同的条件互信息 $I$ 但惩罚项更大。  
  - **命题2**：任何满足可恢复条件的表示 $g$，其因果净化版本 $\bar{g} = \phi(T_C)$ 将移除 $g$ 中与 $Z$ 相关但与 $Y$ 无关的冗余信息，从而提升 $J$。  
  - **命题3**：当惩罚权重 $\lambda$ 小于某个临界值 $\lambda^\star$ 时，最优表示保留完整的 $T_C$，不会损失信息。

- **可优化目标**：  
  由于互信息不可直接计算，推导出变分下界，转化为可处理的损失函数：  
  $$
  L_{\text{final}}(\theta, \phi) = L_{\text{utility}}(\theta) + \lambda \, L_{\text{penalty}}(\theta, \phi)
  $$
  - **效用下界**：采用对比 InfoNCE 损失 $L_{\text{utility}}$，它下界条件互信息 $I(g(T); Y \mid Z)$。在推荐场景中即标准的 *in-batch 交叉熵排序损失*。  
  - **惩罚上界**：利用 NCE‑CLUB 估计器 $I_{\text{NCE-CLUB}}(g(T); Z; d_\phi)$ 上界 $I(g(T); Z)$，并通过可学习的鉴别器 $d_\phi$ 逼近 $\log p(z|g) - \log p(z)$。  
  实际实现中，惩罚项常用梯度反转层（GRL）直接最小化 InfoNCE 下界，并用 NCE‑CLUB 上界作为监控指标。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **合成 SCM**：  
  线性加性高斯因果模型，包含10维处理变量（5维因果 + 5维非因果）、10维混杂变量 $X$。评估**干预敏感度** $|\hat{Y}(T) - \hat{Y}(T')|$（其中 $T'$ 重采样非因果维度）和预测 MAE，验证方法能否忽略非因果信号且不损害预测精度。

- **公开数据集 KuaiRand‑Pure**：  
  ~1.4M 算法曝光交互（有偏训练集）和 ~1.2M 随机曝光交互（无偏测试集），含27,285用户、7,583物品。  
  - **骨干模型**：DeepFM（嵌入、FM、DNN组成）。  
  - **对比**：基线（无惩罚，$\lambda=0$）与 CRL 变体（$\lambda=0.3$）。两者架构、容量完全相同，仅在训练目标中加入 CRL 惩罚。  
  - **评估指标**：有偏验证集 AUC（训练分布），以及随机曝光测试集的 AUC、Recall@5、NDCG@5（衡量分布偏移下的泛化）。

- **线上 A/B 测试**：  
  Spotfiy 个性化播放列表生产环境，**数百万用户**，比较生产基线排序器与容量匹配的 CRL 变体。  
  - **目标**：验证离线鲁棒性增益能否转化为真实线上听众参与度（播放次数、跳过率、播放时长）。  
  - 模型差异仅在于添加了对候选 track 表示与用户/会话上下文之间的互信息惩罚（通过对抗训练实现）。

### 4. 资源与算力
- 论文**未明确提及**训练所用的 GPU 型号、数量及训练时长。仅提到线上 A/B 测试持续两周，训练为生产级规模，但具体算力细节被省略。合成实验和 KuaiRand 实验的训练配置（如批次大小、学习率）在附录中给出，但未提供硬件执行时间。

### 5. 实验数量与充分性
- **实验场景数量**：3 个独立且尺度递增的设置（合成数据、公开离线数据集、大型线上 A/B 测试），覆盖了从理论验证到真实生产影响力。  
- **对比充分性**：在每个场景中均与**容量匹配的基线**进行公平比较，确保增益来自目标函数而非架构优势。合成实验含约 2000 次随机种子，KuaiRand 含 500 次重复，线上 A/B 测试在大规模用户中持续两周，统计重要性通过置信区间报告。  
- **实验覆盖**：仅对比了 $\lambda=0$ 和 $\lambda=0.3$ 两个关键点，未进行广泛超参数灵敏度分析。未与其他去偏方法（如 IPS、DCCL）直接对比，但作者在相关工作中定性区分。整体实验设计严谨且公平，能充分支撑论文“以分布偏移下泛化为目标”的核心主张。

### 6. 论文的主要结论与发现
- 提出的**信息论解耦准则**在理论上能鉴别并保留因果成分，移除混杂依赖，且存在一个“无损区”使完整的 $T_C$ 被保留。  
- 在**合成 SCM**中，CRL 变体完全忽略非因果干扰，干预敏感度趋近于零，且预测误差与基线持平。  
- 在**KuaiRand**上，CRL 变体在有偏训练分布上性能稍有下降（AUC 降低0.0066），但在无偏随机曝光测试集上 AUC、Recall、NDCG 均有统计显著提升（AUC 提升 0.0129，NDCG@5 提升 0.0169），体现了**有效的鲁棒性**（improved effective robustness）。  
- **线上 A/B 测试**证实该增益可转化到真实产品：播放量 +0.75%，跳过率 -0.61%，播放时长 +0.50%，所有变化在 95% 置信区间内显著。  
- 离线指标（训练分布上的 AUC / MAE）在三个实验中均未预测出线上增益，凸显**仅依赖离线评测的局限性**。  
- 方法实现简单，不改变推理管线，可作为现有排序模型的“即插即用”增强。

### 7. 优点：方法或实验设计上的亮点
- **新颖且聚焦的目标**：不追求全面的因果因子识别，而是仅抑制由混淆变量导致的非因果泄露，使目标更具可行性和实用性。  
- **理论支撑坚实**：从互信息分解出发，给出多个命题证明准则的同变性、净化性质和最优保留区间，推导了可优化的变分下界。  
- **方法轻量且通用**：只在训练中增加一个互信息惩罚项，不改变推理架构，无需推理时间开销，可附加于任何有监督模型。  
- **多尺度验证**：从合成控制实验到公开真实数据集，再到大厂百万级用户 A/B 测试，环环相扣地验证了分布偏移下泛化的提升，证据链充分。  
- **揭露离线评测盲区**：多次强调离线分布上的持平而线上显著获益，警示业界仅看重离线 AUC 的风险。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **假设的验证困难**：A1（结果仅与因果分量有关）和 A2（非因果分量携带额外混杂信息）在真实数据上难以实证检验，若假设不成立会削弱理论保证。  
- **单个线上产品的局限性**：A/B 测试仅在 Spotify 一个个性化播放列表场景展开，其他产品形式、商业模式下的效果未经过验证。  
- **超参数 $\lambda$ 的灵敏度未充分展示**：虽然理论分析了无损区，但实验中仅选用一个固定值（0.3），缺乏对不同 $\lambda$ 的消融和调参经验。  
- **未与更丰富的去偏基线对比**：未直接对比基于逆倾向加权（IPS）、双稳健估计或 DCCL 等较成熟的推荐去偏方法，难以定位 CRL 方案在工业界的相对优势。  
- **潜在的复杂度扩展瓶颈**：当混杂结构更复杂（如高维图像、文本）时，当前的双线性或 MLP 互信息鉴别器可能不足以捕捉依赖，需要更强大的估计器。  
- **因果抽象的未连接**：虽然提到可将表示 $g(T)$ 进一步与因果抽象框架连接，但未给出具体方法或实验。

（完）
