---
title: Improving Evaluation of Recombination-based Cartesian Genetic Programming
title_zh: 改进基于重组的笛卡尔遗传编程的评估
authors: "Duy Long Tran, Anja Jankovic, Marie Anastacio, Holger Hoos, Roman Kalkreuth"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28353v1"
tags: ["query:rec"]
score: 9.0
evidence: 基于重组的笛卡尔遗传编程用于符号回归
tldr: 传统笛卡尔遗传编程（CGP）主要依赖变异算子，重组方法因性能欠佳长期被回避。本研究在符号回归基准SRBench上，利用TinyverseGP框架，考察了子图交叉和离散表型重组两种新算子，并通过超参数优化改进其性能。结果显示，超参数优化能有效提升基于重组的CGP性能，为重组方法在CGP中的应用提供了新见解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28353v1/fig-001.webp\", \"caption\": \"Figure 2: Median fitness of the best found configuration against our baselines. Error bar shows the first and third quartiles. The truncated error bars from 192_vineyard and 678_visualizing_env are reaching 1.18 · 1011 and 1.55 · 1019, respectively.\", \"page\": 4, \"index\": 1, \"width\": 473, \"height\": 869}]"
motivation: 传统CGP中重组算子缺乏性能增益，亟需探索提升其效用的策略。
method: 在SRBench符号回归基准上，使用TinyverseGP框架对两种重组算子进行超参数优化。
result: 超参数优化显著改善了基于重组的CGP性能。
conclusion: 超参数优化是释放重组算子潜力、改进CGP性能的有效途径。
---

## 摘要
笛卡尔遗传编程传统上一直使用变异作为其主要且往往是唯一的遗传算子来驱动进化搜索。尽管近年来有所进展，但由于明显缺乏性能增益，基于重组的方法长期被回避。本研究在符号回归基准平台 SRBench 上考察了两种最近提出的基于重组的算子：子图交叉和离散表型重组。利用 TinyverseGP 框架中提供的实现，我们对使用这两种算子的相应表示进行了超参数优化。我们的工作表明，超参数优化可以提升基于重组的笛卡尔遗传编程的性能。

## Abstract
Cartesian Genetic Programming has traditionally been using mutation as its main and often sole genetic operator to drive evolutionary search. Despite advancements in recent years, recombinationbased approaches have long been avoided, due to apparent lack of performance gains. This study examines two recently suggested recombination-based operators, subgraph crossover and discrete phenotypic recombination on SRBench, a benchmarking platform for symbolic regression. Using the implementations provided in the TinyverseGP framework, we perform hyperparameter optimisation of the respective representations with these two operators. Our work demonstrates that hyperparameter optimisation can lead to improvements in performance for recombination-based Cartesian Genetic Programming.

---

## 论文详细总结（自动生成）

### 一、论文的核心问题与整体意义

*   **研究背景**：笛卡尔遗传编程（Cartesian Genetic Programming，CGP）是一种基于图结构的遗传编程范式，传统上几乎完全依赖变异算子作为进化的驱动力。由于过往研究认为重组（如交叉）算子会破坏CGP的图结构，导致性能下降，因此其应用长期被回避。
*   **核心问题**：尽管近期研究提出了适用于CGP的新型重组算子（如子图交叉和离散表型重组），但这些研究的实验评估可能未充分优化超参数，导致无法准确判断重组算子的真实潜力。
*   **整体意义**：本研究旨在探究**超参数优化（Hyperparameter Optimisation, HPO）**能否提升基于重组的CGP的性能，并在此基础上更公平地比较不同重组算子与仅变异CGP之间的性能差异，为CGP领域建立更严谨的基准测试实践。

### 二、论文提出的方法论

*   **核心思想**：采用自动化的算法配置工具，为CGP的重组算子精细调整超参数，释放其性能潜力，从而超越人工设定的基线配置。
*   **技术细节**：
    *   **算法框架**：采用 **TinyverseGP** 基准测试框架，该框架集成了CGP实现、符号回归问题集（SRBench）以及超参数优化器接口。
    *   **超参数优化器**：使用基于贝叶斯优化的 **SMAC3** 工具，对CGP的关键超参数进行寻优。
    *   **优化的超参数空间**：
        | 超参数 | 说明 |
        | :--- | :--- |
        | `population_size` | 种群大小 |
        | `levels_back` | 节点允许回溯的列数 |
        | `mutation_rate` | 变异率 |
        | `cx_rate` | 交叉率 |
        | `tournament_size` | 锦标赛选择规模 |
        | `num_function_nodes` | 功能节点数量 |
    *   **算子算法概要**：
        *   **子图交叉（Subgraph Crossover）**：首先确定双亲的活跃节点位置，选择一个交叉点，交换双亲基因组的子图部分。为保持图结构完整性，算法会执行“邻域连接”和“随机活跃连接”操作。
        *   **离散表型重组（Discrete Phenotypic Recombination）**：遍历双亲的活跃节点，对于每一对活跃节点，以随机概率决定是否交换它们的基因。处理双亲活跃节点数不匹配时，引入“边界扩展”机制随机对齐节点。

### 三、实验设计

*   **数据集**：从符号回归基准平台 **SRBench**（PMLB数据集合集）中选取了 **5个** 小规模数据集（观测数少于500），目标变量均为实值。数据集名称分别为：`192_vineyard`、`210_cloud`、`579_fri_c0_250_5`、`650_fri_c0_500_50`、`678_visualizing_environmental`。
*   **评估基准**：
    *   **性能指标**：测试集上的**均方误差（Mean Squared Error, MSE）**，作为最小化目标。
    *   **配置基线**：两组手动设定的超参数配置，功能节点数分别固定为 **100** 和 **200**。
*   **对比方法**：
    *   **实验配置间的对比**：对每种算法（离散重组、子图交叉、仅变异），比较其**超参数优化后的配置**与**两种基线配置**的性能。
    *   **算法间的对比**：在最佳配置下，横向对比**离散表型重组CGP**、**子图交叉CGP**和**仅变异CGP**的最终性能。

### 四、资源与算力

*   **文中说明**：论文**未提供**任何关于实验算力的说明，如GPU型号、数量、每次运行的时长或总计算时间等。这属于论文在可复现性上的一个信息缺失。

### 五、实验数量与充分性

*   **实验规模**：
    *   在5个数据集上进行了实验。
    *   对于每种算子，超参数优化过程独立重复 **10次**，每次产生一个优化配置。
    *   随后，每个优化配置在测试集上基于 **30个** 不同的随机种子进行评估，以获取性能分布。
*   **实验充分性**：
    *   **充分性**：实验设计较为严谨，通过重复HPO和多次独立评估（30次）来减少随机性影响，并且使用固定的数据划分和随机种子确保了公平性。
    *   **客观性与公平性**：
        *   使用 **5折交叉验证**进行模型调优，随后在**独立的测试集**上报告最终性能，避免了过拟合评估。
        *   采用 **Mann-Whitney U检验**进行性能差异的统计显著性分析，增强了结论的可靠性。

### 六、论文的主要结论与发现

*   **RQ1：超参数优化的影响？**
    *   **超参数优化能有效提升基于重组的CGP性能**。在多数数据集上，优化后的配置匹配或优于至少一种手动设定的基线配置。尤其对于`579_fri_c0_250`（离散重组）和`678_visualizing_environmental`（子图交叉）数据集，性能提升尤为显著。
    *   有趣的是，对于仅变异CGP，超参数优化带来的性能提升并不明显。
*   **RQ2：两种重组算子的性能对比？**
    *   **离散表型重组在整体上性能优于子图交叉**。在5个数据集中，离散重组在4个数据集上取得了更低的MSE中位数。
    *   **子图交叉的性能表现出极大的不稳定性**，在不同运行之间性能差异巨大，其MSE范围在某些数据集上比离散重组高出数十个数量级。
    *   总体而言，配置得当的离散重组CGP比仅变异CGP表现更优或持平。

### 七、优点

*   **严谨的评估方法**：引入自动化超参数优化作为标准环节，解决了传统手动调参在对比新算子时可能带来的不公平性，为未来研究树立了良好范式。
*   **可信的实验设计**：实验包含独立重复的HPO运行、多样本评估、固定随机种子、独立测试集和统计检验，确保了结果的稳健性和可复现性（尽管缺少算力细节）。
*   **开源与可复现性**：所有实现和HPO场景均已开源，有利于社区验证和扩展。

### 八、不足与局限

*   **算力信息缺失**：论文完全没有提及计算资源消耗，这是评估方法实用性和可复现性的重要缺失项。
*   **数据集范围有限**：仅测试了5个小规模符号回归数据集，结论是否能推广到其他问题规模或应用领域（如逻辑综合、策略学习）尚不明确。
*   **算子覆盖不全**：仅评估了两种较新的重组算子，未包含其他文献中已有的重组方法，对“基于重组的CGP”这一大类的覆盖度有限。
*   **高方差问题未解决**：子图交叉算子性能波动巨大，论文虽然揭示了此问题，但未深入分析其原因（如适应度景观分析）或提出解决方案。

（完）
