---
title: "DeGRe: Dense-supervised Generative Reranking for Recommendation"
title_zh: DeGRe：面向推荐的稠密监督式生成式重排序
authors: "Chaotian Song, Jingyao Zhang, Chenghao Chen, Zisen Sang, Dehai Zhao, Guodong Cao, Boxi Wu, Deng Cai, Jia Jia"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25749v1"
tags: ["query:rec"]
score: 9.0
evidence: 用于推荐密集监督生成式重排序
tldr: 在多阶段推荐系统中，生成式重排序面临启发式标签偏差和信用分配问题，导致序列优化困难。DeGRe提出离线-在线解耦设计，通过Lookahead Evaluator挖掘高价值序列并生成密集监督信号，蒸馏到轻量级在线生成器，使生成器内化前瞻规划能力，在线仅需贪婪解码即可逼近全局最优。该方法在公开和工业数据集上表现优异，已在淘宝闪购部署并显著提升推荐效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25749v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of two core challenges in existing generative reranking methods.\", \"page\": 2, \"index\": 1, \"width\": 497, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-003.webp\", \"caption\": \"Figure 2: Overall framework of DeGRe. In the offline phase, the Lookahead Evaluator constructs dense supervision signals through lookahead sequence mining. In the training phase, the Online Generator internalizes lookahead planning capabilities via hybrid distillation. In the online phase, the model achieves efficient inference with a single greedy decoding pass.\", \"page\": 4, \"index\": 3, \"width\": 860, \"height\": 435}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-004.webp\", \"caption\": \"Figure 3: Illustration of the dense supervision construction process. At each step (e.g., 𝑡 = 1, 2), the Lookahead Evaluator determines the target decision (hard label 𝑣𝑖𝑡 , such as 𝑣2, 𝑣4) and constructs soft labels (𝑞𝑡 ) to preserve fine-grained ranking information. These dense signals, which combine deterministic and probabilistic information, are used to guide the training of the generator.\", \"page\": 5, \"index\": 4, \"width\": 832, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-002.webp\", \"caption\": \"Table 2: Performance comparison of generators on the ML-1M, Taobao Ad, and Taobao Flash Shopping datasets. To ensure a fair comparison, all methods are evaluated by an independent external evaluator under a unified configuration.\", \"page\": 7, \"index\": 2, \"width\": 986, \"height\": 217}]"
motivation: 现有生成式重排序方法依赖启发式标签和稀疏列表级奖励，无法有效指导中间步骤优化，存在标签偏差和信用分配问题。
method: 提出DeGRe框架，离线阶段利用Lookahead Evaluator进行束搜索获取逐步价值估计，并转化为密集监督蒸馏到在线生成器，实现在线高效贪婪解码。
result: 实验表明DeGRe在公开基准和工业数据集上超越基线模型，并在淘宝闪购实际部署中显著提升推荐性能。
conclusion: 密集监督的生成式重排序有效弥合了离线探索与在线效率的差距，为推荐系统序列优化提供了高效解决方案。
---

## 摘要
在多阶段推荐系统中，重排序通过捕捉列表内上下文依赖来优化整体效用，但其核心挑战在于在指数级的大排列空间中探索最优序列。近期研究转向了端到端的生成式框架，这些框架通常利用列表级奖励或偏好对齐来指导生成器训练。然而，这些方法仍面临两个关键问题。首先是启发式标签偏差。现有方法往往基于简单规则构建训练目标，例如将点击项提升到顶部，而忽略了列表上下文中的因果依赖关系。其次是信用分配问题。稀疏的列表级后验奖励无法直接指导序列生成的中间步骤，导致优化方向模糊。为了解决这些问题，我们提出 DeGRe（稠密监督的生成式重排序），这是一个通过稠密监督弥合离线探索与在线效率之间差距的生成式重排序框架。DeGRe 的核心在于其离线-在线解耦设计。在离线阶段，我们引入了一个基于累积回归的前瞻评估器，利用束搜索主动挖掘未曝光空间中的高价值前瞻序列。在训练期间，我们将评估器给出的逐步价值估计转化为稠密监督信号，并将其蒸馏到一个轻量级的在线生成器中。这一机制使生成器能够内化前瞻规划能力，在在线推理时仅需一次高效的贪婪解码即可逼近全局最优解。实验表明，DeGRe 在公开基准和工业数据集上优于基线模型。我们已成功将 DeGRe 部署在淘宝闪购上，显著提升了在线推荐效果。

## Abstract
In multi-stage recommender systems, reranking optimizes overall utility by capturing intra-list contextual dependencies, yet its central challenge lies in exploring optimal sequences within an exponentially large permutation space. Recent studies have shifted towards end-to-end generative frameworks, which typically leverage list-wise rewards or preference alignment to guide generator training. However, these methods still face two critical issues. First is the heuristic label bias. Existing methods often construct training targets based on simple rules, such as promoting clicked items to the top, while ignoring causal dependencies within the list context. Second is the credit assignment problem. Sparse list-level posterior rewards fail to directly guide intermediate steps in sequence generation, leading to ambiguous optimization directions.   To address these issues, we propose DeGRe (Dense-supervised Generative Reranking), a generative reranking framework that bridges the gap between offline exploration and online efficiency through dense supervision. The core of DeGRe lies in its offline-online decoupled design. During the offline phase, we introduce a Lookahead Evaluator based on cumulative regression, which leverages beam search to actively mine high-value lookahead sequences in the unexposed space. During training, we transform the step-wise value estimations from the evaluator into dense supervision signals and distill them into a lightweight Online Generator. This mechanism enables the generator to internalize lookahead planning capabilities, requiring only a single efficient greedy decoding pass during online inference to approximate the global optimum. Experiments demonstrate that DeGRe outperforms baseline models on public benchmarks and industrial datasets. We have successfully deployed DeGRe on Taobao Flash Shopping, significantly improving online recommendations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
在多阶段推荐系统中，重排序阶段旨在通过捕捉列表内上下文依赖来优化整体效用（如总点击量、GMV），但面临着**在指数级排列空间中探索最优序列**的难题。现有生成式重排序方法尝试以端到端方式生成最优列表，却普遍存在两个关键缺陷：
- **启发式标签偏差**：训练目标常基于“将点击项推至顶部”等简单规则构造，忽略了列表内部的因果依赖，导致模型拟合有偏分布而无法学习真实全局最优排序。
- **信用分配问题**：使用稀疏的列表级后验奖励（如整体CTR或GMV）难以有效指导序列生成中的每一步局部决策，造成优化方向模糊。

论文提出的 **DeGRe** 旨在通过**密集监督**弥合离线探索与在线效率之间的差距，使生成器具备前瞻规划能力，仅需一次贪婪解码即可逼近全局最优，从而在工业级场景下同时实现高效推理与高质量生成。

### 2. 方法论
DeGRe 采用**离线-在线解耦**设计，整体包含三个核心阶段：

- **离线阶段：基于前瞻评估器的密集监督构建**
    - **Lookahead Evaluator (DeGRe‑E)**：基于Causal Transformer的累积回归模型，接收用户、物品、上下文和排序分特征，使用分组层归一化与域间注意力得到增强表示。其核心是**累积回归头**，通过多个有序二分类问题建模子序列累积值（如点击数）的离散分布：
      $$P(V \ge k \mid l_{1:t}) = \sigma(O_{t,k}), \quad \forall k\in\{1,\dots,t\}$$
      训练损失为有序二元交叉熵，且采用下三角掩码确保 $k\le t$。最终通过求和得到期望累积值 $\hat{V}(l_{1:t})$。
    - **前瞻序列挖掘**：利用训练好的评估器指导束搜索（beam size $B$），在未曝光空间探索高价值序列集合 $\mathcal{T}_{syn} = \{l^{(b)}\}_{b=1}^B$。
    - **密集标签构造**：对 $\mathcal{T}_{syn}$ 中每条序列，在每个时间步 $t$ 设定：
        - **硬标签**：选定物品 $v_{i_t}$ 作为确定性目标。
        - **软标签**：根据评估器对剩余候选物品的价值估计构建概率分布 $q_t(v_i)$，保留精细排序信息。
        - **序列权重**：按序列整体价值 $\hat{V}(l)$ 的softmax归一化赋予权重 $w_l$，控制梯度更新的聚焦程度。

- **训练阶段：混合蒸馏**
    - 在线生成器（DeGRe‑G）采用轻量级编码器‑解码器架构：**双向候选编码器**复用与评估器相同的特征处理，并行编码候选集；**用户引导的因果解码器**以用户表征作为起始输入，利用Causal Transformer建模顺序依赖，并通过**候选约束解码**（类似指针网络）严格限定输出空间为候选集，杜绝重复。
    - 训练目标为混合蒸馏损失：
      $$\mathcal{L}_{Gen} = \sum_{l\in\mathcal{T}_{syn}} w_l \sum_{t=1}^{L} \bigl[ \mathcal{L}_{CE}(v_{i_t}, P_{\theta,t}) + \alpha \mathcal{L}_{KL}(q_t \| P_{\theta,t}) \bigr]$$
      其中左侧为前瞻模仿（硬标签交叉熵），右侧为价值对齐（软标签KL散度）。

- **在线阶段：高效推理**
    - 仅部署轻量级生成器，进行一次贪婪解码生成最终推荐列表，无需运行评估器，显著降低延迟。

### 3. 实验设计
- **数据集**：
    - **ML‑1M**：公开电影推荐数据集，评分≥5视为正样本。
    - **Taobao Ad**：公开淘宝展示广告数据集，按列表混合负样本构造重排序候选集。
    - **Taobao Flash Shopping**：工业级数据集，覆盖亿级日活用户，每条请求包含12个候选物品、6个曝光位，提供点击和转化标签。
- **对比方法**：
    - **生成器基线**：NAR4Rec（非自回归）、GReF（统一框架+RL对齐）、NLGR‑G（邻居列表+采样）、GoalRank（组相对优化）。
    - **评估器基线**：DeepFM（点式）、PIER（列表级上下文）、NLGR‑E（D‑Attention）。
- **评价指标**：
    - 生成器采用蒙特卡洛采样的 **Hit Rate @ Top‑K%**（衡量生成列表在随机比较集中的排名）。
    - 评估器采用 **R‑AUC**（回归AUC）、**PCOC**（校准比）与 **RMSE**。
    - 在线实验使用CTR、ORDER、GMV等业务指标。
- **实现细节**：重排序任务为从12个候选中选6个（排列空间约 $6.6\times 10^5$），按时间序采用留一法划分，所有实验重复5次取平均。

### 4. 资源与算力
论文明确提到**使用PyTorch和NVIDIA A100 (80GB) GPU**进行模型实现与训练，但没有提供具体的GPU数量、总训练时长或能耗数据。训练设置中给出批大小1024、学习率 $5\times 10^{-4}$，评估器和生成器的Transformer层数分别为6和4，嵌入维度16。

### 5. 实验数量与充分性
实验较为全面，包含：
- 三个数据集上的生成器性能对比（含束搜索规模变体 $B=1,8$）。
- 评估器对比（仅Taobao Flash Shopping）。
- 消融实验：去除软标签、序列权重、硬标签，以及仅使用曝光数据，共4个变体，在 $B=2$ 下测试。
- 超参数敏感性分析：束大小 $B$、软标签权重 $\alpha$、序列温度 $\tau_w$。
- 在线A/B测试：在淘宝闪购2%流量下部署8天，对比Base策略与PRM。
- 鲁棒性分析：不同用户群组（存量/新用户）和客户端场景（淘宝App/支付宝）。

实验覆盖生成、评估、消融、超参数、在线性能与鲁棒性等多个维度，**比较公平**（统一外部评估器，留一法分割，多次随机种子），实验设计较为客观。

### 6. 主要结论与发现
- 在离线实验中，DeGRe在所有数据集上显著超越现有最优生成式重排序方法，尤其在 $B=8$ 时，HR@1%比最强基线GoalRank提升高达29.90%~53.19%。
- 评估器在R‑AUC和PCOC上优于列表级基线，验证了累积回归在序列价值估计上的优势。
- 消融研究表明：硬标签（前瞻序列模仿）是性能的基础，序列权重和软标签均贡献正向收益。
- 在线A/B测试中，DeGRe相较于Base策略分别提升CTR +2.85%、ORDER +2.14%、GMV +3.75%，且相比单阶段重排序模型PRM也有显著增益；推理延迟仅增加14.8ms，满足低延迟要求。
- 在不同用户群和跨客户端场景下均表现出强鲁棒性。

### 7. 优点
- **解耦架构巧妙**：将耗时的序列探索完全搬到离线，利用束搜索生成密集步骤级监督，实现在线极简推理，平衡了探索深度与效率。
- **密集监督缓解信用分配问题**：不同于稀疏列表奖励，通过前瞻评估器提供的逐步价值分布（硬标签+软标签+序列权重）能精准指导每一步生成，使生成器内化前瞻规划。
- **可扩展性设计**：束搜索可并行化为单批GPU计算，且通过Top‑K截断灵活控制开销，适合工业候选集场景。
- **实验扎实全面**：涵盖公开/工业数据集、离线多维度指标、在线A/B测试及鲁棒性分析，验证了方法的普适性与工业价值。

### 8. 不足与局限
- **离线束搜索与蒸馏成本**：离线阶段需要训练独立的Lookahead Evaluator并进行束搜索，增加了训练流程的复杂度与计算开销，大规模候选集下的实际成本未详细讨论。
- **依赖评估器的质量**：生成器的性能上限受限于Lookahead Evaluator的准确性；若评估器存在偏差，可能导致监督信号次优。
- **生成序列长度固定**：实验重排序从12选6，未能探索变长列表或动态数量的场景；文中未讨论序列长度 $L$ 对生成质量的影响。
- **仅在点击/转化指标上验证**：未探讨多样性、公平性等其他重排序目标，可能限制在多目标优化场景的应用。
- **对比方法有限**：未包含一些较新的生成式重排序方法（如基于扩散模型的方法），且在线对比仅包含PRM一个额外基线。
- **超参数敏感性**：软标签权重 $\alpha$ 和温度 $\tau_w$ 需仔细调节，可能因数据集而异。

（完）
