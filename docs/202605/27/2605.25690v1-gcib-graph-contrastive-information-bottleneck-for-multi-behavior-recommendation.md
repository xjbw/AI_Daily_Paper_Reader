---
title: "GCIB: Graph Contrastive Information Bottleneck for Multi-Behavior Recommendation"
title_zh: GCIB：面向多行为推荐的图对比信息瓶颈
authors: "Likang Wu, Zihao Chen, Jianxin Zhang, Sangqi Zhu, Yuanyuan Ge, Haipeng Yang, Lei Zhang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25690v1"
tags: ["query:rec"]
score: 9.0
evidence: 用于多行为推荐的图对比信息瓶颈框架
tldr: 针对多行为推荐中辅助行为噪声和目标行为稀疏问题，提出GCIB框架。在结构层利用图信息瓶颈最大化去噪辅助图与目标图的互信息，抑制虚假交互；在特征层通过跨行为图对比学习，以去噪辅助特征丰富目标表示。实验表明该方法优于前沿基线，能学到噪声鲁棒的目标感知表示。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25690v1/fig-005.webp\", \"caption\": \"Figure 3. The overall structure of the presented GCIB model\", \"page\": 4, \"index\": 5, \"width\": 1012, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2605.25690v1/fig-003.webp\", \"caption\": \"Table 2. Performance comparison of GCIB with different models across multiple datasets(HR@10 and NDCG@10)\", \"page\": 7, \"index\": 3, \"width\": 939, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2605.25690v1/fig-002.webp\", \"caption\": \"Table 3. Performance comparison of GCIB with different models across multiple datasets (HR@20 and NDCG@20)\", \"page\": 7, \"index\": 2, \"width\": 939, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.25690v1/fig-004.webp\", \"caption\": \"Figure 4. Analysis of global encoding layers LG and auxiliary/target behavior layers LM across multiple datasets\", \"page\": 8, \"index\": 4, \"width\": 1012, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.25690v1/fig-006.webp\", \"caption\": \"Figure 5. Analysis of loss weights β and λ. (a)-(b) Tmall; (c)-(d) Taobao; (e)-(f) Yelp.\", \"page\": 9, \"index\": 6, \"width\": 488, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.25690v1/fig-001.webp\", \"caption\": \"Figure 6. Visualization of user embedding distributions on Tmall: (a) initial state vs. (b) after GCIB training.\", \"page\": 15, \"index\": 1, \"width\": 1017, \"height\": 415}]"
motivation: 辅助行为图常含噪声且目标行为监督稀疏，影响推荐模型学习精准的用户与物品嵌入。
method: GCIB结合图信息瓶颈进行结构去噪，并采用跨行为图对比学习在特征层面增强目标表示。
result: 在多个基准上，GCIB显著优于现有最先进方法，证明了其噪声鲁棒性和目标感知能力。
conclusion: GCIB有效融合去噪与对比学习，解决了多行为推荐中的噪声和稀疏性挑战，提升了推荐精度。
---

## 摘要
随着推荐系统中多行为学习的迅速兴起，利用辅助用户行为已被证明能有效缓解目标行为数据稀疏性问题。然而，辅助行为图常包含与目标任务不一致的噪声或不相关交互，阻碍了准确用户和物品嵌入的学习。此外，目标行为直接监督信号的稀缺使提取信息丰富的协作信号变得复杂。本文提出GCIB（图对比信息瓶颈），一种在结构和特征两个层面去噪辅助行为信息并丰富目标行为表示的新框架。在结构层面，GCIB采用图信息瓶颈（GIB）目标，最大化去噪后辅助图与目标行为图之间的互信息，同时最小化与原始辅助图的互信息。该形式化保留了任务相关的结构模式并抑制了伪交互。在特征层面，我们提出一种跨行为图对比学习（GCL）方案，其中去噪后的辅助特征和目标行为特征作为用户和物品的互补视图。通过对比这些视图，GCIB利用从辅助行为中蒸馏出的语义丰富了稀疏的目标行为表示。大量实验表明，GCIB优于当前最优基线，凸显了其能为多行为推荐学习抗噪声且目标感知的表示的能力。

## Abstract
With the rapid emergence of multi-behavior learning in recommender systems, leveraging auxiliary user behaviors has proven effective for mitigating target-behavior data sparsity. Yet auxiliary behavior graphs frequently contain noisy or irrelevant interactions that do not align with the target task, impeding the learning of accurate user and item embeddings. Moreover, the scarcity of direct supervision from the target behavior complicates the extraction of informative collaborative signals. In this paper, we introduce GCIB (Graph Contrastive Information Bottleneck), a novel framework that denoises auxiliary behavior information and enriches target behavior representations at both the structural and feature levels. At the structural level, GCIB employs a Graph Information Bottleneck (GIB) objective to maximize mutual information between the denoised auxiliary graph and the target-behavior graph while minimizing mutual information with the original auxiliary graph. This formulation preserves task-relevant structural patterns and suppresses spurious interactions. At the feature level, we propose a cross-behavior Graph Contrastive Learning (GCL) scheme in which denoised auxiliary features and target-behavior features serve as complementary views for both users and items. By contrasting these views, GCIB enriches sparse target-behavior representations with semantics distilled from auxiliary behaviors. Extensive experiments demonstrate that GCIB outperforms state-of-the-art baselines, highlighting its ability to learn noise-resilient and target-aware representations for multi-behavior recommendation.

---

## 论文详细总结（自动生成）

好的，以下是对论文《GCIB: Graph Contrastive Information Bottleneck for Multi-Behavior Recommendation》的结构化总结。

### 1. 论文的核心问题与整体含义

-   **研究背景与动机**：多行为推荐通过引入辅助行为（如点击、收藏）来缓解目标行为（如购买）的数据稀疏问题。现有基于图神经网络的方法虽有效，但主要存在两个局限：一是辅助行为图常包含与目标任务不相关的噪声交互，会误导偏好学习；二是目标行为监督信号本身稀疏，难以保证表示学习的鲁棒性。
-   **核心问题**：如何在利用辅助行为丰富信息的同时，有效去除其中的噪声，并增强稀疏目标行为下的表示学习，从而提升推荐准确性。论文将其总结为双层面挑战：结构层面的噪声边过滤，和特征层面的语义迁移。

### 2. 论文提出的方法论

GCIB框架的核心思想是“结构去噪 + 特征对齐”双层优化。

-   **整体流程**：
    1.  构建包含所有行为的全局异构多行为图，用轻量图卷积网络编码，得到全局初始化嵌入。
    2.  利用目标行为偏好作为引导，对辅助行为图进行结构去噪。
    3.  在去噪后的辅助图与目标图上，通过跨行为对比学习实现特征语义对齐。
    4.  融合目标域和辅助域的表示进行最终预测。

-   **关键技术细节**：
    *   **结构层：基于图信息瓶颈的去噪**
        *   **目标**：优化互信息目标 $\text{Max}: I(R; G'_k) - \beta I(G'_k; G_k)$，其中 $R$ 是目标行为信号，$G_k$ 为原始辅助图，$G'_k$ 为去噪后辅助图。第一项鼓励保留与目标推荐任务相关的结构，论文等价于BPR损失；第二项压缩掉冗余的辅助行为信息。
        *   **去噪过程**：将去噪形式化为边丢弃问题。每条辅助边的保留概率 $w_{ab}$ 由一个基于目标行为嵌入的MLP计算，$\rho_{ab} \sim \text{Bernoulli}(w_{ab})$。使用 Concrete 松弛重参数化使采样过程可微。
        *   **冗余压缩**：由于直接最小化互信息 $I(G'_k; G_k)$ 困难，引入希尔伯特-施密特独立性准则作为替代度量，通过最小化 $\hat{\text{HSIC}}(E'^B_k, E^B_k)$ 来降低去噪前后图节点表示间的依赖。

    *   **特征层：基于图对比学习的语义对齐**
        *   用轻量图卷积分别对目标行为图 $G_{\text{target}}$ 和去噪后的辅助行为图 $G'_k$ 进行编码，得到目标表示 $z^{\text{tgt}}$ 和辅助表示 $z^{\text{aux}}$。
        *   采用 InfoNCE 损失最大化正样本对（同一用户/物品的目标与去噪辅助表示）的互信息，以将辅助语义迁移至稀疏的目标表示。最终对比损失为 $\mathcal{L}_{\text{CL}} = \frac{1}{2}(\mathcal{L}^{\text{CL}}_u + \mathcal{L}^{\text{CL}}_i)$。

    *   **联合优化**：最终损失函数由三部分加权求和构成：
        $$\mathcal{L} = \mathcal{L}_{\text{BPR}} + \beta \cdot \mathcal{L}_{\text{IB}} + \lambda \cdot \mathcal{L}_{\text{CL}} + \gamma \cdot \|\Theta\|^2$$
        其中 $\mathcal{L}_{\text{IB}}$ 是HSIC压缩正则项，$\beta$ 和 $\lambda$ 为超参数。

### 3. 实验设计

-   **数据集/场景**：在四个公开的多行为推荐基准数据集上进行实验：**Tmall**（点击、收藏、加购、购买）、**Taobao**（点击、加购、购买）、**Yelp**（tips、反感、中立、喜欢）和 **ML-10M**（反感、中立、喜欢）。最后一类行为（购买或喜欢）被设定为目标行为，其余为辅助行为。
-   **评测指标**：采用Top-K推荐标准的 **HR@K** 和 **NDCG@K** (K=10, 20)。
-   **对比方法**：与两大类方法进行了比较。
    -   **单行为方法**：MF-BPR，LightGCN。
    -   **多行为方法**：R-GCN，NMTR，MBGCN，S-MBRec，CRGCN，MB-CGCN，PKEF，BCIPM，NSED，MBLFE。

### 4. 资源与算力

-   论文在附录中提供了**模型效率分析**，列出了在Tmall、Taobao、Yelp数据集上的训练时间、推理时间和GPU显存占用，但**未明确说明所使用GPU的具体型号**。
-   相比之下，GCIB的训练和推理时间均显著优于去噪基线 BCIPM，例如在Yelp上，GCIB每轮训练约37.9秒，而BCIPM需912.8秒。GCIB的算力开销与普通GCN推荐模型在同一量级。

### 5. 实验数量与充分性

论文实验设计**较为充分和客观**，具体包括：

-   **整体性能对比**：在4个数据集、2个K值（10，20）、2个指标（HR， NDCG）下与12个基线方法进行了系统比较，共形成至少16组完整对比结果。
-   **消融实验**：对模型的核心组件（全局编码、信息瓶颈去噪、对比学习对齐）设计了4种变体，在3个数据集上进行验证，明确了各模块的贡献。
-   **鲁棒性分析**：设计了向辅助行为注入20%合成噪声的实验，在1个数据集上与2个基线方法对比，验证了模型对噪声的鲁棒性。
-   **超参数分析**：分析了信息瓶颈损失权重 $\beta$、对比损失权重 $\lambda$、编码层数 $L_G$ 和 $L_M$ 共4个关键超参数在3个数据集上的影响，探讨了参数敏感性。
-   **可视化分析**：对训练前后用户嵌入分布进行可视化，直观展示了跨行为对齐效果。

### 6. 论文的主要结论与发现

-   GCIB在所有数据集和指标上**一致地优于所有基线模型**，尤其在数据稀疏的Yelp数据集上提升显著（HR@10提升40.49%），证明了方法的有效性和强适应性。
-   消融实验证实，**结构去噪和特征对齐两个模块缺一不可**，它们的协同作用是性能提升的关键。结构去噪抑制了噪声，语义对齐则富化了稀疏表示。
-   鲁棒性分析显示，GCIB能**有选择性地过滤无关边**，对注入的噪声表现出很强的抵抗力，而对比基线性能下降明显。
-   超参数分析表明，模型对超参数的选择有一定敏感性，需要根据数据集特性（如稀疏度、噪声水平）调整，以获得最佳性能。

### 7. 优点

-   **创新性框架**：提出了一个统一的“结构去噪 + 特征对齐”双层优化框架，系统性地解决多行为推荐中的噪声和稀疏性两大难题。
-   **结构层面的新颖性**：将信息瓶颈直接应用于**图结构（边）** 的修剪，在消息传递前就抑制噪声，优于在表示层面压缩的方法。利用目标行为偏好来引导去噪过程，使过滤更具任务指向性。
-   **实用的近似方法**：采用HSIC作为互信息最小化的可微代理，解决了图结构上互信息估计的难题，使得优化可行。
-   **坚实的实验验证**：实验设计周全，不仅评估整体性能，还深入剖析了模型鲁棒性、各组件贡献和超参数影响，结论可靠。

### 8. 不足与局限

-   **算力细节缺失**：虽然给出了效率对比，但未具体说明所用的GPU型号，不利于精确复现和横向对比算力成本。
-   **先验知识依赖**：模型假设目标行为（如购买）数据是纯净的用于引导去噪。如果目标行为本身也包含噪声，该引导的有效性可能会打折扣，论文未就此展开讨论。
-   **超参数敏感性**：关键超参数 $\beta$ 的最佳取值在不同数据集间差异较大（如在Tmall上为80，Yelp上为1），这表明模型在实际部署时需要一定的调参成本。
-   **应用场景局限**：目前仅针对三种或四种行为类型的数据集进行验证，对于行为类型更加复杂多样的场景，其扩展性和有效性尚待考证。

（完）
