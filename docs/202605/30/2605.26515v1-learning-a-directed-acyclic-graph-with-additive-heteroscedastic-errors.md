---
title: Learning a directed acyclic graph with additive heteroscedastic errors
title_zh: 具有加性异方差误差的有向无环图学习
authors: "Xintao Xia, Li Chen, Yue Hu, Chunlin Li"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26515v1"
tags: ["query:rec"]
score: 8.0
evidence: 使用异方差性和分位数回归进行因果发现
tldr: 本文研究带加性异方差误差的有向无环图因果发现。利用异方差性得到新的可识别性结果，提出迭代过程RESQUE，通过残差构建和复合分位数回归递归识别汇节点，基于条件尺度系数的分位数不变性。理论保证即使在变量数随样本量发散时也能恢复拓扑序和图结构。仿真和应用表明，当因果信息部分编码在方差中时，RESQUE优于现有方法，凸显了利用结构化方差信号进行因果发现的价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26515v1/fig-003.webp\", \"caption\": \"Figure 1: Illustration of the topological-layer decomposition of a DAG.\", \"page\": 17, \"index\": 3, \"width\": 444, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.26515v1/fig-002.webp\", \"caption\": \"Figure 2: Empirical performance of CAM, rank-PC, TL, NOTEARS, and the proposed method in low-dimensional settings. False discovery rates and true positive rates in various graph structures and error distributions are presented.\", \"page\": 23, \"index\": 2, \"width\": 933, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2605.26515v1/fig-004.webp\", \"caption\": \"Figure 3: Empirical performance of CAM, rank-PC, TL, NOTEARS, and RESQUE in high-dimensional settings. False discovery rates and true positive rates in various graph structures and error distributions are presented.\", \"page\": 24, \"index\": 4, \"width\": 933, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2605.26515v1/fig-001.webp\", \"caption\": \"Figure 4: Consensus graph and estimated graph by RESQUE using the Sachs dataset.\", \"page\": 26, \"index\": 1, \"width\": 866, \"height\": 444}]"
motivation: 传统的因果发现主要基于均值建模，忽略了方差中可能蕴含的因果信息，本研究旨在利用异方差性提升因果方向的识别。
method: 提出RESQUE算法，通过迭代进行残差构建和复合分位数回归，利用条件尺度系数跨分位数的不变性递归识别汇节点。
result: RESQUE在拓扑排序和图结构恢复上表现优于现有方法，尤其在方差成分包含因果信息时优势明显。
conclusion: 异方差性为因果发现提供了有效信号，该框架为超越均值建模的多变量因果发现提供了新途径。
---

## 摘要
本文研究在结构方程模型下具有加性异方差误差的有向无环图的因果发现。我们首先为位置-尺度噪声模型建立了新的可识别性结果，表明可以利用异方差性来恢复因果方向。基于这些见解，我们提出了一种新颖的迭代程序，即残差同步分位数估计（RESQUE），其中每次迭代包括残差构建阶段和复合分位数回归阶段，通过条件尺度系数在分位数间的不变性实现汇节点的递归识别。我们随后建立了其在恢复拓扑顺序和图结构方面的理论保证，即使变量数量随样本量发散也是如此。模拟研究和对基准数据集的应用表明，RESQUE与现有方法相比表现良好，特别是当因果信息部分编码在方差分量中时。这些结果凸显了利用结构化方差信号进行因果发现，并为超越均值建模的多变量因果发现提供了一个原则性框架。

## Abstract
This paper studies causal discovery for a directed acyclic graph under a structural equation model with additive heteroscedastic errors. We first establish new identifiability results for location-scale noise models, showing that heteroscedasticity can be leveraged to recover causal directions. Based on these insights, we propose a novel iterative procedure, Residual Simultaneous Quantile Estimation (RESQUE), where each iteration consists of a residual-construction stage and a composite quantile regression stage, enabling recursive identification of sink nodes via the invariance of conditional scale coefficients across quantiles. We then establish its theoretical guarantees for recovering topological order and graph structure, even when the number of variables diverges with the sample size. Simulation studies and application to benchmark datasets show that RESQUE performs favorably compared with existing methods, especially when causal information is partly encoded in the variance component. These results highlight exploiting structured variance signals for causal discovery and provide a principled framework for multivariate causal discovery beyond mean-based modeling.

---

## 论文详细总结（自动生成）

好的，以下是根据论文内容生成的结构化总结。

### 1. 论文的核心问题与整体含义
- **核心问题**：在具有加性异方差误差的结构方程模型（SEM）下，如何从观测数据中学习有向无环图（DAG），即进行多变量因果发现。
- **研究动机与背景**：
  - 传统因果发现方法主要依赖均值建模（如线性非高斯模型、加性噪声模型），但现实数据（如生物学、经济学数据）中普遍存在异方差性，即噪声的方差也依赖于父节点。方差中包含的因果信息常被忽略。
  - 现有处理异方差性因果发现的方法多局限于双变量场景，多变量情况研究不足；且方法多为基于似然（易受参数误设影响）或独立性检验（在高维中效率低），未能充分利用方差中的结构化信号。
  - **整体含义**：本文旨在提供一套从理论可识别性到算法实现再到理论保证的完整框架，系统性地利用异方差性来增强多变量因果发现的准确性和鲁棒性。

### 2. 论文提出的方法论
- **核心思想**：利用位置-尺度噪声模型（LSNM）$X_j = f_j(X_{PA(j)}) + g_j(X_{PA(j)})\varepsilon_j$ 的特性。关键在于，对于汇节点（即无孩子的节点），条件尺度系数 $g_j$ 在不同分位数上应保持不变；而非汇节点则不然。通过检验这种不变性，可以递归地识别并移除汇节点，从而确定拓扑顺序，进而恢复完整的因果图。
- **关键技术：残差同步分位数估计 (RESQUE)**：
  - **第一阶段（残差构建）**：对每个变量 $X_j$，以所有其他变量 $X_{-j}$ 为自变量，通过 $L_1$ 正则化的最小二乘回归拟合条件均值函数 $f_j$，并获得残差。对残差取绝对值再取对数，得到用于第二阶段建模的尺度变量 $\hat{Y}_j$。公式表示为 $L_{n,j}(\beta) = \frac{1}{n}\sum_{i=1}^n \{X_{i,j} - \Phi(X_{i,-j})^\top\beta\}^2/2 + \lambda_1\|\beta\|_1$。
  - **第二阶段（复合分位数回归）**：对构建的尺度变量 $\hat{Y}_j$，以其他变量 $X_{-j}$ 为自变量，进行多分位数回归。为了识别汇节点，采用惩罚复合分位数回归，其目标函数包含三部分：多个分位点的检查损失之和、对各分位数系数的 $L_1$ 稀疏化惩罚，以及**关键项——对不同分位数间系数差异的融合惩罚**（如 $P_{\lambda_3}(\|\gamma_k - \gamma_{k+1}\|_2)$）。该惩罚项会将非汇节点的系数差异保留，而将汇节点的系数恰好收缩为完全相等。
- **算法流程 (Algorithm 1)**：
  1. 初始化节点集 $S$，顺序 $\pi$ 为空。
  2. 重复以下步骤，直到 $S$ 为空：
     a. 对 $S$ 中所有节点，执行上述两阶段回归。
     b. 识别汇节点：查找第二阶段回归中，所有分位数系数 $\gamma_k$ 被压缩为相等的节点 $j$。
     c. 将节点 $j$ 作为当前最深层节点加入拓扑顺序 $\pi$，并从 $S$ 中移除。
     d. 使用惩罚回归或硬阈值法来估计汇节点 $j$ 的父节点集，更新图结构。
- **图结构恢复**：在确定汇节点后，方法提出使用折叠凹惩罚（如SCAD, MCP）或硬阈值法对两个阶段的系数进行变量选择，从而确定父节点集。

### 3. 实验设计
- **数据集与场景**：
  - **合成数据**：生成满足模型 $X_j = X_{PA(j)}^\top\beta_j + \exp(X_{PA(j)}^\top\gamma_j)\varepsilon_j$ 的数据。考虑了三种DAG结构：随机图（Random）、完全树图（TR）和尺度自由图（SF）。噪声 $\varepsilon_j$ 的分布有均匀、Beta、高斯三种。设定为“弱异质性”场景，即因果信息部分蕴含在均值，部分蕴含在方差中。同时测试了低维（$p \in \{5, 10, 15, 20\}$）和高维（$p \in \{50, 75, 100\}$）设定，样本量固定为 $n=200$。
  - **真实基准数据**：
    - **Sachs' 流式细胞数据集**：包含 $p=11$ 种蛋白质在单个细胞中的浓度（$n=7466$），其公认的因果网络作为真实因果图的参照。
    - **CauseEffectPairs 数据集**：包含108个已知因果方向的变量对，用于评估双变量因果推断的准确性。
- **对比方法 (Benchmarks)**：
  - **CAM (Causal Additive Models)**：非线性加性噪声模型，适用于高维稀疏回归。
  - **Rank-PC**：基于秩相关的PC算法，适用于非高斯数据。
  - **TL (RESIT的改进)**：使用稀疏回归确定拓扑顺序的线性非高斯方法。
  - **NOTEARS**：将DAG学习转化为连续优化问题的代表性方法。
- **评估指标**：错误发现率（FDR）、假阳性率（FPR）、真阳性率（TPR）、结构汉明距离（SHD）以及预测准确率。

### 4. 资源与算力
- 论文未明确提及所使用的具体GPU型号、数量及训练总时长。由于其核心算法涉及的是求解线性规划和凸/非凸优化（如Lasso、分位数回归），可能在CPU上运行即可。论文对此没有进行详细说明。

### 5. 实验数量与充分性
- **实验数量**：实验覆盖较为全面。
  - **合成实验**：涵盖了3种图结构 × 3种噪声分布 × (4种低维 + 3种高维) 规模的组合，总计超过60种不同实验配置下的算法性能对比。
  - **真实数据实验**：分别在两个具有代表性的真实因果发现基准数据集上进行了评估。
- **充分性与客观性**：
  - **充分性**：对多个关键维度（图结构、噪声分布、维度）进行了充分的模拟，能够系统性地评估方法在不同场景下的鲁棒性。
  - **客观公平性**：对比了多个主流的、类型各异的方法。所有对比方法均使用作者推荐的默认参数或常用设置，保障了对比的相对公平性。合成数据设定为对基于均值和基于方差的方法都提出了考验，设计客观。

### 6. 论文的主要结论与发现
- **理论结论**：证明了在位置-尺度噪声模型下，即使变量是连续且有界的（放宽了以往要求无限可支撑的假设），DAG也是可识别的。这为利用异方差性进行因果发现奠定了更坚实的理论基础。
- **方法结论**：所提出的RESQUE算法能够有效利用方差中的因果信号，在不同图结构和噪声分布下，特别是当因果信息部分编码在方差中时，其图结构恢复性能（低FDR，高TPR）显著优于仅关注均值的现有方法（如CAM, TL, NOTEARS）。
- **实证发现**：
  - 在Sachs数据集中，RESQUE识别出的正确边数量最多，SHD最小，且成功识别出了一些仅通过方差成分才能发现的因果关系。
  - 在CauseEffectPairs数据集中，RESQUE的预测准确率（0.711）远超其他对比方法。

### 7. 优点
- **理论创新**：将多变量异方差SEM的可识别性条件从无限支撑扩展到有界支撑，理论贡献显著。
- **方法论亮点**：
  - 创造性地将因果发现问题转化为一个特征选择问题，利用**分位数系数的跨分位数不变性**来识别汇节点，从检验独立性转变为检验参数相等，避免了复杂的非参数检验。
  - 采用复合分位数回归与融合惩罚，能够充分利用方差的分布信息，并同步进行“识别汇节点”和“估计系数”两项任务。
  - 算法框架允许通过特征映射 $\Phi$ 和 $\Psi$ 灵活建模非线性关系，具有不错的扩展性。
- **性能优势**：在模拟和真实数据中，尤其是在方差蕴含因果信息的场景下，表现出一致且显著的性能优势，证明了其有效性和实用性。

### 8. 不足与局限
- **计算复杂度**：方法需要迭代地运行两阶段回归，并在每次迭代中进行参数调优，对于非常高的维度（$p$ 很大），虽然理论上有保证，但计算负担可能较重。论文未给出复杂度分析或实际运行时间比较。
- **模型假设依赖**：
  - 假设模型为加性位置-尺度形式，若真实模型违背此形式（如影响方差和均值的变量不同），性能可能下降。
  - 假设因果充分性（无隐混淆变量），这在许多实际应用中是一个强假设。
  - 方法依赖于特征映射 $\Phi, \Psi$ 的选择，这与模型的稀疏性和近似误差密切相关，实践中需要仔细选择。
- **实验覆盖局限**：合成实验中设置的是“弱异质性”，没有系统地探索不同强度的异方差信号对方法性能的影响。对非线性函数的实验覆盖（依赖于 $\Phi, \Psi$ 的基展开）描述不够充分，可能低估了模型误设的风险。
- **理论到实践的距离**：理论分析依赖Cramér条件等假设，且算法中的融合惩罚在实际中很难恰好将系数压缩为绝对相等，论文采用启发式规则（选差异最小的节点）来决定汇节点，虽有效，但与理论间的差距未深入探讨。

（完）
