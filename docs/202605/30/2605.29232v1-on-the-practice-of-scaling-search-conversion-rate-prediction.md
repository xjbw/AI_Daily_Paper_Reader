---
title: On the Practice of Scaling Search Conversion Rate Prediction
title_zh: 论搜索转化率预测模型的规模扩展实践
authors: "James Pak, Jyun-Yu Jiang, Fan Zhang, Sen Wang, Taekmin Kim, Henry Tsai, Vijay Rajaram, Juexin Lin, Mohitdeep Singh, Alessandro Magnani, Johnny Chen, Qian Zhao, Rao Fu, Zhirong Liang, Jordan Gilliland, Winter Jiao"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29232v1"
tags: ["query:rec"]
score: 8.0
evidence: 在电商平台上扩展转化率预测模型
tldr: "本文针对高流量搜索转化率（CVR）预测模型的扩展挑战，实证研究了模型骨干计算、嵌入参数规模和训练数据量三个关键因素的缩放性能，发现三者影响独立且可加，并提出了高效的热启动与推理优化策略。最终部署的模型训练数据扩大2.5倍、推理计算增加8倍，线上CVR关键指标提升2.6%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29232v1/fig-004.webp\", \"caption\": \"Figure 1: Overall CVR Model Framework.\", \"page\": 3, \"index\": 4, \"width\": 656, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2605.29232v1/fig-002.webp\", \"caption\": \"Table 1: Relative changes in mAP and training throughput across scaling factors with inference FLOPs against DCNv2 baseline. Bold factors are the default values for each backbone (the default MaskNet is configured without sequential blocks).\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2605.29232v1/fig-007.webp\", \"caption\": \"Figure 3: Impact of data scaling for a MaskNet model on mAP.\", \"page\": 9, \"index\": 7, \"width\": 639, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2605.29232v1/fig-006.webp\", \"caption\": \"Figure 4: FLOPs vs mAP gain for models and scaling factors.\", \"page\": 9, \"index\": 6, \"width\": 645, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.29232v1/fig-003.webp\", \"caption\": \"Figure 5: Performance gains over different durations of training data with (a) different model backbones and with (b) different item embedding dimension sizes for MaskNet.\", \"page\": 10, \"index\": 3, \"width\": 809, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2605.29232v1/fig-001.webp\", \"caption\": \"Figure 6: P99 latency of CPU serving, as well as unoptimized and optimized GPU serving with hybrid CPU-GPU execution.\", \"page\": 11, \"index\": 1, \"width\": 635, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2605.29232v1/fig-005.webp\", \"caption\": \"Table 6: Impact of server-side batching timeout on Peak QPS under different P99 latency constraints. QPS denotes thousands of items processed per second.\", \"page\": 12, \"index\": 5, \"width\": 644, \"height\": 187}]"
motivation: 在高流量环境下扩展搜索CVR模型需权衡模型质量与训练成本、服务延迟。
method: 基于大规模生产数据集，对多种先进架构进行缩放实证分析，结合热启动策略与推理优化（解耦图执行、动态批处理）。
result: "部署模型用2.5倍训练数据、8倍推理计算，在线A/B测试获得+2.6%搜索转化率提升。"
conclusion: 合理扩展骨干、嵌入和数据，并优化训练与推理流程，可在严格成本下显著提升模型性能。
---

## 摘要
在高流量环境中扩展搜索转化率（CVR）预测模型面临一项挑战：需要在模型质量与训练成本及服务延迟的严格约束之间取得平衡。本文详细阐述了一种有效扩展现代搜索CVR预测模型的方法。我们首先进行实证研究，以理解搜索CVR模型的扩展性能，分析当扩展模型骨干计算、嵌入参数规模及训练数据量这三个关键因素时，模型质量如何提升。我们使用了一个大规模生产数据集，该数据集包含来自高流量电子商务平台超过一年的客户交互日志，用以评估多种最先进架构及其集成方案的可扩展性。主要发现包括：（1）选择合适的骨干网络和扩展因素至关重要；（2）扩展骨干、嵌入和训练数据的影响在很大程度上是独立且相加的，这对更高效的扩展探索具有启示意义；（3）精简的热启动策略可加速训练迭代，同时简化新更新；（4）解耦图执行和动态批处理等推理优化策略，即使对于高容量模型也能实现低延迟的GPU服务。与扩展前的生产基线模型相比，我们最终部署了一个训练数据量扩大2.5倍、推理计算量增加8倍的模型，同时对延迟影响极小。在线A/B测试还表明，我们的上线使搜索转化率这一关键指标合计提升了2.6%。

## Abstract
Scaling a Search Conversion Rate (CVR) prediction model, especially in high-traffic environments, presents a challenge: superior model quality needs to be balanced with strict constraints on training cost and serving latency. This paper details an effective approach for scaling modern search CVR prediction models. We begin with an empirical study to understand the scaling performance of search CVR models, analyzing how quality improves as we scale three key factors of model backbone computation, the size of embedding parameters, and the volume of training data.   We use a large-scale production dataset, comprising over a year of customer interaction logs from a high-traffic e-commerce platform, to evaluate the scalability of several state-of-the-art architectures and their ensembles. Our key findings are: (1) selecting the right backbone and scaling factors is crucial; (2) the impact of scaling backbone, embedding, and data is largely independent and additive, which has implications for more efficient scaling exploration; (3) a streamlined warmstart strategy can accelerate training iterations while simplifying new updates; (4) inference optimization strategies such as decoupled graph execution and dynamic batching can enable low-latency GPU serving even for high-capacity models. Compared to a baseline of a pre-scaling production model, we ultimately deployed a model trained on 2.5x larger training data with 8x more inference compute while having minimal latency impact. Online A/B tests also demonstrate that our launches achieved a combined +2.6% gain in a key metric of search conversion rate.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：在线搜索系统的转化率（CVR）预测是电商平台的核心任务，直接影响用户购物体验和平台收入。随着模型复杂度增加，如何在保证高推理效率、低延迟的同时扩大模型容量（参数、计算、数据），成为一个关键挑战。
- **核心问题**：在高流量、严格延迟约束的生产环境中，如何系统性地扩展搜索 CVR 模型（骨干网络、嵌入表、训练数据）以获得可观的性能提升，同时控制训练成本和服务开销？
- **整体含义**：本文通过大规模实测，揭示了 CVR 模型各扩展维度的影响基本独立且可加，并提供了一套从模型优选、热启动训练、到推理优化的全链路工程实践方案，最终在线上取得 +2.6% 的转化率提升。

### 2. 论文提出的方法论

- **模型架构框架**：将 CVR 模型统一抽象为“特征提取 → 特征交互 → 表示变换”三阶段。特征交互采用不同的骨干网络（DCNv2、MaskNet、Transformer、RankMixer），并通过 DHEN（Deep Hierarchical Ensemble Network）进行模型集成。
- **三大扩展策略**：
    - **骨干网络扩展**：增加交叉层宽度、深度、并行/串行 MaskNet 块数、Transformer 的 $d_{\text{model}}$、序列长度、前馈维度、层数和注意力头数等，以提高特征交互的 FLOPs。
    - **嵌入扩展**：扩大品类特征的嵌入维度或词汇量（通过哈希取模控制），以提升表示容量。
    - **数据扩展**：通过增加训练数据的时间跨度或采样率，捕获更丰富的用户行为模式，并采用“未见占位符”处理特征缺失。
- **热启动策略**：先训练一个高容量基础模型（如一年数据、8倍骨干），再在近期数据上微调。通过固定维度投影层解决输入特征变更导致的兼容问题，显著缩短迭代周期。
- **推理优化**：采用 CPU-GPU 混合执行（预处理固定到 CPU，骨干网络固定到 GPU）和客户端/服务端动态批处理（最优批处理超时 10ms），将 P99 延迟降低 2.6 倍，峰值吞吐提升 4.4 倍。

### 3. 实验设计

- **数据集**：使用某大型电商平台超过一年的匿名用户交互日志，训练样本为至少包含一次购买的搜索结果列表，正标签为购买行为。
- **评测指标**：
    - 离线指标：未来 7 天搜索到购买的均值平均精度（mAP），因大多数查询仅一次购买，该指标近似于平均倒数排名（MRR）。所有离线变化报告为 mAP 的相对百分比差异。
    - 在线指标：搜索转化率等核心业务指标。
- **对比方法**：
    - **单骨干模型**：DCNv2、MaskNet（含并行/串行块）、Transformer、RankMixer，每种模型沿多个维度进行缩放对比（表格 1）。
    - **集成模型**：基于 DHEN 的双模型/三模型组合，并与缩放后的单模型在相同算力预算下对比。
    - **扩展组合实验**：分别对比不同训练时长、不同嵌入维度与骨干的组合，验证影响独立性。
- **线上 A/B 测试**：部署最终优化模型（2.5 倍数据、8 倍推理 FLOPs），对比扩展前的生产基线。

### 4. 资源与算力

- 论文未明确说明训练所用 GPU 型号、数量或具体训练时长，仅提及以下与资源相关的信息：
    - 训练吞吐量的变化以百分比报告（如表 1 中不同变体相对基线的吞吐下降比例）。
    - 推理延迟优化部分提到了 P99 延迟和 QPS 指标，以及采用 GPU 混合执行带来的加速比。
    - 热启动策略将训练时间开销从 +264% 降至 +36%，但未给出绝对训练时间。
- 因此，具体的算力规模（GPU 小时数、集群规模等）在文中未披露。

### 5. 实验数量与充分性

- 实验量较大且系统性强，覆盖了骨干网络、嵌入、数据三个维度的独立与组合分析：
    - 表 1 展示了 4 种骨干网络沿 4~5 个独立因子的多参数级缩放，每个因子至少 3~4 个取值，共约 50 种配置的离线 mAP 和吞吐量对比。
    - 图 2 进一步展示了 FLOPs-mAP 增益的关系，包含更多数据点。
    - 表 3 单独测试嵌入维度和词汇量的组合效应。
    - 图 3 与图 5 分别展示了数据缩放幂律关系以及不同骨干/嵌入维度下数据缩放的性能一致性，验证独立性假设。
    - 最终组合实验（表 5）和线上 A/B 测试（表 7）提供了端到端验证。
- 实验设计较为客观公平：以统一的生产基线为参照，在相同的训练-评估框架下进行 mAP 比较，且同时报告了计算开销（FLOPs 和吞吐量）以评估效率边界。唯一不足是计算资源的绝对数字未提供，复现算力需求不透明。

### 6. 论文的主要结论与发现

- **骨干选择至关重要**：在相近 FLOPs 预算下，调优后的 MaskNet 优于 Transformer 和 RankMixer，且 MaskNet 的交叉宽度扩展效率高于深度扩展。Transformer 的序列长度是最有效的扩展因子。
- **扩展效果具有可加性**：骨干网络、嵌入维度和数据规模的提升对 mAP 的影响基本独立，可以分头优化后再组合。
- **数据缩放的幂律关系具有跨架构一致性**：不同骨干或嵌入维度下，mAP 随训练数据量呈对数线性增长，使得在小数据上筛选架构成为可能。
- **集成 vs. 单模型**：DHEN 虽能稳定提升性能，但在等算力条件下，充分优化的单模型（如拓展后的 MaskNet）可以达到更优的帕累托前沿，长期更可持续。
- **工程优化可释放大模型潜能**：通过热启动将训练迭代加速约 4.5 倍；通过图分解与动态批处理使 GPU 推理延迟大幅降低，确保模型容量提升 8 倍而延迟几乎不变。
- **线上收益显著**：最终部署模型带来搜索转化率 +2.6% 的提升。

### 7. 优点

- **系统性实证研究**：对当前主流 CVR 骨干网络进行了多维、多尺度的全面对比，揭示了各扩展因子的边际效益和最佳实践，具备很强的工程指导意义。
- **独立性验证与加速探索**：验证了扩展维度的相加性，并提出利用小数据预选架构的工程策略，极大降低了大规模模型探索的成本。
- **全链路工程优化**：不仅关注模型精度，还覆盖了训练迭代效率（热启动）和在线推理效率（混合执行、动态批处理），使大模型真正落地可行。
- **指标全面透明**：同时报告 mAP 增益、训练吞吐变化、推理 FLOPs 和在线延迟，为资源受限环境下的模型选型提供了完整的权衡视角。

### 8. 不足与局限

- **算力细节缺失**：未提供训练所用 GPU 型号、数量或实际训练时长，使得成本评估不够透明，后来者难以直接估算资源投入。
- **实验环境单一**：所有实验均基于同一电商平台的搜索日志，未在其他业务场景或公开数据集上验证，结论的通用性可能受限。
- **特征交互以外组件未扩展**：研究集中在特征交互模块，对特征提取（如序列建模）和表示变换（MMoE）等环节未做扩展分析，扩展方案不够全面。
- **隐私与数据偏差声明有限**：仅提到未使用个人身份信息，但对数据分布、长尾效应等可能带来的偏见未做深入讨论。
- **线上测试指标较少**：线上 A/B 测试仅报告了搜索转化率的总体提升，未细分其他辅助指标，难以全面评估用户体验变化。

（完）
