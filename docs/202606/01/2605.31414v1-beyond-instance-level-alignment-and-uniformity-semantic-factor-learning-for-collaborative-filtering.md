---
title: "Beyond Instance-Level Alignment and Uniformity: Semantic Factor Learning for Collaborative Filtering"
title_zh: 超越实例级对齐与均匀性：面向协同过滤的语义因子学习
authors: "Yajie Yu, Chenzhong Bin, Zhoubo Xu, Zhixin Zeng, Tongxin Xu, Cihan Xia, Jiafeng Wu"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31414v1"
tags: ["query:rec"]
score: 9.0
evidence: 推荐系统中的协同过滤语义因子学习
tldr: 现有协同过滤方法采用实例级学习，错误将未交互实例标为负样本，且图网络计算成本高、过平滑，限制泛化。本文提出SaFeAU框架，通过语义因子解耦、匹配和对齐，将未交互但语义相似的物品作为潜在正对，增强稀疏监督信号，使矩阵分解捕获高阶关系。在四个稀疏数据集上，该方法推荐精度与效率均优于现有GCN和MF方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31414v1/fig-002.webp\", \"caption\": \"Figure 1: A comparison between (a) GCN-based alignment and (b) semantic positive pairs alignment on the same interaction graph.\", \"page\": 2, \"index\": 2, \"width\": 494, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.31414v1/fig-005.webp\", \"caption\": \"Figure 2: The architecture of SaFeAU. (a) Taking user 𝑢1 as an example, we show how to expand its potential positive items. (b) Illustration of the Semantic Factor Routing (SFR) algorithm, which performs 𝑟 iterations to route all item embeddings to semantic factors based on the semantic assignment weights, resulting in disentangled factor embeddings.\", \"page\": 4, \"index\": 5, \"width\": 1027, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2605.31414v1/fig-001.webp\", \"caption\": \"Table 2: A comparison between SaFeAU and state-of-the-art baselines is provided. The best values are shown in bold, while the second-best values are underlined, results are averaged over five runs. R@k refers to Recall@k, and N@k refers to NDCG@k. Improv.% indicates the relative improvement over the top-performing baseline. An asterisk (*) indicates significant improvements with a t-test 𝑝 < 0.05 compared to the best baseline.\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2605.31414v1/fig-003.webp\", \"caption\": \"Table 4: Compatibility analysis with different CF models on three various density datasets. “Rel Impr.” indicates the relative improvements compared to the corresponding methods. Orange denotes an improvement, with darker shades indicating larger relative changes.\", \"page\": 7, \"index\": 3, \"width\": 512, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2605.31414v1/fig-004.webp\", \"caption\": \"Table 8: Training time comparison of GCN-based and MFbasedmethods on three datasets, including the average epoch time, number of epochs, and total training time. Here, s, m, and h denote seconds, minutes, and hours, respectively.\", \"page\": 13, \"index\": 4, \"width\": 502, \"height\": 437}]"
motivation: 实例级协同过滤错误处理未交互物品为负样本，导致假阴性问题，且图神经网络存在过平滑和高计算开销，限制模型泛化与扩展。
method: 提出SaFeAU框架，包含语义因子路由解耦物品表示、语义因子匹配识别潜在正样本、语义对对齐增强对齐与均匀性。
result: 在四个稀疏真实数据集上，SaFeAU在推荐准确性和计算效率上均超越主流GCN和MF方法。
conclusion: 通过语义因子增强的学习范式，能有效缓解假阴性标签，使矩阵分解捕捉高阶协同信号，实现高效且精准的推荐。
---

## 摘要
协同过滤（CF）因其简洁高效而被广泛应用于推荐系统。然而，现有CF方法遵循实例级学习范式。在实例学习阶段，大量未交互的用户-物品实例中，那些用户潜在感兴趣的物品被错误地当作真实负样本，严重限制了模型的泛化能力和可扩展性。此外，主流图卷积网络（GCN）固有高计算成本和过度平滑问题，这限制了捕获高阶连接的能力，导致在稀疏监督信号下泛化性能较差。为解决上述局限，我们提出了语义因子增强的对齐与均匀性（SaFeAU）框架，该框架通过语义因子增强已交互实例，从而缓解虚假负样本标记问题，并使矩阵分解（MF）能够在不依赖图邻域聚合的情况下捕获高阶CF信号。具体而言，SaFeAU包含三个紧密耦合的组件：首先，语义因子路由（SFR）将物品表示解耦为独立且全局的语义因子；基于这些因子，语义因子匹配（SFM）将具有相同语义因子的未交互物品识别为潜在正样本对，以丰富稀疏监督信号；最后，语义对对齐（SPA）对齐观察到的和潜在的正样本对，同时促进用户和物品表示的均匀性。在四个稀疏真实世界数据集上的大量实验表明，SaFeAU在推荐精度和计算效率上均持续优于基于GCN和基于MF的最先进CF方法，验证了所提出的语义增强学习范式的有效性。

## Abstract
Collaborative filtering (CF) is widely used in recommender systems (RecSys) due to its simplicity and efficiency. However, existing CF methods follow an instance-level learning paradigm. During the instance learning stage, a large number of uninteracted user-item instances, of which items are potential interested by the user, are incorrectly treated as true negative samples resulting in a severe limitation to the generalization and scalability of models. Moreover, mainstream graph convolutional networks (GCNs) inherently suffer from high computational cost and over-smoothing issues, which limit the ability in capturing higher-order connectivity and lead to a poor generalization under sparse supervision signals. To address the above limitations, we propose Semantic Factor enhanced Alignment and Uniformity (SaFeAU), a novel framework that augments interacted instances with semantic factors, thereby mitigating false negative labeling and enabling matrix factorization (MF) to capture high-order CF signals without graph neighborhood aggregation. Specifically, SaFeAU consists of three tightly coupled components. First, Semantic Factor Routing (SFR) disentangles item representations into independent and global semantic factors. Building on these factors, Semantic Factor Matching (SFM) identifies uninteracted items, which share the same semantic factors with interacted ones, as potential positive pairs for enriching sparse supervision signals. Finally, Semantic Pairs Alignment (SPA) aligns both observed and potential positive pairs while promoting uniformity of user and item representations. Extensive experiments on four sparse real-world datasets show that SaFeAU consistently outperforms GCN-based and MF-based state-of-the-art CF methods in both recommendation accuracy and computational efficiency, confirming the effectiveness of the proposed semantic enhanced learning paradigm.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**
    *   协同过滤（CF）是推荐系统的核心技术，但现有方法（包括矩阵分解MF和图卷积网络GCNs）普遍遵循**实例级学习范式**。
    *   **核心问题：虚假负样本（False Negative）**。在该范式下，大量用户未交互但潜在感兴趣的物品被粗暴地标记为“负样本”，导致模型在稀疏数据和大规模推荐场景下**泛化能力受限**。
    *   **GCNs的固有缺陷**：主流基于GCN的方法存在**计算成本高**和**过度平滑（Over-smoothing）** 问题，限制了其捕获高阶连接的能力，进一步加剧了在稀疏监督信号下的泛化难题。

*   **整体含义与目标**
    *   本文旨在从根本上解决上述问题。核心思想是改变传统CF只关注“实例”的方式，转而从**语义层面**理解物品属性，从而找到那些“虽未交互但语义相似”的潜在正样本，为模型提供更丰富、更准确的训练信号。

### 2. 论文提出的方法论

*   **核心思想**
    *   提出一个名为 **SaFeAU (Semantic Factor enhanced Alignment and Uniformity)** 的新框架。其核心是引入**语义因子**，将有交互的实例与全局共享的语义属性（如品牌、材质、外观等）关联起来，从而在语义空间内识别潜在正样本。

*   **关键技术组件与流程**
    *   **1. 语义因子路由**
        *   **功能**：将一批次内的物品表示 $\tilde{f}(i)$ 解耦为 $K$ 个独立且全局共享的语义因子 $\mathbf{F}_j$。
        *   **算法流程**：通过迭代路由过程实现。每次迭代中，根据路由逻辑值 $b_{ij}$ 计算物品 $i$ 到因子 $j$ 的软分配权重 $w_{ij}$（通过softmax归一化），然后聚合表示 $\mathbf{z}_j = \sum_{i} w_{ij}\tilde{f}(i)$，并归一化得到语义因子 $\mathbf{F}_j = \text{normalize}(\mathbf{z}_j)$。随后，根据物品与因子的相似度更新路由逻辑值 $b_{ij} \leftarrow b_{ij} + \mathbf{F}_j^T\tilde{f}(i)$。此过程重复 $r$ 次。
    *   **2. 语义因子匹配**
        *   **功能**：富化稀疏的监督信号，识别未交互物品中的“潜在正样本”。
        *   **算法流程**：对每个已交互物品 $i$，根据语义分配权重选择其**top-k个最重要的语义因子** $K_i$。然后，定义一个语义阈值 $\delta$，若一个未交互物品 $i'$ 与 $i$ 共享的top-k因子数量达到 $\delta$，则将其加入 $i$ 的**语义增强集** $N_i$。这些未交互物品随即被认定为该用户-物品对的“潜在正样本对”。
    *   **3. 语义对对齐**
        *   **功能**：同时对齐观察到的和潜在的正样本对，并保证表示空间的均匀性。
        *   **损失函数**：
            *   保持基础的对齐损失 $L_{align}$（拉近观察到的用户-物品正样本对）和均匀性损失 $L_{uniform}$（使所有用户和物品的表示在空间中均匀分布）。
            *   引入**语义对对齐损失** $L_{semantic\_align}$，其目标是拉近用户表示与由SFM识别出的潜在正样本物品表示之间的距离：
            $$ L_{semantic\_align} = \mathbb{E}_{(u,i) \sim p_{pos}} \left[ \frac{1}{|N_i|} \sum_{i' \in N_i} \|\tilde{f}(u) - \tilde{f}(i')\|^2 \right] $$
        *   **最终优化目标**为一个多任务训练的联合损失：$L_{SaFeAU} = L_{align} + \gamma_1 L_{uniform} + \gamma_2 L_{semantic\_align}$。

### 3. 实验设计

*   **数据集**
    *   使用了四个不同稀疏度的现实世界数据集：**Gowalla**、**Yelp2018**，以及来自Amazon子集的 **Toys-and-Games** 和 **Beauty**。
    *   实验遵循标准的预处理流程，移除重复交互，保留交互次数不少于5次的用户和物品，并按80%/10%/10%的比例随机划分训练集、验证集和测试集。

*   **Benchmark（对比方法）**
    *   对比了多种最先进的CF方法，分为两类：
        *   **基于矩阵分解（MF-Based）的**：BPR-MF, DirectAU, LightGODE, LightCCF。
        *   **基于图卷积网络（GCN-Based）的**：LightGCN, GraphAU, FourierKAN-GCF, SimGCF。

*   **评估协议与指标**
    *   采用**全量排名（full-ranking）** 策略，即在测试时为每位用户对所有未交互物品进行排名。
    *   主要评估指标为 **Recall@K** 和 **NDCG@K**（其中K=10, 20），所有结果均为5次不同随机种子实验的平均值。

### 4. 资源与算力

*   论文没有明确提及实验所使用的具体**GPU型号**与**数量**。
*   然而，论文在**时间复杂度和效率分析（Section 3.5 和 Appendix C.2）** 中提供了详细的对比。结果表明，SaFeAU作为MF-based方法，规避了复杂的图构建和卷积操作，其训练效率显著优于GCN-based方法。例如，在Gowalla数据集上，LightGCN总训练时长为3.53小时，而SaFeAU仅为0.34小时，与LightCCF等高效方法持平，收敛所需的epoch数也更少。

### 5. 实验数量与充分性

*   **实验组数多样，设计充分**：
    *   **主性能对比实验（Q1 / Table 2）**：在4个数据集上，将SaFeAU与8个SOTA基线方法进行了对比。
    *   **兼容性分析（Q2 / Table 4）**：将SaFeAU的插件组件集成到3种不同的主流CF模型（BPR-MF， LightGCN， LightCCF）中，评估其普适性。
    *   **消融研究（Q3, Q4 / Table 5 & 6）**：设计多种变体（如随机匹配、严格匹配等），并与下游推荐指标、直接测试集命中率进行对比，以验证语义匹配的质量。
    *   **对齐效果分析（Q5 / Table 7）**：通过计算正样本对在训练/测试集上的平均欧氏距离及其差距，直观展示不同方法的泛化能力。
    *   **参数敏感性分析（Q6 / Figure 3-5）**：系统探究了5个关键超参数（$\gamma_1$, $\gamma_2$, $K$, $k$, $r$）对模型性能的影响。
*   **客观与公平性**：所有实验均在统一框架（RecBole）下实现，采用标准预处理、评估协议和多次随机种子取平均，并报告了统计显著性（t-test），保证了对比的公平性和结果的可靠性。

### 6. 论文的主要结论与发现

*   SaFeAU在所有四个数据集上的推荐精度（Recall, NDCG）上均**显著超越**了所有基于GCN和MF的最先进基线方法，尤其在极其稀疏的Gowalla数据集上表现最为突出。
*   通过语义因子增强，SaFeAU能够有效识别并利用潜在正样本，解决了传统方法将大量潜在感兴趣的物品错误标记为负样本的问题。
*   作为一个MF-based方法，SaFeAU避免了图卷积，不仅实现了顶尖的性能，还保持了**极高的计算效率**。
*   SFM组件作为一个模型无关的插件，能灵活集成到现有CF模型中，并带来一致的性能提升，证明了其方法的普适性。

### 7. 优点

*   **问题视角新颖**：首次从语义因子角度探索并解决协同过滤中的虚假负样本问题，是对传统实例级学习范式的一次重要突破。
*   **方法论扎实优雅**：巧妙地将胶囊网络的路由机制引入到物品语义因子解耦中，并通过语义匹配和对齐构建了一个端到端的、逻辑清晰的框架。理论分析（定理1-4）为损失函数的关系和方法的有效性提供了支撑。
*   **效率与效果兼备**：在性能超越复杂图网络方法的同时，保持了MF模型的计算简洁性，非常适合大规模工业推荐场景。
*   **实验详尽有力**：实验设计覆盖全面，从主实验、兼容性、消融研究到对表示质量的可视化分析，多维度验证了方法的有效性。

### 8. 不足与局限

*   **数据集规模的局限**：实验仅使用了Gowalla、Yelp和Amazon子集等学术研究中常用的中等规模数据集，缺乏在更大规模（如数十亿交互）的工业级数据集上的验证。
*   **语义因子的可解释性未充分展示**：论文虽然声称学习了“品牌、材质”等因子，但未对学习到的因子做具体的案例分析或可视化展示，以证明其语义的直观可解释性。
*   **超参数敏感性**：论文引入了多个与语义因子匹配相关的新超参数（$K$, $k$, $r$, $\delta$）, 这些参数需要针对不同数据集进行调整，可能增加实际应用中的调参成本。
*   **硬负样本的忽略**：方法论主要聚焦于解决假阴性（False Negative），但未讨论如何处理更棘手的硬负样本（Hard Negative），即那些语义上有些相似但用户确实不感兴趣的物品。

（完）
