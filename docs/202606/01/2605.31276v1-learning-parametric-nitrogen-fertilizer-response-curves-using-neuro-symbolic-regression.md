---
title: Learning Parametric Nitrogen Fertilizer Response Curves Using Neuro Symbolic Regression
title_zh: 使用神经符号回归学习参数化氮肥响应曲线
authors: "Giorgio Morales, John Sheppard"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31276v1"
tags: ["query:rec"]
score: 10.0
evidence: 神经符号回归用于作物建模
tldr: 针对精准农业中氮肥响应建模，提出神经符号回归方法，结合Transformer多集符号骨架预测与遗传算法，无需预定义函数形式即可从数据中发现不同管理区的参数化氮响应曲线。合成与真实冬小麦数据实验显示，该方法在低误差下捕获多样函数行为，优于传统模型，为站点特定农艺关系发现提供新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31276v1/fig-002.webp\", \"caption\": \"Fig. 3: f̂it(x) vs. f̃it(x) throughout the AS process for cosqr. (a) it=1. (b) it=5. (c) it=10. (d) it=15. (e) it=20. (f) it=25. (g) it=30. (h) it=35. (i) it=40. (j) it=45. (k) it=50.\", \"page\": 4, \"index\": 2, \"width\": 475, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-001.webp\", \"caption\": \"TABLE I: Identified expressions during the AS process\", \"page\": 4, \"index\": 1, \"width\": 525, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-003.webp\", \"caption\": \"Fig. 5: Comparison between NN-generated N-response curves r̃ and fitted curves r̂ from (a) MZ 1, (b) MZ 2, (c) MZ 3, and (d) MZ 4. Equation r̂ at the bottom of each plot.\", \"page\": 5, \"index\": 3, \"width\": 526, \"height\": 398}]"
motivation: 现有氮肥响应模型依赖预定义形式或黑箱方法，限制了对站点特定功能关系的解释与发现。
method: 采用Transformer多集符号骨架预测策略构建多样输入子集并强制一致性，恢复符号骨架后通过遗传算法拟合数据。
result: 合成与真实数据实验证明，方法能恢复正确表达式，拟合误差低于传统模型，并捕捉不同管理区的多样行为。
conclusion: 神经符号回归可有效发现站点特定氮响应曲线，在数据稀缺时鲁棒，有助于精准农业决策。
---

## 摘要
精确建模作物对氮肥施用的响应是精准农业中的一项基本挑战，因为它影响经济回报和环境可持续性。现有方法要么依赖预定义的参数形式，要么采用不透明的机器学习模型，限制了它们从数据中解释或发现特定地点功能关系的能力。在这项工作中，我们提出一种神经符号回归（SR）方法，在不预设函数形式的情况下学习参数化氮响应曲线。我们的方法集成了基于transformer的多集符号骨架预测策略，能够发现跨多个子域或管理区的共享功能结构。通过构建多样化的输入子集并强化彼此间的一致性，该方法恢复出稳健的符号骨架，随后使用遗传算法将其拟合到观测数据。该框架首先在合成的一维问题上进行评估，以考察其在不同认知不确定性水平下的鲁棒性。结果表明，即使在数据稀缺的情况下，所提出的SR方法也能恢复正确的表达式。我们在本工作中展示了将方法应用于真实冬小麦数据的结果，为田间不同管理区学习到不同的参数化氮响应曲线。结果显示，发现的表达式不仅拟合误差低于二次平台和指数函数等传统模型，而且捕捉到了跨空间区域的不同功能行为。这表明神经符号回归具有发现特定地点农学关系并支持精准农业中知情决策的潜力。

## Abstract
Accurately modeling crop response to Nitrogen (N) fertilization is a fundamental challenge in precision agriculture, as it impacts both economic returns and environmental sustainability. Existing approaches either rely on predefined parametric forms or opaque machine learning models, limiting their ability to interpret or discover site-specific functional relationships from data. In this work, we propose a neuro symbolic regression (SR) approach to learn parametric N-response curves without assuming a predefined functional form. Our approach integrates a transformer-based Multi-Set Symbolic Skeleton Prediction strategy, enabling the discovery of shared functional structures across multiple subdomains or management zones (MZs). By constructing diverse input subsets and enforcing consistency across them, the method recovers robust symbolic skeletons that are subsequently fitted to observed data using a genetic algorithm. This framework was first evaluated on synthetic one-dimensional problems to assess its robustness under varying levels of epistemic uncertainty. The results demonstrate the ability of the proposed SR approach to recover correct expressions even in data-scarce regimes. In this work, we present the results of applying our method to real-world winter wheat data, learning distinct parametric N-response curves for different MZs within a field. The results show that the discovered expressions not only achieve lower fitting errors than traditional models such as quadratic-plateau and exponential functions, but also capture diverse functional behaviors across spatial regions. This demonstrates the potential that neuro SR has to enable the discovery of site-specific agronomic relationships and support informed decision-making in precision agriculture.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题背景**：精准农业中，准确建模作物对氮肥（N）的响应曲线至关重要，直接影响经济效益与环境可持续性。传统方法或依赖预定义参数函数（如二次平台、指数模型），或使用不透明的机器学习模型，难以从数据中解释或发现特定地点的功能关系。
- **研究动机**：希望从实际数据中自动发现不同田间管理区（MZ）的氮响应数学表达式，既不预先假定函数形式，又能提供可解释的、站点专用的模型，以帮助农艺决策（如计算经济最优施氮量）。
- **整体含义**：提出一种神经符号回归（Neuro-symbolic regression）方法来学习参数化氮响应曲线，将Transformer的多集符号骨架预测与遗传算法拟合相结合，实现从黑箱神经网络中提取可解释的、管理区特定的数学关系。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：利用基于Transformer的“多集符号骨架预测”（Multi-Set Symbolic Skeleton Prediction, MSSP）策略，从多个数据子集中发现共享的符号骨架（即带常数占位符的表达式结构）；再通过遗传算法（GA）拟合系数得到最终表达式。
- **关键技术细节**：
  - **符号骨架定义**：$e(x_v) = c_1(\log x_1) + c_2$ 等形式，其中 $c_i$ 代表待定常数或其它变量的函数。
  - **多集构造**：对于1D问题，从原始曲线不同随机子区间采样构造多个不同输入集；对于管理区问题，随机选择同一MZ内的多条氮响应曲线来构建集合。所有集合都共享同一底层函数结构，强化了骨架的通用性和鲁棒性。
  - **骨架预测**：使用预训练的多集Transformer $g(\cdot)$ 处理这些集合，输出候选符号骨架（beam size $n_B$，生成 $n_{\text{cand}}$ 个候选）。
  - **系数拟合与选择**：用遗传算法优化器 `fitCoefficients` 将候选骨架拟合到观测数据，选择均方误差（MSE）最小的作为最终表达式。
- **算法流程**（文字描述）：
  1. 输入观测数据 $D$、测试域网格及预测响应 $\hat{y}_{\text{test}}$、多集Transformer $g$、子集数量 $N_S$、每集点数 $n$、候选数 $n_{\text{cand}}$ 等。
  2. 重复 $n_{\text{cand}}$ 次：从随机输入区间选取子集（必要时线性插值），构造集合 $\mathcal{D}$；用 $g$ 生成骨架候选。
  3. 对所有候选骨架进行系数拟合，选取MSE最低的表达式作为结果。

### 3. 实验设计：数据集 / 场景、基准与对比方法
- **合成1D问题验证**：
  - 场景：三个合成函数 `cos`、`hetero`、`cosqr`，均受异方差噪声影响。
  - 设计：初始数据集不完整（高认知不确定性），在自适应采样框架ASPINN下迭代（每次新增5个样本），共进行50次迭代。观察符号回归发现的表达式随不确定性降低的演变。
  - 对比基准：主要评估自身表达式是否收敛到真实函数，并报告MSE；未直接对比其他SR方法。
- **真实冬小麦数据应用**：
  - 数据：一块冬小麦旱地（Field A），预训练的2D-3D卷积神经网络生成所有田间点的氮响应曲线，并基于曲线形状相似性聚为4个管理区（MZ）。
  - 方法对比：对每个MZ，本文提出的神经符号回归（SeTGAP变体）学习到的表达式 vs. 传统预定义模型（二次平台 `c1 + c2 (min(x, c3) + c3)^2`、指数 `c1 (1 - exp(c2 + c3 x)) + c4`）。
  - 评估指标：每个MZ内所有点的平均拟合误差 $\bar{r}$。

### 4. 资源与算力
- 论文未明确说明所使用的GPU型号、数量或训练时长。
- 但提到使用预训练的“Multi-Set Transformer”，其训练细节在原始参考文献[11]中，本文未重复描述具体算力消耗。

### 5. 实验数量与充分性
- **合成实验**：3个不同函数，每个在自适应采样50次迭代中生成表达式，总计150个表达式评估点。同时报告了迭代过程中的MSE和定性曲线对比。
- **真实数据实验**：1块田的4个管理区，比较3种方法（本方法、二次平台、指数）各MZ的平均拟合误差。
- **充分性评价**：
  - 合成实验逐步展示了不确定性降低时方法的稳健性，设计合理，能验证多集策略在低数据情况下的优势。
  - 真实案例仅在一块田上验证，虽证明了概念可行性，但样本量偏少，缺乏多田地、多作物、多气候条件的泛化性测试。
  - 未见详细的消融研究（如不同 $N_S$、$n_{\text{cand}}$ 的影响）或与其他神经符号回归方法的横向比较。
- **客观性与公平性**：对比的传统模型为常用农学模型，对比公平；但未纳入其他SR或可解释机器学习方法。

### 6. 论文的主要结论与发现
- 所提多集符号回归方法在合成数据上，即使早期数据稀缺、不确定性高，最终也能收敛到正确函数形式；局部噪声或模型误差不会破坏骨架发现。
- 在真实冬小麦田上，为各管理区学习到的定制表达式（如 `c1 + c2 tanh(c3 + c4 x)`）不仅拟合误差低于传统固定函数模型，而且揭示了不同空间区域氮响应的多样性（如饱和平台出现的位置、斜率等）。
- 神经符号回归能够在精准农业中从黑箱模型提取可解释的、站点特定的农学关系，具备支持优化决策的潜力。

### 7. 优点：方法或实验设计上的亮点
- **无需预定义形式**：打破了传统农学模型必须预设函数类型的限制，让数据驱动发现函数结构。
- **多集策略对抗不确定性**：通过在不同子域/多条曲线上寻找共享骨架，有效缓解了局部数据稀疏和认知不确定性带来的过拟合风险。
- **可解释性输出**：最终产生简洁的数学表达式，可直接用于分析各管理区的氮响应特征和计算经济最优施氮量。
- **合成验证设计巧妙**：利用自适应采样框架动态展示符号回归的鲁棒性，清晰说明了方法在不确定性下降时的行为变化。
- **真实应用贴合需求**：结合管理区划分，给出了对田间变异性有洞察力的定制函数。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验覆盖有限**：真实世界测试仅基于一块冬小麦田，无法充分论证跨气候、土壤、作物的泛化能力。合成实验也只有一维函数，未涉及多维农学因素交互。
- **缺乏横向方法对比**：没有与近年来其他神经符号回归（如DSR、SymbolicGPT）或基因编程方法进行对比，难以评估方法的相对先进性。
- **预训练模型依赖**：依赖特定的多集Transformer，其训练语料可能引入偏差，对未见过的函数结构泛化性未作分析。
- **应用域限制**：仅处理1D氮响应曲线，且需要预先定义管理区。实际中管理区划分和曲线生成依赖于前序神经网络，形成管道串行风险，若前面步骤出错，会影响最终表达式质量。
- **算法细节影响不明**：缺少对关键超参数（如子集大小、候选数量、集数）的敏感性分析。
- **解释性未深度利用**：虽生成了可读的数学式，但未进一步验证这些表达式在农学决策（如经济最优氮计算）中的实际效用差异。

（完）
